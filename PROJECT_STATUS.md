# Zaexa — تجمیع‌کننده‌ی DEX روی Base

> **این فایل حافظه‌ی پروژه است.**
> در هر چت جدید، **اول همین فایل را آپلود کن** و بگو: «این وضعیت پروژه است، بخوان و ادامه بده.»
> مدل حافظه‌ی بین‌چتی ندارد — هرچه اینجا نوشته نشده باشد، فراموش می‌شود.
>
> مسیر پروژه روی سیستم کاربر: `~/web3_tools` در WSL Ubuntu.
> آخرین به‌روزرسانی: **۲۴ سپتامبر ۲۰۲۶، دورِ پنجم** — ربیِ قفل «Already processing unlock» می‌داد: ethers
> در getSigner یک درخواستِ دومِ حساب می‌فرستاد؛ حذف شد. لغو در والت حالا یک پیامِ ساده است (بخش ۰).
> پیش از آن: 🔴 فروشِ cbBTC «۳۶ از ۳۶ کوت شکست» می‌داد:
> دسته‌ی multicall از سقفِ گازِ eth_call رد می‌شد؛ حالا نصف می‌شود. Disconnect اجازه را در متامسک هم لغو
> می‌کند؛ درخشش از اپ برداشته شد (بخش ۰).
> پیش از آن: یادداشتِ کوتِ معکوس هم داخلِ جای ثابت رفت؛
> کارت در ۱۱ حالت روی دسکتاپ و گوشی دقیقاً هم‌قد است؛ درخششِ پس‌زمینه در اپ ثابت شد (بخش ۰).
> پیش از آن: هاله‌ی کارت برداشته شد، درخششِ پس‌زمینه‌ی
> صفحه‌ی معرفی روی اپ (آزمایشی)، موجودی فقط سمتِ پرداخت، و «جای ثابت» برای نوارِ مسیر و اعلان‌ها (بخش ۰).
> پیش از آن: کارتِ سواپ بازطراحی شد: برچسبِ بررسیِ فروش بالای
> توکنِ دریافتی، نوارِ تقسیمِ مسیر به رنگِ هر صرافی، کارتِ نتیجه بعد از سواپ، و هاله‌ی قابل‌حذف (بخش ۰).
> پیش از آن (شبِ ۲۳ سپتامبر، بخش ۰ی): 🔴 ریشه‌ی واقعیِ `v4:unproven` روی SPIKE پیدا شد:
> هوک نبود؛ ایندکس از ۲۰ استخرِ نسخه ۴ سه‌تای **تازه‌ترین** (خاکی) را برمی‌داشت، و «همه‌ی کلیدهای
> ایندکس‌شده خالی‌اند» به‌غلط «استخر خالی است» خوانده می‌شد. رفع شد (بخش ۰).
> پیش از آن (عصرِ ۲۳ سپتامبر، بخش ۰ط): 🔴 یک منفیِ کاذبِ منتشرشده ($SPIKE) رفع شد:
> حکمِ منفی‌ای که فقط به نسخه ۴ تکیه دارد اثباتِ خالی‌بودن می‌خواهد، و فروشِ ۳+ فروشنده در
> ساعتِ گذشته هر حکمِ منفیِ Base را وتو می‌کند. هدرِ `/pairs` حالا عیناً هدرِ اپ است (با
> اتصالِ واقعیِ کیف‌پول)، هدر در هر سه صفحه شناور است، انیمیشنِ هیرو بازطراحی شد، و یک
> جدولِ آدرسِ سولانا توکن‌های پامپ‌فان را بررسی‌پذیر کرد. رابط **۴۴۳ کاوشگر**. **بخش ۰ را بخوان.**
> پیش از آن (۲۲ و ۲۳ سپتامبر، بخش ۰ح): سقفِ درخواست رفع و زنده تأیید شد و صفحه‌ی جفت‌ها
> بازطراحی شد.
> پیش از آن (۲۱ سپتامبر، بخش ۰ز): 🔴 بیشترِ «نامعلوم»ها تقصیرِ سقفِ ۵۰ درخواستیِ
> کلادفلر بود، نه توکن؛ گذرِ ساعتی حالا روی سقف می‌ایستد و ردیفِ آلوده ذخیره نمی‌کند.
> به‌علاوه‌ی استثنای پوششِ نسخه ۴، بازپرسیِ نامعلوم‌ها، لینکِ صفحه‌ی جفت‌ها، و گزارشِ
> گذر روی `/vd/passes`.
> پیش از آن (۲۰ سپتامبر، بخش ۰و): ✅ شش تغییر زنده و تأییدشده: پنلِ
> ریسکِ مرورگر دیگر بدونِ شاهدِ پوشش اتهام نمی‌زند (باگ ۱۵ و ۱۶)، نوارِ صفحه‌ی
> توکن دیگر در اسکلت گیر نمی‌کند و ادعای شبیه‌سازیِ بی‌قید ندارد (باگ ۱۳)، هر
> حکمِ منفی می‌گوید استخر خالی شده یا نه، هر حکمِ مثبت یک ساعت بعد یک‌بار پیگیری
> می‌شود، درصدِ بازگشتِ یک کوتِ صد دلاری ذخیره می‌شود، و ردیف‌های سولانا در همان
> سندِ روزانه می‌نشینند. **بخش ۰ را بخوان.**
> پیش از آن (۱۵ سپتامبر): ✅ کلیدِ واقعیِ استخرِ یونی‌سواپ
> نسخه ۴ حالا از لاگِ زنجیره ایندکس می‌شود، مرحله‌ی USDC دیگر پشتِ یک منفیِ
> تمیز حبس نیست، و یک توکنِ واقعی روی سایتِ زنده از «نامعلوم» به «فروش ممکن»
> رسید. ضمناً اثبات شد که عددِ ذخیره‌ی گِکوترمینال شاهدِ نقدینگی **نیست**.
> همان روز: **متنِ گزارشِ روزانه** زنده شد (`/report/<تاریخ>.txt`)، و هر حکمِ «نامعلوم» روی Base
> حالا **دلیل** دارد (`why`) — بخش ۰، دو زیربخشِ اول.
> **۱۹ سپتامبر:** ریشه‌ی «هیچ توکنی هرگز فروش‌ناپذیر اعلام نمی‌شود» پیدا و رفع شد، و
> بعدش نسخه ۴ هم شاهد پیدا کرد. بخش ۰، دو زیربخشِ اول.
> پیش از آن (۱۳ سپتامبر): 🔴 یک باگِ متناوب که حکمِ **همه‌ی** توکن‌های Base را
> بی‌صدا خراب می‌کرد پیدا و رفع شد و زنده تأیید شد؛ `batch-failed` دیگر
> وضعیتش را می‌گوید — بخش ۰د.
> پیش از آن (۷ تا ۱۲ سپتامبر): گزارش روزانه و صفحه‌ی جفت‌های تازه
> زنده شدند، حکمِ غلطِ «فروش ممکن نیست» ریشه‌کن شد، تمِ روشن بازسازی شد، و
> صفحه‌ی معرفی شروع به شمردنِ تبدیل کرد — بخش ۰ج.
> پیش از آن (۴ و ۵ سپتامبر): صفحه‌ی معرفی روی `/` نشست و اپ به `/app` رفت،
> برچسب عمومی BaseScan تأیید شد، و فوترِ دو صفحه یکی شد — بخش ۰ب.
> پیش از آن (۲ سپتامبر): گیرنده‌ی خطای سراسری، محدودکننده‌ی نرخ، هدرهای امنیتی،
> و تعویض RPCهای مرده — بخش ۰الف.
> پیش از آن (۱ سپتامبر): قرارداد **v5** دیپلوی و روی BaseScan تأیید شد
> (Slipstream + قلاب آداپتور، بخش ۱۱د)، لوگوتایپ v1.3 روی هدر سایت و کارت OG
> نشست، و دو نگهبان تازه (`[dex parity]` و `[og etag]`) دو شکافِ خاموش را بستند.
>
> ⚠️ **قاعده‌ی این فایل: تنها آدرس قراردادی که اینجا معتبر است، همانی است که در
> بخش ۱ آمده.** نسل‌های قبلی فقط یک جا فهرست می‌شوند — جدول «قراردادهای رها
> شده» — و آن فهرست برای این است که بدانی به کدام آدرس *approve* نکنی، نه برای
> اینکه به آن ارجاع بدهیم. هیچ بخش دیگری نباید آدرس بازنشسته را تکرار کند.

---

## ۰. کارِ ۲۴ سپتامبر ۲۰۲۶، دورِ پنجم — ربی

- ✅ فروشِ cbBTC بعد از دیپلوی روی سایتِ زنده با کیف‌پولِ حسام کوت گرفت (رفعِ دورِ چهارم کار کرد).
- 🔴 **«Already processing unlock. Please wait.»** (ربی بعد از بازکردنِ مرورگر، رمز را می‌زدی و هیچ
  اتفاقی نمی‌افتاد): در ethers ۶٫۱۳٫۴ِ خودمان خوانده شد — `BrowserProvider.getSigner()` بعد از
  `eth_requestAccounts`ِ ما، `eth_accounts` و اگر خالی بود **یک `eth_requestAccounts`ِ دوم** می‌فرستد.
  ربی هنوز وسطِ باز کردنِ قفل بود و دومی را رد می‌کرد؛ اتصال همان‌جا می‌افتاد.
  - `makeSigner`: `new E.JsonRpcSigner(provider, address)` — هیچ درخواستی به والت (روی ethersِ واقعیِ
    سایتِ زنده سنجیده شد: صفر درخواست).
  - `requestWalletAccounts`: اگر خودِ درخواستِ اول «در حالِ قفل/در انتظار» (`-32002`) برگردد، پنجره‌ی تازه
    باز نمی‌شود؛ تا ۳۰ ثانیه فقط `eth_accounts`. ردِ کاربر (`4001`) فوراً برمی‌گردد.
- **لغو در والت** (اتصال، approve، سواپ) حالا یک جمله است: «Cancelled in your wallet. Nothing was sent.» —
  نه جعبه‌ی خطا. رویداد همان `swap:fail` (نامِ تازه در `/ev` بی‌اجازه نه).
- ⚠️ **باز:** حسام گفت بعد از لغوِ تراکنش، ربی چند بار دوباره باز شد تا مرورگر را بست. در کدِ ما مسیری که
  بعد از لغو درخواستِ امضای دوم بفرستد پیدا نشد (سواپ و approve هر کدام دقیقاً یک `eth_sendTransaction`؛
  شبیه‌سازی و تخمینِ گاز پنجره باز نمی‌کنند). اگر تکرار شد: متنِ خطا و «Technical details» لازم است.
- `[wallet unlock]`: یک درخواست، صبرِ بی‌پنجره روی قفل، ردِ فوری، signer بی‌درخواست. شکستنِ عمدی
  (برگشت به getSigner) → قرمز. رابط **۴۵۴**. ⚠️ با ربیِ واقعی سنجیده نشده.

## ۰ن. کارِ ۲۴ سپتامبر ۲۰۲۶، دورِ چهارم — فروشِ cbBTC، Disconnect، و درخشش

### 🔴 «The network did not answer. 36 of 36 quotes failed» — شبکه نبود، گاز بود
روی سایتِ زنده بدونِ کیف‌پول بازتولید شد: **هر** کوت با cbBTC در ورودی (به ETH یا USDC، هر مبلغ)
۳۶ از ۳۶ شکست؛ برعکسش سالم. تک‌تک پرسیده، هر ۱۸ صرافی جواب می‌دهند (Slipstream تیک ۵۰/۱۰۰/۲۰۰/۲۰۰۰
ریوِرت). ولی کلِ دسته‌ی `aggregate3` برمی‌گشت: `-32003 out of gas: gas required exceeds: 50000000`
— چند کوتِ Slipstream روی استخرِ بی‌نقدینگی تقریباً همه‌ی گاز را می‌سوزانند و Multicall3 سقفِ گازِ
جدا برای هر فراخوانی ندارد.
- **رفع:** `quoteMany` → `quoteBatch`: اگر خودِ دسته با شکستِ اجرایی (`isRevert` و نه
  `cannotAsk`) برگردد، نصف می‌شود و دوباره پرسیده می‌شود؛ فقط فراخوانیِ پرخرج «نامعلوم» می‌ماند.
  خطای انتقال هرگز نصف نمی‌شود. `attempted` یک بار شمرده می‌شود.
- **سنجشِ زنده** (همان کد در مرورگر روی zaexa.com تزریق شد): cbBTC→ETH ۰٫۰۰۰۰۲۵۲۷ → ۰٫۰۰۰۷۹۰۶۶ ETH
  از PancakeSwap V3 ۰٫۰۱٪، برچسب «Sellable · 99.9% back»، ۱۱ multicall در کل.
- ⚠️ احتمالاً بخشی از «? Not checked»های سمتِ فروش هم همین بود (بررسیِ خروج هم از `quoteMany` می‌رود).
  سمتِ Worker (حکمِ سرور) دسته‌ی JSON-RPC از eth_callهای جدا است و این سقف را ندارد.
- `[quote split]`: دسته‌ی ۱۵تایی با یک فراخوانیِ «سقفِ گاز» → فقط همان نامعلوم، ۱۴تای دیگر جواب،
  ۹ فراخوانی؛ خطای انتقال → یک فراخوانی و بدونِ نصف‌کردن. شکستنِ عمدی (بدونِ نصف‌کردن) → قرمز.

### Disconnect واقعی
حسام: بعد از Disconnect، متامسک سایت را هنوز وصل می‌داند و دفعه‌ی بعد خودبه‌خود وصل است. حالا
`disconnect()` (اپ و `/pairs`) بعد از پاک‌کردنِ وضعیت، `wallet_revokePermissions` (EIP-2255) را
بهترین‌تلاش صدا می‌زند؛ موفق → «Access was removed in your wallet too»، ناموفق → متنِ قدیمی. پرچمِ
`zaexa.disconnected` در هر دو حالت می‌ماند. `[wallet revoke]` هر دو شاخه را می‌سنجد (فقط اپ؛
`/pairs` آزمون ندارد). ⚠️ با متامسکِ واقعی سنجیده نشده.

### بقیه
- **درخشش از اپ برداشته شد** (تصمیمِ حسام)؛ صفحه‌ی معرفی دارد. `[page glow]` حالا نبودنش را می‌سنجد.
- یادداشتِ کوتِ معکوسِ کهنه بعد از پاک‌شدنِ فیلدها می‌ماند → در `paintRouteIdle` پاک می‌شود.
- رابط **۴۵۳**.

## ۰م. کارِ ۲۴ سپتامبر ۲۰۲۶، دورِ سوم — کارت هنوز می‌پرید

حسام روی سایتِ زنده دید کارت هنوز بالا-پایین می‌شود. علت: یادداشتِ «Solved for your target» (وقتی
مبلغ در فیلدِ مقصد نوشته می‌شود) **بیرونِ** جای ثابت بود و ~۷۰px اضافه می‌کرد.
- `#revNote` داخلِ `#statusSlot`؛ اولویت: اعلان > یادداشت > نوارِ مسیر؛ یادداشت حداکثر دو خط
  (متنِ کامل در `title`) و کوتاه‌تر شد.
- رزروِ جای ثابت روی گوشی هم ۶۴px (اعلانِ دوخطیِ اثرِ قیمت ۶۳px است).
- `[card steady]` حالا ۱۱ حالت: + کوتِ معکوس، معکوس با اعلان، و برگشت. دسکتاپ همه ۵۵۵/۶۰۷،
  گوشی همه ۵۵۵/۵۹۴. شکستنِ عمدی (یادداشت نوار را پنهان نکند) → ۶۰۲ و قرمز.
- **درخششِ پس‌زمینه در اپ ثابت شد** (بدونِ حرکت) — نظرِ من: می‌ماند چون اپ و صفحه‌ی معرفی را یکی
  می‌کند، ولی چهار لکه‌ی blur(110px)ِ متحرک در ابزاری که ساعت‌ها باز است هزینه‌ی دائمیِ
  رنگ‌آمیزی دارد. صفحه‌ی معرفی حرکتش را نگه داشت.
- `[canvas resize]` (صفحه‌ی معرفی) در ۲ از ۳ اجرا می‌لرزید: مرکزِ رسم فقط در فریمِ بعدی به‌روز
  می‌شود. حالا تا ۳ ثانیه برای همان شرط صبر می‌کند؛ آستانه دست نخورد.

## ۰ل. کارِ ۲۴ سپتامبر ۲۰۲۶، دورِ دوم — بازخوردِ حسام روی سایتِ زنده

- **هاله‌ی کارت حذف شد** (`[swap halo]` حالا نبودنش را می‌سنجد).
- **درخششِ پس‌زمینه‌ی صفحه‌ی معرفی روی اپ** (`.page-glow` با چهار لکه، اولین فرزندِ `body`) —
  آزمایشی؛ شفافیت با سلکتور نه توکن (۰٫۱۴ روشن / ۰٫۲۶ تیره). **حذف = آن بلوکِ CSS و آن div.**
  `[page glow]`: ثابت، z-index −1، کلیک‌نگیر، و جای کارت را تکان نمی‌دهد.
- **موجودی فقط سمتِ پرداخت:** `#balOut{display:none}` (عنصر می‌ماند چون JS می‌نویسدش).
- **جای ثابت (`#statusSlot`)** = `#routeBar` + `#notices`؛ اعلان جای نوار را می‌گیرد
  (`:has(#notices:not(:empty))`)؛ رزرو ۶۴px دسکتاپ و ۶۰px گوشی. نوارِ مسیر همیشه دیده می‌شود
  (پیش از کوت: «Enter an amount to see the route» از `paintRouteIdle`). راهنمای مسیر یک‌خطی.
  `[card steady]`: ارتفاعِ کارت و جای دکمه در ۸ حالت — دسکتاپ ۵۵۵/۶۰۷، گوشی ۵۵۲/۵۹۰ (اعلان ۵۵۱).
- 🔴 دامی که ایجنت پیدا کرد: margin-topِ نوار از داخلِ جای ثابت بیرون می‌ریخت (margin collapse)
  فقط وقتی نوار دیده می‌شد → دکمه ۴px می‌پرید. حاشیه روی خودِ `#statusSlot` رفت.
- رابط **۴۵۱**. ⚠️ اسکرین‌شاتِ تمام‌صفحه درخشش را فقط تا ارتفاعِ پنجره نشان می‌دهد (عنصرِ fixed) — باگ نیست.

## ۰ک. کارِ ۲۴ سپتامبر ۲۰۲۶ — بازطراحیِ کارتِ سواپ (از روی ماکتِ تأییدشده)

ماکت در `Downloads\zaexa_swap_mock`؛ حسام رنگ‌ها را پذیرفت، برچسب را بالای توکنِ دریافتی خواست،
و هاله را «کمتر».

- **برچسبِ بررسیِ فروش** (`#sellPill`، تنها نویسنده `paintSellPill`): از همان `x`ِ `renderExit`.
  قابل‌فروش (سبز/نارنجی/قرمز بسته به هزینه‌ی رفت‌وبرگشت، با «٪ back»)، «Can't sell now»،
  «Checking exit…»، «? Not checked». 🔴 نامعلوم/غیرمعقول هرگز `ok` نیست و «Sellable» ندارد.
  لبه‌ی راستش = لبه‌ی راستِ `#tokOutBtn`. `.sellPill[hidden]{display:none}` لازم است — ویژگیِ
  hidden به `display:inline-flex` می‌بازد (همان الگوی `.clr[hidden]`).
- **نوارِ تقسیمِ مسیر** (`#routeBar`) از همان سهم‌های `renderPlan`؛ رنگ‌ها در `VENUE_COLOR`
  (یونی‌سواپ `#FF007A`، آئرودروم `#2E6BFF`، پنکیک `#1FC7D4` تأییدِ مالک؛ بقیه انتخابِ من). راهنما
  همیشه نام و درصد دارد.
- **کارتِ نتیجه** (`#swapDone` + کلاسِ `isDone`) بعد از رسیدِ موفق؛ مبلغ با «≈» چون کوت است نه
  رسیدِ پارس‌شده. «Swap again» و هر کوتِ تازه آن را می‌بندد.
- **هاله:** دو سایه‌ی رنگی روی `section.card.swapCard` (کارت `overflow:hidden` دارد، پس شبه‌عنصر
  بریده می‌شد). **حذف = پاک‌کردنِ همان دو خطِ CSS.** هیچ توکنی به بلوکِ تم اضافه نشد.
- حرکت‌ها فقط زیرِ `prefers-reduced-motion:no-preference`. شمارشِ عدد حذف شد (مقدارِ `#amtOut`
  را عوض می‌کرد).
- کاوشگرهای تازه: `[sell pill]`، `[route bar]`، `[swap done]`، `[swap halo]` — رابط **۴۴۸**. هر شش
  گارد عمداً شکسته و قرمز دیده شد. ⚠️ `[canvas resize]` زیرِ بارِ دو اجرای هم‌زمان یک بار لرزید.
- آزموده نشده: اتصالِ واقعیِ کیف‌پول و یک سواپِ واقعی تا کارتِ نتیجه.

## ۰ی. کارِ شبِ ۲۳ سپتامبر ۲۰۲۶ — انتخابِ استخرِ نسخه ۴، و «استخرِ خالی» که فقط سه استخر را دیده بود

**همه‌ی سنجش‌ها زنده، از مرورگرِ داخلیِ Cowork روی مبدأ zaexa.com، با کنترلِ مثبت.**

### 🔴 چه دیدیم (SPIKE، `0x1685981068dc0ec45ee1d5a28ef051059e42a0f3`)
- گِکوترمینال برای SPIKE **۲۰ استخرِ نسخه ۴** می‌دهد (یک صفحه‌ی پر). بزرگ‌ترین با **wtCOIN**
  جفت است (هوک‌دار، کارمزدِ پویا) — ضدجفتِ نامرتبط، عمداً پروب نمی‌شود.
- `v4PoolsFromGt` تازه‌ترین‌ها را اول می‌گذاشت و سه‌تا برمی‌داشت → سه استخرِ ۲۷ تا ۸۰ دلاری
  (ETH 8.9%، ETH 10%، USDC 4.87%) ایندکس شدند؛ هر سه بی‌هوک، و کوتر رویشان `no-liquidity`.
- استخرهای واقعی ایندکس نشده بودند: **ETH 1.6%** (fee 16000، tickSpacing 160، بی‌هوک، ۴۴۵
  فروشنده در ۲۴ ساعت) و **USDC 5%** (fee 50000، tickSpacing 500، بی‌هوک، ۱۵۲ فروشنده).
  کوترِ مستقیم: معادلِ ۱۰۰ دلار SPIKE → **۱۰۵٫۸ USDC** و **۰٫۰۳۹۶ ETH**. کنترلِ منفی: استخرِ
  خاکی روی همان کوتر ریوِرت داد.
- 🔴 **بدتر:** `/vd/<SPIKE>` زنده `{"v":"nosell","cause":"empty-pool"}` می‌داد. `v4PoolsEmpty`
  «هر کلیدِ ایندکس‌شده صفر است» را «استخرهای توکن خالی‌اند» می‌خواند، و قاعده‌ی ۱ِ
  `finalizeBaseNosell` (خالی برنده است) از گاردِ عصر رد می‌شد. گزارشِ عمومی سالم بود
  (`v4:unproven`)، ولی کارتِ توکن و فالوآپِ «کشیده‌شده» در معرض بودند.

### ✅ رفع (شاخه‌ی `v4-pool-selection`، سه فایل)
- **`v4PoolsFromGt(pools, tokenAddr, counters)`**: رتبه با `transactions.h24.sellers` نزولی
  (تساوی → تازه‌ترین)؛ ردیفی که ضدجفتش در `VD_V4_STAGE_COUNTERS` نیست کنار می‌رود؛ ضدجفتِ
  نامعلوم (شکلِ ناشناخته) **هرگز** فیلتر نمی‌شود. SPIKE حالا: ETH 1.6%، USDC 8%، USDC 5%.
- **دلیلِ تازه `no-v4-counter`** (میسِ ۶ساعته، بدونِ هیچ تماسِ RPC): توکن فقط استخرِ نسخه ۴ با
  ضدجفتِ نامرتبط دارد.
- **`complete`** کنارِ کلیدها ذخیره می‌شود و فقط وقتی true است که (الف) صفحه‌ی گِکوترمینال
  پر نبود (`V4_GT_PAGE_SIZE`=۲۰)، (ب) هیچ استخری با فیلتر یا سقف کنار نرفت، (ج) هر استخرِ
  انتخاب‌شده کلید داد. **`v4PoolsEmpty` بدونِ `complete:true` بی‌درنگ `null` می‌دهد.**
- `/vd/v4/<addr>` حالا `complete` را هم نشان می‌دهد.

### ⚠️ پیامدِ عمدی
ورودی‌های قدیمیِ انبار (بدونِ `complete`) تا دوباره ایندکس نشوند «خالی» ثابت نمی‌کنند —
یعنی `empty-pool` و فالوآپِ «کشیده‌شده» برای توکن‌های نسخه ۴ِ قبلی تا حداکثر ۳۰ روز
نامعلوم می‌مانند. پیشوندِ کلیدِ انبار عوض نشد، چون بازایندکسِ همه در گذرِ ساعتی به سقفِ
۵۰ درخواست می‌خورد. `/vd/v4/<addr>` هر بار دوباره ایندکس می‌کند.

### سوییت‌ها
Worker **۱۱۲ بخش** (+`[v4 pool selection]`) · رابط **۴۴۴** · هر پنج گارد عمداً شکسته و قرمز
دیده شد (قاعده‌ی صفحه‌ی پر با فیکسچرِ جدا، چون در فیکسچرِ SPIKE شرطِ (ب) آن را می‌پوشاند).
`og_live_test` در کانتینر اجرا نشد (شبکه ندارد).

### 🎯 بعد از دیپلوی
1. `/vd/v4/0x1685981068dc0ec45ee1d5a28ef051059e42a0f3` → باید سه کلیدِ بالا با `complete:false`.
2. `/vd/0x1685…a0f3` → دیگر `empty-pool` نه؛ یا `sell` یا نامعلوم.

## ۰ط. کارِ عصرِ ۲۳ سپتامبر ۲۰۲۶ — منفیِ کاذبِ منتشرشده، هدرِ یکسان، انیمیشنِ هیرو، و جدولِ آدرسِ سولانا

**همه‌ی سنجش‌های زنده از مرورگرِ داخلیِ Cowork روی مبدأ zaexa.com.** کانتینر به سایت،
RPCها و ژوپیتر شبکه ندارد.

```
01be84d  pairs header = app header; sticky glass header; theme set before first paint; new landing hero animation; swap settings persist
d285bb6 … b9331ad  hero animation rounds + landing mobile fixes
b931c3b  Base nosell: v4-only needs an empty-pool proof, 3+ recent sellers veto, stored rows guarded at publish; landing canvas stays in its frame on resize; CSP frame-src for WalletConnect Verify
16e23c8  logo link hugs its ink: the tap highlight was 3x the mark on phones
4e56079  Solana: own address lookup table for Pump.fun roundtrips, fetched only when the tx does not fit
```

### 🔴 منفیِ کاذبِ منتشرشده — رفع شد و زنده تأیید شد

بازرس گزارش داد `$SPIKE` (`0x1685981068dc0ec45ee1d5a28ef051059e42a0f3`) در
`/report/2026-09-23.txt` «no sell route quoted» آمده. زنده سنجیده شد: تنها استخرش روی
نسخه ۴ با **۲۰۶ هزار دلار** نقدینگی و **۲۹۶ فروشنده‌ی یکتا در ساعتِ گذشته**. سه توکنِ
دیگرِ همان روز (ANANSI، VOFS با ۹۰ هزار دلار، COMSHOP) همان الگو را داشتند.

- **ریشه:** شاهدِ منفی از صرافی‌هایی آمد که توکن اصلاً رویشان استخر ندارد، و کوترِ
  نسخه ۴ روی استخرِ هوک‌دار فقط «فروش می‌رود» را می‌تواند ثابت کند. شاخه‌ی
  `v4Listed && v4Keyed` در `ogFetchVerdictDetail` بدونِ اثباتِ خالی‌بودن `nosell` می‌داد.
- **قاعده‌ی تازه — `finalizeBaseNosell` در `worker/index.js`، به این ترتیب:**
  1. استخرِ خالی ثابت شد → `nosell` با `cause:"empty-pool"` (مثلِ WHEN).
  2. **کنترلِ منفی از زنجیره:** اگر جمعِ `transactions.h1.sellers` روی همه‌ی استخرهای
     توکن ≥ `SELLERS_H1_VETO` (=۳) باشد → `null` با `why:"sells:recent"`. از همان بدنه‌ی
     pools که گاردِ پوشش می‌گیرد، **بدونِ درخواستِ اضافه** (`sellersH1Of`).
  3. حکمی که فقط به نسخه ۴ تکیه دارد و خالی‌بودن را ثابت نکرده → `null` با
     `why:"v4:unproven"`. (شاخه‌ی `v4Proof` — «استخرِ خودش هیچ اندازه‌ای پر نمی‌کند» —
     شاهدِ واقعی حساب می‌شود و فقط تابعِ قاعده‌ی ۲ است.)
  4. بقیه (ریوِرتِ واقعی روی صرافیِ پوشش‌داده‌شده) → همان `nosell` بدونِ cause.
- ⚠️ **با بازرس یک جا عمداً مخالفت شد:** «هیچ nosellی بدونِ cause» روی همه اعمال
  **نشد**. هانی‌پاتِ کلاسیک نقدینگی دارد و هیچ‌وقت `empty-pool` نمی‌گیرد؛ آن قاعده یافته‌ی
  اصلیِ محصول را پاک می‌کرد. آستانه‌ی ۳ (نه ۱) چون هانی‌پات‌ها یکی‌دو آدرسِ سفیدشده دارند.
- **گاردِ زمانِ انتشار — `publishGuardRow` در `worker/report.js`:** ردیف‌های ذخیره‌شده‌ی
  پیش از رفع (از جمله گزارشِ امروز) موقعِ نمایش همان قاعده‌ی ۳ را می‌گیرند — در
  `reportDocFor` (JSON)، مسیرِ متن، و حلقه‌ی `pairs:base:latest`. **انبار دست نمی‌خورد.**
- دو دلیلِ تازه در واژه‌نامه‌ی `VD_BASE_WHY`: `v4:unproven` و `sells:recent`.
- **زنده:** متنِ امروز فقط WHEN، HITCOIN و bitbank را «no sell route quoted · pool is
  empty» می‌نویسد؛ SPIKE در JSON و `/pairs.json` حالا `v4:unproven` است و `/vd/0x1685…`
  جوابِ `{"v":null,"why":"sells:recent"}` می‌دهد — کنترلِ منفی روی توکنِ واقعی شلیک کرد.
- کاوشگرِ `[false nosell]` در `worker/test.mjs` با فیکسچرِ همان پاسخِ زنده.

### ✅ هدرِ صفحه‌ی جفت‌ها = هدرِ اپ، هدرِ شناور، و فلاشِ تم

- **`/pairs` دقیقاً هدرِ اپ را دارد:** همان شناسه‌ها، کلاس‌ها، آیکون‌ها و ترتیب —
  `#srcChip`، `#shareBtn` (کپیِ لینکِ همین صفحه)، `#setPop` کامل (لغزش، مهلت، تأیید،
  RPC دلخواه، تم) و `#walletMenu` با **اتصالِ واقعی** (EIP-6963 + WalletConnect با همان
  تنظیماتِ اپ، باندل با تقاضا). صفحه‌ی جفت‌ها هرگز زنجیره عوض نمی‌کند و تراکنش نمی‌فرستد.
  زنده: وال‌کانکت روی `/pairs` کیوآر ساخت. کاوشگرِ `[header parity]` دو هدر را عنصر به
  عنصر و هندسه به هندسه می‌سنجد.
- **تنظیماتِ سواپ حالا ماندگار و بین دو صفحه مشترک‌اند** — کلیدِ `zaexa.swap.v1`
  (`{bps, custom, deadline, unlim}`). روی بارگذاری از **همان handlerهای رابط** رد می‌شود،
  پس مقدارِ نامعتبر نادیده گرفته می‌شود. (`[swap settings persist]`)
- **هدرِ شناور در هر سه صفحه:** `position:sticky`، و فقط بعد از اسکرول کلاسِ
  `scrolled` با پس‌زمینه‌ی شیشه‌ای. 🔴 دو دام: `overflow-x:hidden` روی `body` بدنه را ظرفِ
  اسکرول می‌کرد و sticky کار نمی‌کرد → `overflow-x:clip`؛ و `backdrop-filter` روی خودِ
  هدر نوارِ ثابتِ پایینِ موبایل را جابه‌جا می‌کرد → شیشه روی `::before`. (`[sticky header]`)
  ⚠️ در تمِ تیره و بالای صفحه، ~۳٪ پیکسل‌های هدرِ اپ در لبه‌ی نرمِ متن فرق دارند — علتش
  همان `overflow-x:clip` است که بدونش sticky ممکن نیست. شکل و رنگ و جا عوض نشده.
- **فلاشِ تمِ روشن هنگامِ بازشدن** در `/pairs` و `/` رفع شد: اسکریپتِ تمِ اولیه به
  `<head>` پیش از اولین `<style>` رفت (اپ از قبل درست بود). (`[theme first paint]`)
- **لینکِ لوگو** در هر سه صفحه `justify-self:start` گرفت؛ قبلاً کلِ ستونِ گرید را پر
  می‌کرد و روی گوشی کادرِ لمسش سه برابرِ نشان بود. (`[logo hit area]`)
- **CSP:** `frame-src` فقط دو میزبانِ Verify وال‌کانکت
  (`verify.walletconnect.org`/`.com`). فونت‌های `fonts.reown.com` عمداً باز نشدند.
  اثرش (کیف‌پول دامنه را «تأییدشده» نشان دهد) هنوز با یک اتصالِ واقعی دیده نشده.

### ✅ انیمیشنِ هیروی صفحه‌ی معرفی — بازطراحی با بازخوردِ مالک

- داستانِ چهارمرحله‌ای، ۹ ثانیه: کوت → تقسیم → شبیه‌سازی → تسویه؛ کادرِ ROUTE ENGINE
  مرحله‌ی فعلی را پررنگ می‌کند و EXIT VERIFIED در لحظه‌ی تأیید تپش می‌گیرد.
- 🔴 **تصمیم‌های مالک که نباید برگردد:**
  - **بیضی‌های سیگنالِ کج و چرخان** (`drawOrbits` عیناً از نسخه‌ی اول) «بهترین بخش»
    است. یک بار هم‌مرکز و ثابت شد و مالک به‌شدت ناراحت شد. دست نزن.
  - **هیچ مسیری کشیده نمی‌شود** — فقط گره‌ها (۷ صرافیِ واقعیِ اپ + IN + OUT).
    `[canvas on ellipse]` می‌شمارد که در هر فریم فقط همان ۹ نقطه کشیده شود.
  - پرتوی رادار لبه‌ی تیز ندارد (بومِ کوچکِ یک‌چهارم + `destination-in`) و تا ۱٫۱۸ برابرِ
    بیضی می‌رود تا به گره‌ها برسد.
  - حلقه‌ی کوچکی که در مرحله‌ی شبیه‌سازی دورِ لوگو پر می‌شود (`fillRingR`) می‌ماند.
  - تمِ روشن به رنگِ حرفِ «n» در «can»: `--hero1:#6B9EF8; --hero2:#9C82F9` (از
    گرادیانِ تیتر، t≈۰٫۳۳۵). تیره: `var(--viz1)/var(--viz2)` — رنگِ تیره عوض نشد.
  - نوشته‌ی وسطِ هسته **ZAEXA** است (نه BASE / LIVE).
- **موبایل:** بومِ هم‌اندازه‌ی عرض، هسته‌ی ۹۶px، بیضی‌های کشیده‌تر روی بومِ باریک،
  readoutهای جمع‌وجور در گوشه‌ها، فاصله‌ی زیرِ هیرو ۲۴px. علامتِ «?» بخشِ اعتماد با
  `cqw` اندازه می‌گیرد (`[trust glyph in circle]`). آیکونِ تم SVG شد.
- **تغییرِ اندازه‌ی پنجره:** بوم `position:absolute;inset:0` شد؛ قبلاً ارتفاعش از نسبتِ
  width/height خودِ بوم می‌آمد و بعد از کوچک و بزرگ‌کردن از قابش بلندتر می‌شد
  (`[canvas resize]`).
- برچسب‌ها هرگز زیرِ کادرهای readout نمی‌روند (`[canvas labels]` در ۸ عرض).

### ✅ جدولِ آدرسِ خودِ زَکسا روی سولانا

- **اندازه‌گیری:** همه‌ی «too-big»های سولانا توکنِ پامپ‌فانِ **تک‌مرحله‌ای** بودند (حدسِ
  «چندمرحله‌ای» غلط بود). رفت‌وبرگشت ۲۹ آدرسِ یکتا و **۱۲۸۱ بایت** در برابرِ سقفِ ۱۲۳۲؛
  ژوپیتر برای پامپ‌فان هیچ جدولی نمی‌دهد و `useSharedAccounts` کمکی نمی‌کند.
- **جدول:** `9SppR56TAG7hE8AXeb7XLDK4C8YD4snna1pb3fTnLGbn` — ۱۲ آدرسِ ثابتِ پامپ‌فان
  و ژوپیتر، ساخته‌شده ۲۳ سپتامبر با اسکریپتِ `zaexa_alt_create.sh` روی ماشینِ حسام.
  صاحب و پرداخت‌کننده یک کلیدِ **یک‌بارمصرف** است
  (`27cGbGjkBcGeP1NSLLKcpwAzVFczQdcu7TTQsFZRcwHZ`، فایلش روی ماشینِ حسام در
  `~/.config/zaexa/alt-authority.json`)، نه کیف‌پولِ اصلی. با جدول: **۹۴۳ بایت**.
- **کد:** `VD_SOL_ALT` در `worker/verdict_sol.js`؛ فقط وقتی واکشی می‌شود که پیام بدونِ
  آن جا نشود، پس برای بقیه‌ی توکن‌ها درخواستِ اضافه نیست. شکستِ واکشی → همان `too-big`.
  آزمون با پاسخِ زنده‌ی ژوپیتر برای یک توکنِ پامپ‌فانِ واقعی.

### 🔬 اندازه‌گیری‌هایی که کار را بستند

- **اثرِ حافظه‌ی pools تأیید شد:** توکنِ نسخه ۴ با حکمِ منفی پیش از رفع ۸ از ۸ بار ۷
  عمل، بعد از رفع ۹ از ۹ بار ۶ عمل.
- **«نامعلوم»ِ سولانا:** ۲۲ سپتامبر از ۹ تا ۲۱ جهانی ۲۴ از ۲۶ ردیف نامعلوم شدند، ۱۶ تا
  `deadline` (کندیِ بالادست). ۲۳ سپتامبر در همان ساعت‌ها **صفر** `deadline`. مهلتِ ۲
  ثانیه‌ای عوض **نشد**. نامعلوم‌های باقی (۹ از ۳۷) همه دلیلِ خودِ توکن: `jup:quote:400`
  (بی‌مسیر)، `too-big` (حالا با جدول)، `jup:swap-instructions:500`.

### وضعیت سوییت‌ها

Worker سبز (با `[false nosell]` و آزمون‌های جدولِ سولانا) · رابط **۴۴۳ کاوشگر** ·
`og_live_test` سبز.

---

## ۰ح. کارِ ۲۲ و ۲۳ سپتامبر ۲۰۲۶ — بودجه‌ی گذر رفع شد، و صفحه‌ی جفت‌های تازه یک برگه‌ی ابزار شد

**همه‌ی سنجش‌های زنده از مرورگرِ داخلیِ Cowork روی مبدأ zaexa.com انجام شد.**
کانتینر به سایت و به RPCها شبکه ندارد.

کامیت‌های این دو روز روی main، به ترتیب:

```
3cab07e  report: cache off in the pass; order Base, follow, recheck, Solana
dc64665  one pools call per token per pass; Solana back in the daily text
3f8b55e  report: today's explicit date gets the short cache too
71b2e7c  New pairs: link in the top nav, search box, token logos
86cbd49  New pairs redesign: app nav, chain tabs, filters, sort, dense rows, trade/copy, auto refresh; Solana pairs
18e92eb  New pairs: wallet chip with disconnect, chain-aware title
c9dd96c  pairs: the app's header shell, settings popover and full footer
```

### ✅ سقفِ درخواستِ کلادفلر — رفع شد و زنده تأیید شد

- **Cache API در طولِ گذرِ ساعتی کاملاً خاموش است.** اندازه‌گیریِ ۲۱ سپتامبر گفت
  ۱۵ تا از ۵۱ عمل، عملیاتِ کش بود و کش برای توکنِ تازه تقریباً همیشه خالی است.
  حالا در طولِ گذر، خواندن و نوشتنِ کش اصلاً انجام نمی‌شود و فقط شمرده می‌شود
  (`sub.cacheSkipped` — کنترلِ مثبت؛ `sub.cache` باید صفر بماند).
- **ترتیبِ مرحله‌ها عوض شد:** Base → پیگیری → بازپرسی → سولانا. پیش از این
  سولانا وسطِ گذر بودجه را می‌خورد و پیگیری و بازپرسی همیشه صفر می‌ماندند.
- **نتیجه‌ی زنده (۹ گذرِ اول، ۲۳:۱۷ تا ۰۷:۱۷ UTC):** هر ۹ گذر `capProbe:"ok"`،
  `stoppedAt:null`، `discarded:0`، `sub.cache:0`، `cacheSkipped` ۱۴ تا ۱۶،
  `sub.total` ۳۷ تا ۴۹. صفر نامعلومِ `internal`. پیگیری بالاخره ثبت می‌کند
  (دو `pool-empty`، یک `pool-there`). بازپرسی هدف نداشت چون Base نامعلوم نداشت.
- ⚠️ **حاشیه کم است:** بدترین گذر ۴۹ (با بالاسریِ شمارش؛ ۳ عملِ KV جزوِ سقف نیست).

### ✅ یک تماسِ pools به‌ازای هر توکن در هر گذر

`baseVenueCoveredDetail` (گاردِ پوشش) و `fetchV4Pools` (ایندکسِ نسخه ۴) یک آدرس را
دوبار از بالادست می‌پرسیدند. حالا هر دو روی `fetchBaseTokenPoolsRaw` سوارند و در
طولِ گذر یک حافظه‌ی موقت (`passPoolsMemo`) دارند که **فقط جوابِ موفق** را نگه
می‌دارد و در `finally` پاک می‌شود.

🔴 **سنجشِ زنده یک باگ را گرفت که هیچ آزمونی نمی‌گرفت:** پراکسیِ خودمان
(`PATH_OK`) هر بخشِ مسیر را به ۱۲۰۰ نویسه محدود می‌کند، پس ۳۰ آدرس در
`tokens/multi/` **۴۰۰** می‌گیرد و ۲۵ آدرس ۲۰۰. دسته‌ها ۲۵تایی شدند و یک کاوشگر
همین سقفِ طولِ مسیر را هم می‌سنجد.

⏳ **اثرِ حافظه هنوز سنجیده نشده:** فقط روی توکنِ نسخه ۴ با حکمِ `nosell` اثر
دارد (۷ عمل → انتظار ۶) و در ۷ گذرِ بعدی هیچ نمونه‌ای نیامد. دفعه‌ی بعد که چنین
ردیفی در گزارش بود، `sub.baseTokens` را در `/vd/passes` نگاه کن.

### ✅ سولانا در متنِ عمومیِ گزارش، و کشِ تاریخِ امروز

- `/report/<تاریخ>.txt` دوباره `reportText(doc,{solana:true})` صدا می‌زند. گیتِ
  `opts.solana` سرِ جایش ماند.
- باگِ قدیمی: در روزی که هیچ توکنِ Base نبود، بلوکِ سولانا هم چاپ نمی‌شد
  (خروجِ زودهنگامِ `total===0`). رفع شد؛ متنِ آن حالت به‌شکلِ دقیق در آزمون است.
- باگِ قدیمیِ دوم: `/report/<تاریخِ امروز>.txt|.json` کشِ یک‌روزه می‌گرفت، چون
  `isToday` فقط `today.txt/today.json` را می‌شناخت. قاعده‌ی تازه: **تاریخِ امروز یا
  آینده → ۳۰۰ ثانیه**، گذشته → ۸۶۴۰۰. زنده تأیید شد.

### ✅ صفحه‌ی `/pairs` — از یک سندِ جدا به یک برگه‌ی ابزار

حسام گفت صفحه «خشک و محدود، انگار زندان» است و بعد با اسکرین‌شاتِ علامت‌زده چهار
اختلاف را نشان داد. همه رفع شد:

- **هدر عیناً هدرِ اپ است:** همان لوگو با همان اندازه و همان جا (چپ ۲۶، عرض
  ۱۶۰٫۱۷، ارتفاع ۲۶٫۵۲ — با `getBoundingClientRect` سنجیده و در هر دو صفحه یکی)،
  بدونِ خطِ زیرِ هدر، با ناوبریِ چهارتایی و نوارِ پایین در موبایل.
- **خوشه‌ی راست مثل اپ:** چیپِ شبکه (نامِ زبانه)، دکمه‌ی تنظیمات با بخشِ
  Appearance و تعویضِ تم، و چیپِ والت (وصل‌نشده = چیپِ گرادیانی، وصل = چیپِ ساده
  با آدرسِ کوتاه). دکمه‌ی ماهِ قبلی حذف شد.
- **چیپِ والت فقط می‌خواند:** با `eth_accounts` بی‌صدا (هرگز `eth_requestAccounts`)،
  و اگر `zaexa.disconnected` ست باشد اصلاً نمی‌پرسد. منویش آدرسِ کامل، کپیِ آدرس و
  قطعِ اتصال دارد؛ نشستِ WalletConnect فقط نشان داده می‌شود و قطعش به اپ ارجاع
  می‌شود، چون فقط آنجا واقعاً بسته می‌شود.
- **همان «Copy address» به منوی والتِ خودِ اپ هم اضافه شد**، بالای Disconnect.
- **فوترِ کاملِ چهارستونی** (برند، Trade، About، Support) مثل اپ؛ لینک‌های داخلی
  به `/app#…`، بدونِ تلگرام (لینکش خالی است)، و خطِ آخر همان جمله‌ی صادقانه‌ی این
  صفحه.
- **محتوا:** زبانه‌ی Base و سولانا (`?chain=`)، جست‌وجو روی نماد/نام/آدرس،
  چیپ‌های شمارش که فیلتر هم هستند، مرتب‌سازی (تازگی، نقدینگی، حجم، سنِ استخر)،
  ردیفِ یک‌خطی با دکمه‌ی معامله (`/app#swap?out=…`، فقط Base) و کپیِ آدرس،
  تازه‌سازیِ خودکار هر ۵ دقیقه، آیکونِ تب، و عنوانِ تب که با زبانه عوض می‌شود.
- **سرور:** `/pairs.json?chain=solana` دیگر خالی نیست؛ از سندهای امروز و دیروز
  ساخته می‌شود (تازه‌ترین اول، سقفِ ۲۰۰).
- **تمِ تیره:** این صفحه از روی صفحه‌ی معرفی ساخته شده بود و تیره‌ی بنفشِ آن را
  داشت. حالا دقیقاً تیره‌ی اپ است (`#0b0d13/#14171f/#1a1e28`)؛ فقط
  `landing.html` بنفش ماند.

### 🔬 لوگوی توکن‌ها — اندازه‌گیری شد، و تصمیم گرفته شد

- گِکوترمینال برای **۲ از ۲۰۰** جفتِ تازه لوگو دارد؛ دکس‌اسکرینر برای **۱ از ۳۰**.
  یعنی توکنِ چندساعته تقریباً هیچ‌جا لوگو ندارد.
- [stated] تصمیمِ حسام: جای لوگوی نداشته **خالی** بماند (نه حرفِ اول، نه دایره‌ی
  رنگی). ۲۵ ردیفِ اول همان لحظه‌ی باز شدن پرسیده می‌شوند، بقیه با اسکرول.
- دنبالِ منبعِ سوم نرو مگر داده‌ی تازه بگوید.

### 🔴 درسِ کاریِ تازه — «written» شاهدِ تازگیِ فایل نیست

یک بار `device_commit_files` جوابِ موفق داد ولی محتوای **قدیمی** روی دیسک نشست؛
حسام کپی کرد، `git status` هیچ تغییری نشان نداد و سوییت عددِ قدیمی را داد.
**قاعده: بعد از هر تحویل، اندازه‌ی بایتِ هر فایل روی دستگاه با نسخه‌ی خودمان
مقایسه شود.**

### وضعیت سوییت‌ها

Worker **۱۱۰ بخش** · رابط **۴۲۱ کاوشگر** · `og_live_test` سبز.

---

## ۰ز. کارِ ۲۱ سپتامبر ۲۰۲۶ — «نامعلوم» بیشترش تقصیرِ خودمان بود: سقفِ درخواستِ کلادفلر

**همه روی سایتِ زنده سنجیده شد، از مرورگرِ داخلیِ Cowork روی مبدأ zaexa.com.**
کانتینر هنوز به سایت شبکه ندارد؛ ماشینِ حسام هم برای سنجش لازم نبود.

### 🔴 کشفِ اصلی — گذرِ ساعتی به سقفِ ۵۰ درخواستیِ کلادفلر می‌خورد

- هر اجرای Worker روی پلنِ رایگان حدودِ **۵۰ درخواست** دارد. عددِ زنده‌ی گذرِ
  ساعتِ ۲۰:۱۷: اولین خطای سقف در **عملِ ۵۲‌ام** (۳۶ درخواستِ شبکه + ۱۵ عملِ
  کش). پس **عملیاتِ کش هم جزوِ سقف است**. نوشتن در KV جزوِ سقف نیست (بعد از
  سقف هم نوشته شد).
- هر توکنِ Base بین ۴ تا ۹ عمل خرج می‌کند، هر توکنِ سولانا ۶ تا ۱۲.
- تا امروز **هر ردیفی که بعد از سقف بررسی می‌شد با برچسبِ نامعلوم ذخیره
  می‌شد** (why = `internal` یا `meta:0`). الگوی زنده در دو روز و چهل گذر:
  شکست‌ها همیشه تهِ گذر، هرگز وسطش. سولانا که همیشه آخر بود تقریباً کاملاً
  می‌افتاد (۴۳ از ۴۴ در یک روز)، و پیگیری و بازپرسی هم هرگز اجرا نمی‌شدند.
- شاهدِ مستقل: یک درخواستِ آزمایشی تهِ هر گذر (`capProbe`) که پیامِ سقف را
  طبقه‌بندی می‌کند. در هر گذرِ شکسته گفت `cap`.

### ✅ آنچه امروز ساخته و زنده شد (به ترتیبِ کامیت)

```
abce451     verdict: a v4 pool we really asked is coverage
b54d2b5     re-ask unknown rows; Solana in the daily text; later checks on pairs; quote-back percent; share:copy event
f172bea     report: hide Solana text for now; pass log with cap probe; link New pairs
bc8afda     report: count subrequests per pass stage
348c6e2     report: stop at the subrequest cap, never store a poisoned row; batch Base metadata; per-stage shares
```

1. **استثنای پوششِ نسخه ۴.** گاردِ پوشش نسخه ۴ را نمی‌شناخت (جدولِ
   `GT_DEX_TO_VENUE`)، پس توکنی که فقط استخرِ v4 داشت همیشه `cover:false`
   می‌گرفت. حالا اگر بالادست استخرِ `uniswap-v4-base` ببیند **و** دست‌کم یک
   کلیدِ واقعیِ ایندکس‌شده داشته باشیم، حکمِ منفی می‌ماند. فقط وقتی چکِ پوشش
   واقعاً «نه» گفته؛ چکی که جواب نداده همیشه نامعلوم. زنده: چهار توکنِ v4 از
   نامعلوم به حکم رسیدند، کنترلِ مثبت (USDC) و منفی (توکنِ آئرودروم) درست ماندند.
2. **بازپرسیِ ردیفِ نامعلوم** (`recheck`, `recheckAt`, `recheckCause`)، ۵۵ تا
   ۱۸۰ دقیقه بعد، فقط افزایشی؛ `v` و `why` اصلی هرگز بازنویسی نمی‌شوند.
3. **صفحه‌ی جفت‌ها** زیرِ هر نشان یک خط دارد: درصدِ بازگشت، پیگیری، بازپرسی.
   `/pairs.json` این‌ها را هنگامِ خواندن از سندِ امروز و دیروز ادغام می‌کند.
4. **صفحه‌ی توکن** در حالتِ «بررسیِ سرور، فروش ممکن» درصدِ بازگشتِ کوتِ صد
   دلاری را می‌گوید. بدونِ آستانه (تصمیمِ حسام، ۱۶ سپتامبر).
5. **رویدادِ `share:copy`** — فقط وقتی کپیِ لینکِ یک بررسی موفق شود؛ با
   اجازه‌ی صریحِ حسام، فقط همین یک نام.
6. **لینکِ «New pairs»** در ستونِ Trade فوترِ اپ و صفحه‌ی معرفی، و `/pairs` در
   نقشه‌ی سایت. تا امروز هیچ راهی به این صفحه نبود.
7. **سولانا در متنِ عمومی** ساخته شد و **به تصمیمِ حسام موقتاً پنهان است**
   (`reportText(doc, {solana:true})` برای روشن‌کردن). دلیل: عددش راست بود ولی
   ۴۳ از ۴۴ نامعلوم به‌خاطرِ سقفِ خودمان.
8. **گزارشِ گذر** — `GET /vd/passes` (کلیدِ KV `report:passlog`، ۴۸ گذرِ
   آخر، فقط عدد و نامِ میزبان، هرگز آدرس یا مسیر). شاملِ شمارنده‌ی درخواست به
   تفکیکِ مرحله و میزبان، `capAt`، `discarded`، `stoppedAt`، `metaBatch`.
9. **بودجه‌ی گذر:** به محضِ اولین خطای سقف هیچ کارِ تازه‌ای شروع نمی‌شود؛
   توکنی که وسطِ بررسی‌اش سقف خورد **ذخیره نمی‌شود** (گذرِ بعدی دوباره برش
   می‌دارد). متادیتای Base با یک درخواستِ دسته‌ای (`tokens/multi/…`). سهمِ هر
   گذر: Base ۵، سولانا ۲، پیگیری ۳، بازپرسی ۲.

### ✅ اولین گذرِ بعد از بودجه (۲۰:۱۷ UTC) — معیارها پیش از عدد نوشته شده بود

```
Base 5 of 5 got a verdict, 0 unknown     (before: first unknown at the 4th token)
Solana 1 stored, 1 discarded at the cap  (before: 0 stored)
CoinGecko calls 24 -> 12                 (batch metadata)
cap hit at op 52 = 36 fetches + 15 cache ops
follow-ups 0, rechecks 0                 <- still starved: Solana runs before them
```

### 🎯 قدمِ بعد (به ترتیب)

1. ⭐ **کش را در گذرِ ساعتی خاموش کن.** ۱۵ از ۵۱ عمل کش بود، و گذر همیشه
   توکنِ تازه می‌بیند پس کش تقریباً همیشه خالی است. این یک‌تنه جا را برای
   پیگیری و بازپرسی باز می‌کند. بعد از آن، ترتیب را به Base → پیگیری →
   بازپرسی → سولانا عوض کن و با `/vd/passes` بسنج.
2. بعد از چند گذرِ سالم: روشن‌کردنِ دوباره‌ی سولانا در متنِ عمومی.
3. دو تماسِ یکسانِ `tokens/<addr>/pools` (ایندکسِ v4 و چکِ پوشش) را در یک گذر
   یکی کن.
4. شناسه‌ی `aerodrome-slipstream-3`: شناسه‌ی جدولِ ما (`aerodrome-slipstream`)
   در شش روز یک بار هم دیده نشد. **پیش از نگاشت** ثابت کن روتری که صدا
   می‌زنیم همان استخر را کوت می‌دهد.
5. بقیه‌ی صفِ قبلی: تصویرِ کارت (به دلیلِ کهنه‌شدنِ حکم فعلاً رد شد، مگر با
   زمانِ بررسی روی تصویر)، فارکستر، رفت‌وبرگشتِ واقعیِ Base، سولانا برش ۲.

### 📌 درس‌های این روز

- **«نامعلوم» اول باید با بودجه‌ی خودمان سنجیده شود، بعد با توکن.** الگوی
  «شکست فقط تهِ گذر» یعنی مشکل در ماست.
- **یک شمارنده‌ی ارزان از ده حدس بهتر است.** حدسِ «ایندکسِ v4 پرخرج‌ترین
  است» غلط بود؛ عدد گفت متادیتا و کش.
- **خروجیِ پروبِ تشخیصی حکمِ سایت نیست:** `/vd/<addr>?probe=1` گاردِ پوشش را
  رد می‌کند.
- **ایجنت ممکن است آزمونش را با خروجیِ خراب هم‌شکل کند** (بلوکِ سولانا دو خطِ
  خالی داشت و آزمون همان را «درست» می‌دانست). خودت خروجی را بخوان.
- **دو نگهبانِ هم‌پوشان:** در چند جا شکستنِ یکی به‌تنهایی سبز ماند
  (`row.v !== null` در `applyRechecks` با `recheckForRow`). رفتار محافظت شده،
  ولی نگهبانِ اول به‌تنهایی آزموده نیست.
- **پیش از شکستنِ عمدی کامیت کن.** یک بار `git checkout` روی کارِ کامیت‌نشده
  کلِ تغییرِ یک فایل را پاک کرد.

**سوییت‌ها:** Worker **۹۹ بخش** · رابط **۳۶۶ کاوشگر** (شاملِ بخش‌های Worker) ·
`og_live_test` سبز. هر سه را خودم اجرا کردم.

---

## ۰و. کارِ ۲۰ سپتامبر ۲۰۲۶ — «فروش ممکن نیست» بیشتر یعنی «استخر خالی شد»

**شش کامیت، همه روی سایتِ زنده تأیید شده.** کلِ اندازه‌گیریِ این روز از مرورگرِ
داخلیِ Cowork روی مبدأ zaexa.com انجام شد، چون نه کانتینر و نه ماشینِ حسام آن روز
به سایت شبکه نداشتند.

```
9913839  risk panel: no accusation without a coverage witness (bugs 15, 16)
2781d0a  token page: the round-trip bar always settles (bug 13)
5326cf7  negative verdict says why: empty pool is not a blocked token
e5c621b  report: one follow-up an hour later (verdict staleness)
31e2756  report: how much a $100 sell quote gives back
2fa48a1  report: Solana rows land in the same daily document
```

### ۱. بندِ آخرِ معیارِ پذیرشِ نسخه ۴ — بسته شد

۳۰ ردیف از ۳۳ ردیفِ نسخه ۴ حکم گرفتند (روزِ قبل ۱ از ۶۵). سهمِ حکمِ منفی ۱۲٫۳٪،
زیرِ سقفِ ۱۵٪، و کنترلِ مثبت در همان نمونه بود. ولی هر ۹ حکمِ منفی از نسخه ۴ آمده
بود و همان تمرکز، کشفِ اصلیِ روز را بیرون کشید.

### ۲. کشفِ اصلی — شاهد از خودِ زنجیره، نه از ابزارِ خودمان

رویدادِ برداشتِ نقدینگی روی PoolManager، مقایسه‌شده با لحظه‌ی بررسیِ ما:

```
7 of 9 nosell   liquidity removed 1 to 5 minutes BEFORE our check
2 of 9 nosell   never removed, still funded          <- the only honeypot suspects
20 of 20 sell   removed AFTER our check (2 to 47 min) or never
```

یعنی حکم درست بود ولی دلیلش قفلِ قرارداد نبود. دو موردِ واقعاً مشکوک:

```
CASHCAT    0x8d8c899d6fa16afda3df7dc53aec751c5f8469c1
MUSEWORLD  0xeb53c373629ba590ab3887e7f2a6782dc25adb07
```

### ۳. باگ ۱۵ و ۱۶ — اتهام بدونِ شاهدِ پوشش

وقتی کوت‌های خودِ صفحه فروشی پیدا نکنند، داورِ نهایی `/vd` است که پوششِ نسخه ۴ و
گاردِ پوشش را دارد: سرور مثبت → هیچ اتهامی؛ سرور منفی → هشدارِ بحرانی بدونِ کلمه‌ی
هانی‌پات؛ سرور ساکت → «نامعلوم» بدونِ جریمه‌ی امتیاز. ادعای نقدینگیِ کم هم شاهدِ
پوشش می‌خواهد، وگرنه «کامل سنجیده نشد».
تأییدِ زنده: توکنِ نسخه ۴ از «احتیاط، ریسک ۳۲، حدود صفر دلار» به ریسک ۰؛ یک استخرِ
۲۶ میلیون دلاری که «حدود صفر دلار» گزارش می‌شد درست شد؛ کنترل‌های منفی و مثبت هر
دو سرِ جای خودشان.

### ۴. باگ ۱۳ — نوارِ گیرکرده و ادعای شبیه‌سازی

اندازه‌گیریِ پیش از رفع: ۴۴ ثانیه اسکلت، با جمله‌ی «inside one simulated
transaction». ریشه: پنج خروجیِ زودهنگامِ خطِ کوت هیچ‌کدام نوار را رسم نمی‌کردند.
حالا هر خروجی نوار را می‌بندد، به‌علاوه‌ی نگهبانِ زمانیِ ۱۸ ثانیه‌ای؛ و وقتی صفحه
نمی‌تواند کوت بگیرد، جوابِ سرور با برچسبِ «بررسیِ سرور» نشان داده می‌شود و صریح
می‌گوید این یک کوت است نه شبیه‌سازی. جوابِ سرور عمداً وارد `exitCache` نمی‌شود.

### ۵. سه فیلدِ تازه‌ی ردیفِ گزارش

```
cause  "empty-pool"                only on nosell
follow "pool-empty" | "pool-there" only on sell, one follow-up 55-180 min later
ret    percent of a $100 sell quote, only on sell
```

`ret` قیمتِ اتر را از کاناریِ همان دسته می‌گیرد، پس هیچ تماسِ اضافه ندارد. همان
شب یک چیزِ مهم را لو داد: چند توکنِ نسخه ۲ با حکمِ «فروش ممکن» فقط ۰٫۱٪ برمی‌گردانند.
سنجشِ زنجیره‌ای نشان داد استخرشان ۰٫۰۰۰۰۴۶ WETH دارد و قیمتِ لیست‌شده‌ی بالادست
۲٫۳ میلیارد برابرِ قیمتِ خودِ استخر است. حکم فنی درست بود، عدد کنارش لازم بود.

### ۶. سولانا در همان انبار

دو صفحه‌ی `new_pools` سولانا، سقفِ ۶ توکن در هر گذر، `checkKind:"roundtrip"`.
عمداً بیرونِ متنِ عمومی، بیرونِ حلقه‌ی جفت‌ها، و بیرونِ پیگیریِ یک‌ساعته.
اندازه‌گیری: از ۴۰ ردیف فقط ۳ تا از کفِ ۵۰۰۰ دلار رد شدند و ۲ تا حکمِ واقعی گرفتند.

### ۷. آنچه هنوز آزموده نشده

- ردیف‌های سولانا روی گزارشِ زنده (اولین گذرِ کرون بعد از استقرارِ ۲۰ سپتامبر).
- شاهدِ «استخر خالی» برای صرافی‌های غیرِ نسخه ۴ — امروز فقط کلیدهای v4.
- مسیری که کیفِ پولِ واقعی وصل باشد.

**سوییت‌ها: مرورگر ۳۲۹ کاوشگر (شاملِ ۷۶ بخشِ Worker).** هر سه سبز.

---

## ۰ه. کارِ ۱۴ و ۱۵ سپتامبر ۲۰۲۶ — ایندکسِ کلیدِ واقعیِ نسخه ۴، و عددی که شاهد نبود

**زنده، تأییدشده روی سایت، و با یک نتیجه‌گیریِ غلط که وسطِ راه اصلاح شد.**

### 🆕 ۱۹ سپتامبر (دوم) — نسخه ۴: استخری که هیچ اندازه‌ای را پر نمی‌کند

**ادعای ایجنتِ بازرس:** «۲۲ از ۲۲ توکنِ نسخه ۴ نامعلوم؛ نیمی از گزارشِ روزانه.»
با عددِ خودمان تأیید شد: در گزارشِ ۱۹ سپتامبر **۶۴ از ۶۵**.

**سه فرض با اندازه‌گیری رد شد، به همین ترتیب:**
۱. «کلید اشتباه است» → هشِ کلیدِ ذخیره‌شده دقیقاً همان شناسه‌ی استخر بود.
۲. «رمزگذاریِ مسیرِ کلیدِ واقعی خراب است» → بایت‌به‌بایت با مسیرِ حدسی یکی بود.
۳. «مقدارِ صد دلاری بزرگ است» → جاروی هفت اندازه از ۱e۱۵ تا ۱e۲۱: همه ریوِرت.

**بعد ابزار ساخته شد، نه حدسِ چهارم:** خواندنِ خودِ داده‌ی ریوِرت. کوترِ نسخه ۴
خطای استخر را در `UnexpectedRevertBytes(bytes)` می‌پیچد؛ چهار بایتِ **درونی**
اسمِ واقعی را می‌دهد. روی شش توکنِ زنده (`v4_pool_truth.py`، با کلیدِ خودِ هر توکن):
- پنج‌تا: `NotEnoughLiquidity(bytes32)` با شناسه‌ی **خودِ همان استخر**، هم در
  اندازه‌ی یک واحدِ خام هم یک توکنِ کامل.
- یکی: کوتِ سالم — و آن یکی هوک هم داشت. **کنترلِ مثبت از داده‌ی واقعی:
  کوت‌گرفتنِ ما از نسخه ۴ سالم است.**

**تغییر (با تصمیمِ صریحِ حسام، ۱۹ سپتامبر):** یک ردیفِ نسخه ۴ فقط و فقط در همین
حالت اجازه‌ی اثباتِ منفی دارد، با چهار شرط با هم:
۱. کلید از لاگِ `Initialize` زنجیره آمده باشد (ردیفِ حدسی `poolId` ندارد).
۲. چهار بایتِ خطای درونی دقیقاً `NotEnoughLiquidity` باشد.
۳. شناسه‌ی داخلِ خطا با همان کلیدی که فرستادیم یکی باشد.
۴. کاناری زنده باشد.
و چون آن کلید از خودِ زنجیره آمده، همین ردیف **پوششِ خودش** هم هست: گاردِ پوشش
(`baseVenueCovered`) دست‌نخورده می‌ماند و فقط همین یک استثنا را می‌پذیرد.
`PoolNotInitialized` (یعنی کلیدِ ما غلط بوده) هیچ‌چیز اثبات نمی‌کند.

**و یک شکافِ جدا که همان‌جا بسته شد:** کلیدِ واقعی با `ctx.waitUntil` **بعد از**
حکم ساخته می‌شد و گزارش هر توکن را فقط یک بار می‌بیند — پس آن کلید هرگز روی هیچ
ردیفی اثر نمی‌گذاشت. حالا در گذرِ کرون، برای دکسِ نسخه ۴ که ورودیِ ذخیره‌شده ندارد،
ایندکس **پیش از** حکم و منتظرشونده اجرا می‌شود. مسیرِ کارتِ `/t/` دست‌نخورده است.

**🔴 باگی که همان شب روی سایتِ زنده پیدا شد و رفع شد:** همان توکن در دو درخواستِ
پشت‌سرهم دو جوابِ متفاوت داد — اول `nosell`، یک دقیقه بعد «نامعلوم / cover:false».
علت: حکم کش می‌شد ولی **شاهدِ نسخه ۴ با آن کش نمی‌شد**، پس روی ضربه‌ی کش گاردِ پوشش
دوباره تنزلش می‌داد. حالا پرچمِ شاهد کنارِ خودِ حکم کش می‌شود و نگهبانش یک کشِ
واقعی‌نما می‌سازد و همان دو درخواستِ پشت‌سرهم را می‌زند.
📌 **درس: یک حکم نباید بینِ دو درخواستِ یک‌دقیقه‌ای عوض شود** — و این را فقط
اجرای دوبار روی سایتِ زنده نشان داد، نه هیچ تستی.

**آزموده:** پنج شکستِ عمدی؛ دوتاشان اول خطا ندادند و برایشان نگهبانِ تازه نوشتم
(خطای درونیِ دیگر با همان شناسه، و ترتیبِ ایندکس در کرون). 📌 شرطِ ۱ عملاً تکرارِ
دفاعیِ شرطِ ۳ است و به‌تنهایی قابلِ شکستن نیست — این را می‌نویسم چون هست، نه اینکه
ادعا کنم چهار نگهبانِ مستقل داریم.
**✅ تأییدِ زنده (۱۹ سپتامبر، شب — بعد از رفعِ کش):** چهار اجرا، دو دورِ جدا با
فاصله، خروجی‌ها عیناً یکی:
```
0x8335…2913 (USDC، کنترلِ مثبت)  sell    · هر چهار اجرا
0x3266…0817                      nosell  · هر چهار اجرا
0xfa8c…5ab0                      nosell  · هر چهار اجرا
```
زمان‌ها هم همین را می‌گویند: اجرای اولِ هر دور ~۶۰۰ تا ۹۰۰ میلی‌ثانیه (محاسبه‌ی
تازه)، اجرای دوم زیر ۵۰ (ضربه‌ی کش) — یعنی جوابِ کش‌شده همان جوابِ محاسبه است.
پروب هم `no-liquidity: 1` نشان می‌دهد، واژه‌ای که فقط در همین نسخه وجود دارد.

**⏳ تنها بندِ باقی‌مانده‌ی معیار: گزارشِ روزِ بعد.**

**معیارِ پذیرشِ زنده:**
۱. ⏳ **باز — فردا:** در گزارشِ روز، ردیف‌های نسخه ۴ باید حکم بگیرند، نه «نامعلوم».
۲. ✅ هر حکمِ منفیِ نسخه ۴ که دستی پروب شد `no-liquidity` با شناسه‌ی جور داد.
۳. ✅ کنترلِ مثبت برقرار ماند: توکنِ مرجع در هر چهار اجرا `sell`.
   ⚠️ تصحیح: آن توکنِ v4 که در `v4_pool_truth.py` «کوت داد» کنترلِ مثبت **نیست** —
   مقدارِ خروجی‌اش صفر بود (خروجیِ چاپ‌شده بریده بود و من زود خواندمش). کنترلِ مثبتِ
   واقعیِ نسخه ۴ همان موردِ ۱۵ سپتامبر است که روی سایت از «نامعلوم» به `sell` رسید.
۴. ⏳ **باز — فردا:** اگر سهمِ حکمِ منفی از ~۱۵٪ بالاتر رفت، فرض کن اشتباه است
   نه کشف، و برگرد.
۵. ✅ دو بارِ پشتِ‌سرهم: یکی بود، در هر دو دور.

**دستورِ فردا (همان شمارشِ دلیل‌ها):**
```bash
curl -s https://zaexa.com/report/today.json | python3 -c "
import json,sys,collections as c
r=json.load(sys.stdin)['rows']; w=[x for x in r if 'why' in x]
print('rows',len(r),'| with why',len(w))
print('v',dict(c.Counter(str(x['v']) for x in w)))
print('--- dex x verdict')
for k,v in c.Counter((x['dex'],str(x['v'])) for x in w).most_common(): print(v,*k)
print('--- why x dex (null only)')
for k,v in c.Counter((x['why'],x['dex']) for x in w if x['v'] is None).most_common(): print(v,*k)
"
```

### ۱۹ سپتامبر — یک صرافیِ ممتنع، نوزده شاهد را پاک می‌کرد

**ادعای ایجنتِ بازرس:** «در ۳۱۹ توکن طیِ دو روز، حتی یک `nosell` هم شلیک نکرده؛
شاخه‌ای که هرگز اجرا نشود از شاخه‌ی خراب قابلِ‌تشخیص نیست.» درست بود، و **علتش
شانس نبود**.

**اندازه‌گیری (پروبِ زنده، از ماشینِ حسام، توکنِ `0x6F63…4378`):**
```
v: None | covered: True | v4Keys: 2
by out: {'quoted': 1 (canary), 'revert:3': 19, 'zero': 2}
```
کاناری زنده، پوشش true، نوزده ریوِرتِ کدِ ۳ (اثباتِ منفی)، و دو صفر از آئرودروم.

**ریشه:** در `verdictFrom` صفرِ یک صرافیِ `zero-is-not-proof` (امروز فقط SOLIDLY)
کلِ حکم را null می‌کرد. و روترِ سالیدیتی وقتی استخر ندارد **صفر می‌دهد، نه ریوِرت** —
پس آن دو صفر روی تقریباً هر توکنِ تازه هست. نتیجه: شاخه‌ی `nosell` عملاً
غیرقابلِ‌رسیدن بود.

**تغییر:** آن صفر حالا **رأیِ ممتنع** است: نه اثبات می‌کند، نه اثباتِ بقیه را پاک.
- `VD_MIN_NEGATIVE_PROOF = 1` صادر شد؛ کفِ شاهد همان یک اثباتِ واقعیِ قبلی است.
- «نتوانستیم بپرسیم» دست‌نخورده: کدِ خطای غیرِ ۳/-۳۲۰۰۰، رمزگشاییِ ناموفق، شکلِ
  نامعتبر، یا kindِ ناشناخته هنوز کلِ حکم را نامعلوم می‌کند.
- ردیف‌های `V4_SINGLE` (positive-only) هنوز هیچ کورومی نمی‌سازند.
- 🔴 `VD_ZERO_IS_PROOF.SOLIDLY` همچنان `false` است — رفعِ ۷ سپتامبر سرِ جایش.
- 🔴 گاردِ پوشش (`baseVenueCovered`) دست‌نخورده و حالا **تنها** چیزی است که جلوی
  اتهام به توکنی را می‌گیرد که استخرش روی صرافیِ پروب‌نشده است. تستِ سرتاسری هر دو
  حالت را می‌سنجد و خودم گاردش را شکستم.
- ⚠️ یک اثرِ جانبیِ عمدی: فیکسچرِ «مرحله‌ی WETH مبهم» در تست‌ها با «همه `0x`» ساخته
  می‌شد، که حالا `nosell`ِ تمیز است. ابهام حالا با کدِ خطای غیرِ اثباتی ساخته می‌شود.
  گذرِ اثباتِ USDC (که فقط می‌تواند null را به sell تبدیل کند) دست‌نخورده است.
- **آزموده نشده:** رفتارِ زنده. معیار پیش از دیدنِ عدد نوشته شده (زیر).

**معیارِ پذیرشِ زنده — پیش از دیدنِ عدد:**
۱. در گزارشِ روزِ بعدِ deploy، `nosell` باید **بیش از صفر** باشد.
۲. هر توکنِ `nosell` که دستی پروب شود باید `covered: true` و دستِ‌کم یک `revert:3`
   داشته باشد. حتی یک مورد بدونِ این دو یعنی برگرد.
۳. کنترلِ مثبت: چهار توکنِ سالمِ مرجع (`USDC`، `WETH`، `cbBTC`، `DAI`) باید
   `sell` بمانند.
۴. سهمِ `nosell` اگر از ~۱۵٪ بالاتر رفت، فرض کن اشتباه است نه کشفِ بزرگ.

### چرا «نامعلوم»؟ — `why` با واژه‌نامه‌ی بسته روی Base

گزارشِ ۱۴ سپتامبر: **۱۰۶ از ۲۱۰** ردیف نامعلوم. هیچ‌جا ثبت نمی‌شد هر کدام *کجا*
ایستاد. این تغییر **فقط ابزارِ اندازه‌گیری است** — هیچ حکم، کش، یا تعدادِ تماسی عوض
نمی‌شود (نگهبان: همان سناریو با و بی‌مشاهده‌گر، همان حکم و همان تعدادِ fetch).

- هر ردیفِ گزارش یک کلیدِ آخرِ تازه دارد: `why`. برای sell/nosell همیشه `null`؛ برای
  null یکی از `VD_BASE_WHY` (`worker/verdict.js`)، به ترتیبِ جایی که خط‌لوله می‌ایستد:
  `meta:<status>` · `meta:timeout` · `meta:shape` · `no-amount` · `deadline` · `rpc-down` ·
  `canary-dead` · `no-quote` · `proof-rpc` · `proof-no-quote` · `usdc-rpc` ·
  `usdc-no-proof` · `cover:false` · `cover:<status>` · `cover:timeout` · `cover:shape` ·
  `internal`. هر چیزِ خارج از فهرست در انبار `internal` می‌شود.
- **در لحظه‌ی خودِ کرون ثبت می‌شود**، نه با پروبِ بعدی — سقفِ نرخِ بالادست فقط زیرِ بارِ
  خودِ کرون دیده می‌شود.
- `GET /vd/<آدرسِ Base>` برای حکمِ null همان `why` را برمی‌گرداند؛ sell/nosell همان
  `{v, ms}`.
- دلیل از کدِ عددی و شکل می‌آید، هرگز از متنِ خطا؛ تایم‌اوت از `signal.aborted`.
- قراردادِ `runReportPass` عوض شد: `metaOf → {meta, why}`، `verdictOf → {v, why}`.
  مسیرِ کارتِ `/t/` دست‌نخورده است.
- ردیف‌های پیش از deploy کلیدِ `why` ندارند.
- **آزموده نشده:** رفتارِ واقعیِ کرون روی کلادفلر — تا اولین اجرا و شمارش.

### متنِ گزارشِ روزانه — `/report/today.txt` و `/report/<تاریخ>.txt`

همان سندِ KV، نمای سوم: متنِ ساده‌ی آماده‌ی پُست. تابعِ خالصِ `reportText` در
`worker/report.js`، مسیرِ `reportTextRoute` در `worker/index.js`، بخشِ ۲۸ در
`worker/test.mjs` (خطِ `[report text]`).

```
Exit Report · 14 Sep

210 new Base tokens checked.
0 had no sell route quoted.
104 had a sell route quoted.
106 could not be checked.

Sell quotes on Base DEXes, not a simulated round trip.
Last check 23:17 UTC.
```

- 🔴 **قالبِ پیش‌نویسِ Exit Report سه ادعا دارد که روی Base نداریم:** «round trip
  simulated»، مالیاتِ فروش، و بلک‌لیست. متن فقط چیزی را می‌گوید که داریم: کوتِ فروش.
  یک نگهبانِ واژگانی هر خروجیِ تست را برای این کلمه‌ها می‌گردد.
- **سه حالت، سه خط، جمعشان همیشه کل است.** `null` هرگز «نمی‌شود فروخت» خوانده نمی‌شود.
- **نماد از بالادست می‌آید و در پُستِ عمومی می‌نشیند** → فقط `[A-Za-z0-9_-]{1,16}`؛
  هر چیزِ دیگر (فاصله، خطِ نو، نقطه، لینک، یونیکد) با آدرسِ کوتاه جایگزین می‌شود.
- **پیش از `2026-09-08` هیچ متنی ساخته نمی‌شود** (۴۰۴، پیش از لمسِ KV) — چون
  `report:2026-09-07` هنوز ردیف‌های nosellِ غلط دارد.
- **«نتوانستیم بخوانیم» ≠ «چیزی نبود»:** بدونِ KV یا پرتابِ KV → ۵۰۳؛ کلیدِ غایب →
  ۴۰۴ «no report for <date> yet»؛ JSONِ خراب یا تاریخِ ناجور → ۵۰۳. عمداً از
  `reportDocFor` استفاده نمی‌کند، چون آن هر سه را به یک سندِ خالی تخت می‌کند.
- هر توکنِ پرچم‌خورده دو خط می‌گیرد (نماد و «no sell route quoted»، سپس لینکِ `/t/`)؛
  حداکثر ۱۰ توکن فهرست می‌شود، بقیه «+N more» با لینکِ JSON.
- ✅ **زنده تأیید شد:** خروجیِ بالا عیناً همان است که سایت برای ۱۴ سپتامبر داد، و ۷
  سپتامبر ۴۰۴.
- کش مثلِ JSON: امروز ۳۰۰ ثانیه، روزِ گذشته ۸۶۴۰۰؛ خطاها `no-store`.
- ⚠️ **هنوز نیست:** «[۹۳٪] زیرِ خرید» (`verdict.js` باید مقدارِ کوت را برگرداند) و
  «استخر کشیده شد» (بازسنجیِ زمانی). خطی برای هیچ‌کدام، حتی جای خالی، در متن نیست.
- آزموده: نه نگهبانِ تازه عمداً شکسته شد و هر کدام FAIL داد؛ مسیر روی workerd با
  یک KVِ واقعیِ miniflare هم اجرا شد. **آزموده نشده:** KVِ واقعیِ کلادفلر — تا
  deploy و یک curl.

### ✅ کلیدِ واقعیِ استخر حالا از زنجیره خوانده می‌شود

ردیفِ نسخه ۴ تا ۱۳ سپتامبر چهار `PoolKey` استاندارد را **حدس** می‌زد. حالا
کلیدِ واقعی از لاگِ `Initialize` خودِ PoolManager خوانده می‌شود: این رویداد یک
بار در عمرِ هر استخر شلیک می‌شود و کلِ کلید را اعلام می‌کند، و گِکوترمینال
شناسه‌ی استخر را می‌دهد، پس فیلترِ لاگ دقیق است.

🔑 **تأییدشده روی پاسخِ زنده، نه فرض:** برای ردیفِ `uniswap-v4-base` مقدارِ
`attributes.address` خودِ **شناسه‌ی ۳۲بایتیِ استخر** است (۶۴ نویسه‌ی هگز)، نه
آدرسِ ۲۰بایتی مثلِ بقیه‌ی دکس‌ها در همان فهرست.

ماژولِ تازه `worker/v4index.js` (خالص، بدونِ هیچ `fetch`ی — همان انضباطِ
`worker/report.js`)، سیم‌کشی‌اش در `worker/index.js`، و مصرفش در
`worker/verdict.js`. نتیجه در KV می‌نشیند: `ok` سی روز، میسِ **اثبات‌شده** شش
ساعت، و نامعلوم **هرگز**.

**واژه‌نامه‌ی بسته‌ی دلیل‌ها** — سه تفکیکی که عمدی‌اند:

```
ok  no-kv  no-pools  no-v4-pool  no-pool-id  no-created-at  no-anchor  rpc-down  no-log
```

- `no-log` = یک اندپوینت با آرایه‌ی خالیِ **خوش‌شکل** جواب داد → منفیِ واقعی.
- `rpc-down` = هیچ اندپوینتی جوابِ خوش‌شکل نداد → نامعلوم، ذخیره نمی‌شود.
- `no-pools` = بالادستِ قیمت جواب نداد، نه آرپی‌سیِ زنجیره.

### ✅ گذرِ اثباتِ مثبت روی مرحله‌ی USDC

مرحله‌ی USDC فقط پشتِ یک «nosell»ِ تمیزِ مرحله‌ی WETH اجرا می‌شد، چون کلِ
هدفش **تأییدِ یک منفی** بود. ولی یک کلیدِ واقعیِ USDC-جفت می‌تواند یک
**مثبت** اثبات کند، و مثبت به ابهامِ مرحله‌ی قبل هیچ ربطی ندارد.

حالا وقتی چنین کلیدی هست، مرحله‌ی USDC اجرا می‌شود حتی با مرحله‌ی WETHِ مبهم.
قیدها: فقط ردیف‌های `buildRealV4Probe` فرستاده می‌شوند (همه فقط-مثبت)، هر
نتیجه‌ای جز `"sell"` همان نامعلوم می‌ماند، و بدونِ کلیدِ واقعیِ USDC **حتی یک
فراخوانی هم اضافه نمی‌شود**. مرحله‌اش در لاگ `usdc-proof` است.

**اثباتِ زنده، با معیارِ از‌پیش‌نوشته:**

```
0x31c2c14134e6e3b7ef9478297f199331133fc2d8
قبل  {"v":null}
بعد  {"v":"sell"}      هم روی /vd/<آدرس> هم روی ?probe=1
```

ورودیِ معادلِ صد دلار `1106852987840255817` بود و خروجی `99745784` یعنی
**۹۹٫۷۵ دلار** — افتِ یک‌چهارمِ درصد. این توکن فروختنی بود و ما هرگز
نمی‌پرسیدیم.

### 🔴 عددِ `reserve_in_usd` گِکوترمینال شاهدِ نقدینگی نیست

حدسِ حسام بود و با `StateView` اثبات شد. آن عدد خودارجاع است: قیمتِ توکن از
همان استخر درمی‌آید و در موجودیِ همان استخر ضرب می‌شود.

```
استخر                       getLiquidity              ذخیره‌ی گزارش‌شده
0xe161922a… (0xd94e6c01)    0                         ~۲۰ هزار دلار
0xcb88ab49… (0xb0ccb2ed)    997519511065749950282714  ~۲۵٫۷ هزار دلار
0x30659013… (0x31c2c141)    22171764229562            ~۲۱ هزار دلار
0x4273af42… (کنترلِ مثبت)    44970693614421769510836   —
```

اولی نقدینگیِ درون‌بازه‌اش **صفرِ مطلق** است و در هر نُه اندازه ریوِرت می‌دهد —
استخر ساخته شده، قیمت و تیک دارد، ولی چیزی برای فروختن به آن نیست. دومی تا
`1e25` کوت می‌دهد ولی معادلِ صد دلارش `3.88e26` است، پس ریوِرتِ سایت رویش
**درست** است.

📌 `StateView` روی `0xa3c0c9b65baD0b08107Aa264b0f3dB444b867A71` ·
`getLiquidity(bytes32)` = `0xfa6793d5` · `getSlot0(bytes32)` = `0xc815641c`
(از رویِ امضا حساب شدند؛ کنترلِ مثبت درستی‌شان را ثابت کرد).

### 🔴 سه درسِ روشی که گران به‌دست آمدند

**۱. کوتِ صفر روی مقدارِ ریز هیچ‌چیزی اثبات نمی‌کند.** اولین سنجش `1e6` واحد
پرسید و *همه* — از جمله کنترلِ مثبت — صفر دادند. بدونِ کنترل، همان صفر
«استخرِ مرده» خوانده می‌شد. **در هر سنجشِ کوت، مقدار را جارو کن، یک عدد نگیر.**

**۲. نمونه‌ای که از فهرستِ «نامعلوم» برداشته شود به‌سمتِ مرده اریب است.** دو
نمونه‌ی اولِ این کار از `pairs.json` آمدند، یعنی از توکن‌هایی که گزارش از قبل
نامعلوم اعلامشان کرده بود.

**۳. ابزارِ تشخیصی‌ای که ورودیِ مسیرِ واقعی را ندارد شاهد نیست.** `?probe=1`
کلیدهای ذخیره‌شده را به مسیرِ سنجش نمی‌داد، پس خروجی‌اش شبیهِ «کلیدِ واقعی
پروب نشد» به‌نظر می‌رسید در حالی که هیچ‌چیزی نمی‌گفت. یک دورِ کامل هزینه داشت.

🔴 **و یک نتیجه‌گیریِ غلطِ ثبت‌شده:** پس از دو نمونه اعلام شد «کلیدِ واقعی
پرسیده شد و ریوِرت داد، پس این کار سودِ عملی ندارد». **غلط بود** — هر سه
نمونه‌ی اترجفت اتفاقاً استخرِ ساختگی بودند. سؤالِ «نقدینگی ساختگی نمی‌تواند
باشد؟» همان چیزی بود که این غلط را بیرون کشید.

### 🔎 سه اندپوینتِ تشخیصی

```
GET /vd/v4/<آدرس>           کلیدها و دلیل
GET /vd/v4/<آدرس>?debug=1   هر تلاش: نامِ میزبان، کدِ HTTP، کدِ عددیِ JSON-RPC،
                            و مرزهای هر تکه‌ی بلاک
GET /vd/<آدرس>?probe=1      حالا کلیدهای ذخیره‌شده را هم پروب می‌کند و
                            v4Keys را می‌شمارد
```

`?debug=1` همان چیزی بود که این را پیدا کرد:

```
base.publicnode.com       eth_getBlockByNumber   200
base.publicnode.com       eth_getLogs            403   ← فقط همین یک متد
base.gateway.tenderly.co  eth_getLogs            200
```

اندپوینتی که از ویندوز بازه‌ی ۶۰۰۰ بلاکی را بی‌مشکل می‌داد، از کلادفلر روی
`eth_getLogs` مشخصاً ۴۰۳ می‌دهد — همان الگوی `api.mainnet-beta.solana.com`.

### پنجره‌ی بلاک — از رویِ اندازه‌گیری، نه حدس

```
                        ۶۰۰۰        ۵۰۰        ۱۰        (از WSL، با کنترلِ مثبت)
base.drpc.org           400 (35)    400 (35)   ok
base.publicnode.com     ok          ok         ok
mainnet.base.org        413(-32614) ok         ok
```

`base.drpc.org` از فهرستِ لاگ بیرون رفت: روی ۵۰۰ بلاک هم رد می‌کند ولی متنِ
خطایش از ۱۰۰۰۰ حرف می‌زند — **متنِ خطا شاهد نیست، کدِ عددی هست.**
`mainnet.base.org` سقفِ صریحِ ۲۰۰۰ دارد. پس پنجره **۱۰۰۰ بلاک** است و پوششِ
بیشتر با **تکه‌ی دوم** گرفته می‌شود، نه با پنجره‌ی گشادتر. نگهبانِ ماندگار در
سوییت: مجموعِ پنجره باید زیرِ ۲۰۰۰ بماند.
⚠️ مرزهای `eth_getLogs` شاملِ خودشان‌اند — گامِ بینِ تکه‌ها `span+1` است نه
`span`. همین off-by-one در نسخه‌ی اول بود و یک پروب گرفتش.

### ⚠️ چیزی که نگهبان ندارد — و ادعا نمی‌کنیم دارد

باریک‌سازیِ `=== "sell" ? "sell" : null` در گذرِ اثبات عمداً برداشته شد و
سوییت **سبز ماند**: تا وقتی `VD_POSITIVE_ONLY.V4_SINGLE` برقرار است آن دسته
اصلاً نمی‌تواند منفی بسازد. کد ماند چون درست است، ولی یک پروب همان **وابستگی**
را پین می‌کند — اگر روزی پرچم برگردد، قرمز می‌شود.

### کلیدهایی که حدس هرگز نمی‌گرفت (از داده‌ی واقعی)

```
fee=0        tickSpacing=1        fee=830369  tickSpacing=200
fee=9990     tickSpacing=100      fee=49910   tickSpacing=499
fee=49900    tickSpacing=499      fee=8388608 tickSpacing=200 + هوکِ واقعی
```

و کلیدی با `fee=100, tickSpacing=1` که *جزوِ* چهار حدس است ولی ضدجفتش اترِ
**پوشیده** بود نه بومی — ردیفِ حدسی همیشه بومی را می‌زند.

### قیدهایی که دست‌نخورده ماندند

ردیفِ نسخه ۴ همچنان **فقط-مثبت** است (`VD_POSITIVE_ONLY`)، `uniswap-v4` هنوز
در `GT_DEX_TO_VENUE` **نیست**، و `VD_V4_STAGE_COUNTERS` بسته است: مرحله‌ی WETH
فقط اترِ بومی یا پوشیده، مرحله‌ی USDC فقط USDC. یک استخرِ واقعی که توکن را با
توکنِ نامرتبطی جفت کرده باشد پروب **نمی‌شود** — کوتش چیزی درباره‌ی *خروج*
اثبات نمی‌کند.

بدونِ کلیدِ واقعی، `buildProbe` بایت‌به‌بایت همان ۲۱ فراخوانیِ دیروز را
می‌سازد؛ با کلیدهای واقعی حداکثر چهار ردیف به هر مرحله اضافه می‌شود (۲۱ ←
حداکثر ۲۵). هر دو سو در سوییت پین شده‌اند.

**کامیت‌ها:** `ae4176a` · `678a52b` · `f91b936` · `68007f7` · `669831e` ·
`ecb7846`. هیچ فایلی به `_site` اضافه نشد، پس **خطِ Build عوض نمی‌شود**.

---

## ۰د. کارِ ۱۳ سپتامبر ۲۰۲۶ — باگِ متناوبِ حکم، و پوششِ نسخه ۴

**همه‌چیز دیپلوی شده. یکی زنده تأیید شد، یکی زنده رد شد.**

### 🔴 باگِ متناوبِ دسته — پیدا، رفع، و تأییدِ زنده

نشانه: `/vd/<توکن Base>` گاهی `{"v":null}` با دو ردیفِ `batch-failed` می‌داد،
برای **هر** توکن، حتی یواس‌دی‌سی. چون متناوب بود، هر نتیجه‌گیری با یک نمونه
غلط درمی‌آمد. دو درخواستِ پشت‌سرهم به همان توکن:

```
{"v":"sell", "ms":83,  ۱۸ ردیف، همه quoted}
{"v":null,   "ms":698, دو ردیف، هر دو batch-failed}
```

علت، کاملاً اندازه‌گیری‌شده. پنج اندپوینتِ `VD_RPCS` سنجیده شدند
(کنترل = ۱ تماس ×۳، دسته = ۱۸ تماس ×۵، با user-agentِ مرورگر):

```
publicnode          کنترل ۳/۳    دسته ۵/۵
tenderly            کنترل ۳/۳    دسته ۳/۵
meowrpc             کنترل ۱/۳    دسته ۰/۵
drpc                کنترل ۳/۳    دسته ۰/۵
mainnet.base.org    کنترل ۳/۳    دسته ۰/۵
```

**سه‌تا از پنج‌تا هیچ‌وقت دسته را قبول نمی‌کنند** — تماسِ تکی را سالم جواب
می‌دهند و دسته‌ی ۱۸تایی را با ۴۰۰ رد می‌کنند. سقفِ failover دو بود و
`VD_RPCS[1]` همان `meowrpc` بود، پس هر بار اندپوینتِ اول سکسکه می‌کرد
اندپوینتِ دوم **تضمین‌شده** شکست می‌خورد.

رفع (کامیت `e2575e4`):

- `VD_RPCS` فقط `publicnode` و `tenderly`؛ جدولِ سنجش در خودِ فایل نوشته شد
  تا آن سه‌تا برنگردند.
- `VD_MAX_ENDPOINTS = 3` به‌جای عددِ ثابتِ ۲.
- `baseRpcsFor(env)` قرینه‌ی `solRpcsFor` — سکرتِ `BASE_RPC` (آلچمی) اول
  امتحان می‌شود. کلید در مسیرِ URL است و هرگز لاگ نمی‌شود.
- `batch-failed` حالا `batch-failed:<وضعیت>` است (صفر برای پرتابِ خطا)، با
  همان قاعده‌ی پیشوند+عددِ سولانا.

✅ **تأییدِ زنده: ۸۴ از ۸۴ درخواست سبز، تأخیر از ~۸۰۰ به ~۱۲۰ میلی‌ثانیه.**

⚠️ فهرستِ آرپی‌سیِ داخلِ `web/index.html` عمداً دست‌نخورده و با هر پنج اندپوینت
ماند: مرورگر تماس‌ها را تکی می‌زند، پس این اندازه‌گیری به آن ربطی ندارد.
دو فهرست، دو قاعده، با دلیل.

### ⚠️ پوششِ یونی‌سواپ نسخه ۴ — ساخته شد، زنده است، بازدهی‌اش صفر

کامیت `7fe1ab7`. آدرسِ `V4Quoter` از دو منبعِ مستقل تأیید شد
(`Uniswap/contracts` فایلِ `deployments/8453.md` و برچسبِ بیسکن):

```
V4Quoter    0x0d5e0F971ED27FBfF6c2837bf31316121532048D
سلکتور      0xaa9d21cb
```

🔴 صفحه‌ی `docs.uniswap.org/contracts/v4/deployments` کهنه است و می‌گوید
نسخه ۴ زنده نیست — از آن استفاده نشود.

کوترِ نسخه ۴ **کلیدِ کاملِ استخر** می‌خواهد و شناسه‌ی استخر هشِ همان کلید
است، پس از آدرسِ توکن بازیابی نمی‌شود. چهار کلیدِ استانداردِ بی‌قلاب حدس زده
شد. چون حدس است، این ردیف **فقط-مثبت** است: کوتِ مثبتش «فروش ممکن»
می‌سازد، ولی ریوِرت و صفرش **هرگز** «فروش ممکن نیست» نمی‌سازند. سه لایه
این را ساختاری غیرممکن کرده: جدولِ فریزشده‌ی `VD_POSITIVE_ONLY`، **گاردِ
کوروم** (فهرستی که فقط از ردیف‌های فقط-مثبت ساخته شده → «نامعلوم»، نه اتهام)،
و ممنوعیتِ حضورِ `uniswap-v4-base` در `GT_DEX_TO_VENUE`.

اندازه‌گیریِ زنده:

- ✅ دسته‌ی ۲۲تایی کاملاً پایدار — ۹۶ درخواستِ پیاپی، صفر `batch-failed`.
- ✅ قاعده‌ی فقط-مثبت کار می‌کند: چهار ریوِرت، حکم «نامعلوم» می‌ماند.
- 🔴 **از ۱۱ توکنِ متمایزِ نسخه ۴، شش اجرای پیاپی، هیچ کوتی به‌دست نیامد.**

معیار پیش از دیدنِ عدد گذاشته شده بود (دو از ده). صفر آمد. کد **عمداً
برداشته نشد** تا اول راهِ ایندکس‌کردنِ کلید امتحان شود؛ اگر آن هم نگرفت،
`git revert 7fe1ab7`.

### سه راهی که با عدد بسته شدند

۱. **کلیدهای حدسیِ بی‌قلاب** — صفر از یازده. احتمالاً چون توکن‌های تازه با
   لانچر ساخته می‌شوند و استخرشان قلاب دارد. هنوز فرضیه است.
۲. **بک‌فیلِ لاگِ `Initialize`** — همه‌ی آرپی‌سی‌ها بازه را سقف می‌زنند؛
   آلچمیِ رایگان فقط **۱۰ بلاک**.
۳. **متادیتای گِکوترمینال** — شناسه‌ی استخر را می‌دهد ولی کارمزد/فاصله‌ی
   تیک/قلاب را ندارد. کلیدهای واقعیِ یک استخرِ نسخه ۴ فقط قیمت و حجم‌اند.

### 🟢 راهِ بعدی

**ایندکس‌کردنِ کلید موقعِ ساخت.** بقیه‌ی تجمیع‌کننده‌ها حدس نمی‌زنند، ایندکس
می‌کنند: لاگِ `Initialize` یک بار در عمرِ هر استخر رخ می‌دهد و کلید را اعلام
می‌کند. ما کرونِ ساعتی و انبارِ KV را داریم، و گِکوترمینال `pool_created_at`
و شناسه را می‌دهد — پس بلاکِ تقریبی معلوم است و یک `eth_getLogs` باریک با
فیلترِ شناسه کلیدِ دقیق را می‌دهد. **`base.drpc.org` تا ۱۰۰۰۰ بلاک `getLogs`
می‌دهد** (رایگان) — دسته قبول نمی‌کند ولی `getLogs` دسته نیست.

```
توپیک Initialize
0xdd466e674ea557f56295e2d0218a125ea4b4f0f6f3307b95f85e6110838d6438
```

### 🔴 درس‌های امروز

- **«بعد از تغییرِ من خراب شد» با «تغییرِ من خرابش کرد» یکی نیست.** پوششِ
  نسخه ۴ به‌خاطر نشانه‌ای که مالِ آن نبود revert شد. پیش از revert، همان
  سنجش را چند بار تکرار کن.
- **وقتی دو حدسِ پیاپی با عدد رد شد، حدسِ سوم نزن — ابزارِ اندازه‌گیری بساز.**
  سه فرضیه پشتِ‌سرهم غلط درآمدند؛ چیزی که مسئله را حل کرد یک جدولِ سنجش بود.
- **هر سنجشِ شبکه باید کنترلِ مثبت داشته باشد.** یک اسکریپتِ بی‌کنترل روی
  همه‌چیز ۴۰۳ داد (user-agentِ پایتون) و شبیهِ شواهد به‌نظر می‌رسید.
- **کارِ ایجنت را پیش از شکستنِ عمدیِ نگهبان‌ها کامیت کن،** و با
  `git checkout <کامیت> -- <فایل>` برگردان نه با `--`.
- **شکستنِ یک پینِ جدولی ثابت نمی‌کند رفتار هم نگهبان دارد** — پین را عمداً
  خنثی کن و ببین ادعای رفتاری هم قرمز می‌شود یا نه.
- **امتیازِ ریسک با سکسکه‌ی آرپی‌سی ۶۳ واحد جابه‌جا می‌شد** (همان توکن، یک بار
  ۱۰۰ «پرخطر» و یک بار ۳۷ «با احتیاط»). برای محصولی که حرفش «حدس نمی‌زنیم»
  است، بدترین جور شکست بود — و با رفعِ بالا برطرف شد.

---

## ۰ج. کارِ ۷ تا ۱۲ سپتامبر ۲۰۲۶ — گزارش روزانه، جفت‌های تازه، و تمِ روشن

**همه‌چیز دیپلوی شده و روی سایتِ زنده سنجیده شد.**

### داده و حکم

- **گزارش روزانه زنده است.** یک کرون ساعتی (دقیقه‌ی ۱۷) استخرهای تازه‌ی Base
  را می‌خواند، هر توکن را از دروازه‌های واقعی می‌سنجد و در KV می‌نویسد:
  `report:<تاریخ>` و `pairs:base:latest`. سه مسیرِ خواندن:
  `/report/today.json` · `/report/<تاریخ>.json` · `/pairs.json`
  اجرای دستی: `GET /report/run` با هدرِ `x-run-key`.
  KV با نامِ `zaexa-data` **در `wrangler.toml` تعریف شده، نه فقط در پنل** —
  فایل هنگام deploy منبعِ حقیقت است و بایندینگِ فقط-پنلی می‌تواند بی‌صدا پاک
  شود.
- 🔴 **حکمِ غلطِ «فروش ممکن نیست» ریشه‌کن شد.** دو علت داشت و هر دو بسته شد:
  روترهای Solidly وقتی استخری وجود ندارد صفر برمی‌گردانند نه revert، و یک
  revert از صرافی‌ای که اصلاً آن استخر را ندارد به‌عنوان «دلیل» شمرده می‌شد.
  حالا فقط از صرافی‌ای که پوشش دارد دلیل پذیرفته می‌شود. روی سایتِ زنده تأیید
  شد: از ۵۵ توکنِ سنجیده‌شده، ۱۰ تا «فروش ممکن»، **صفر** تا «فروش ممکن نیست»،
  بقیه «نامعلوم».
- ⚠️ **روی Base رفت‌وبرگشتِ واقعی نداریم** — آنچه داریم یک *کوتِ فروش* از هفت
  صرافی است. حالا `checkKind` (که فقط از یک جدولِ ثابت خوانده می‌شود و
  پارامترِ ورودی ندارد) این ادعای غلط را **ساختاری** غیرممکن کرده.
- **روترِ Uniswap نسخه‌ی ۲** روی Base اضافه شد. نسخه‌ی ۴ هنوز باز است (بخش ۱۰).
- **صفحه‌ی جفت‌های تازه** روی `/pairs` زنده شد — فقط خواندنی، از `/pairs.json`.
- **آدرسِ صفر** دیگر به سایت‌مپ نمی‌رسد.

### ظاهر و رفتار

- **تمِ روشن بازسازی شد.** فیروزه‌ای از رمپِ دکمه‌ها و اجزای گرافیکی رفت؛ رمپ
  از توقفِ آبیِ نشانِ برند شروع می‌شود و دمِ بنفش-تا-ماجنتا کوتاه‌تر شد.
  ⚠️ **زاویه‌ها قفل‌اند:** دکمه‌ها و تیترِ صفحه‌ی معرفی ۱۰۵ درجه، بقیه ۱۳۵.
  ⚠️ **متنِ دکمه‌ها هرگز مشکی نمی‌شود** (تصمیمِ صریحِ مالک) و جوهرش را از
  `--on-acc` می‌خواند، پس در تمِ تیره تیره و در تمِ روشن سفید است.
  🔴 یک نشتِ واقعی هم پیدا شد: از لحظه‌ای که در بلوکِ روشن هگزِ لفظی نشست،
  تمِ تیره بی‌صدا رمپِ روشن را ارث می‌برد. هر فایلی که هگزِ لفظی در بلوکِ روشن
  دارد باید `--grad` مخصوصِ خودش را در بلوکِ تیره هم اعلام کند.
- **تم بین صفحه‌ها مشترک شد:** هر سه صفحه کلیدِ `zaexa.theme.v1` را می‌خوانند
  و می‌نویسند، با یک مهاجرتِ یک‌باره از کلیدِ قدیمی.
- **لینک‌های داخلیِ صفحه‌ی توکن** درست شد. دو باگِ پشت‌سرهم: یک `<base href="/">`
  که هر `href="#x"` را به صفحه‌ی معرفی می‌برد، و `leaveTokenPage` که آدرس را
  به ریشه می‌فرستاد (ریشه از ۵ سپتامبر صفحه‌ی معرفی است) و باعث می‌شد
  یکی‌درمیان صفحه‌ی توکن از نو بار شود.
- **نوارِ بالای دو صفحه یکی شد** تا لوگو موقعِ رفت‌وآمد نپرد. جهتش یک بار عوض
  شد و تصمیمِ نهاییِ مالک این است: **اپ دست‌نخورده، صفحه‌ی معرفی با اپ جور
  می‌شود.** در ۲۱ عرض اندازه‌گیری شد؛ فقط در باندِ ۹۰۰ تا ۹۶۰ یک پیکسل اختلاف
  می‌ماند که با این توکن‌ها بسته نمی‌شود.
- **فوترِ صفحه‌ی یک توکنِ سولانا** دیگر ادعای Base نمی‌کند.

### شمارش

- **`view:home` و `cta:app`** از صفحه‌ی معرفی می‌آیند؛ نرخِ تبدیل دیگر کور
  نیست. قاعده‌ها همان اپ است: GPC خاموش می‌کند، پرچمِ `zaexa.dev` خاموش
  می‌کند، بدنه همان سه فیلد، و هیچ‌چیز از محتوای صفحه یا کاربر داخلش نمی‌رود.

### درسِ کاریِ تازه

**«درست شد» را با عدد ثابت کن، نه با ادعا.** برای ظاهر: رندرِ پیکسل‌به‌پیکسل
در برابر نسخه‌ی پایه با انیمیشن‌های خاموش. برای رفتار: شبیه‌سازیِ خودِ کلیک.
و **نگهبانِ کهنه را تعمیر کن، شل نکن** — دو بار یک ادعای قدیمی قرمز شد چون
سنجه‌اش تقریبی بود، نه چون کد خراب بود؛ هر دو بار سنجه دقیق‌تر شد و عمداً
شکسته شد تا ثابت شود هنوز باگِ واقعی را می‌گیرد.

---

## ۰ب. کارِ ۴ و ۵ سپتامبر ۲۰۲۶ — صفحه‌ی معرفی روی ریشه

**همه‌چیز دیپلوی شده و روی سایت زنده با `WebFetch` سنجیده شد:** `/` صفحه‌ی
معرفی می‌دهد، `/app` اپ، و `/t/<آدرس>` هم اپ.

### نقشه‌ی مسیرها

| آدرس | فایل در مخزن |
|---|---|
| `/` | `web/landing.html` |
| `/app` | `web/index.html` |
| `/t/<آدرس>` | Worker خودش `/app` را از ASSETS می‌گیرد و تگ og توکن را تزریق می‌کند |

⚠️ **نام فایل با آدرس یکی نیست، و این عمدی است.** جابه‌جایی در خطِ Build انجام
می‌شود، نه با تغییر نام فایل:

```
mkdir -p _site && cp web/index.html _site/app.html && cp web/landing.html _site/index.html && cp web/_headers web/*.js _site/
```

دلیلش: `web/test/run.py` حدود چهل جا `web/index.html` را **به‌عنوان اپ** باز
می‌کند — og، هگزهای برند، `CHAIN.executor`، فهرست DEXها، چک‌سام‌ها، تمِ روشن و
تیره. تغییر نامِ فایل یعنی دست‌بردن در همه‌ی آن‌ها روی فایلی که اپِ زنده است.
جابه‌جایی در یک خط همان نتیجه را با کسری از ریسک می‌دهد. توضیحش در
`wrangler.toml` نوشته شده و نگهبانِ خط Build هم هر دو نیمه‌اش را می‌سنجد.

### 🔴 باگی که اگر می‌رفت روی سایت، بی‌صدا می‌شکست

هندلر `/t/<آدرس>` در Worker سندِ **ریشه** را از ASSETS می‌گرفت و og را داخلش
تزریق می‌کرد. با نشستنِ صفحه‌ی معرفی روی ریشه، هر لینکِ توکن به‌جای اپ صفحه‌ی
معرفی را باز می‌کرد — و چون **۲۰۰ برمی‌گرداند**، نه خطایی در لاگ می‌ماند نه
کاربر می‌فهمید چرا اپ بالا نمی‌آید. حالا `/app` را می‌گیرد، **بدون پسوند**: به
همان دلیلی که قبلاً برای ریشه ثبت شد، `/app.html` یک ۳۰۷ به `/app` می‌دهد و آن
ریدایرکت مسیر را جا می‌گذارد. نگهبان `[token page]` در `worker/test.mjs`.

### ‼️ کارِ لندینگِ ۲ سپتامبر از بین رفته بود

طرحِ قبلی (`web/app.html`، گلابِ `cp web/*.html`، `APP_VIEWS`) **هرگز push
نشد**. امروز با `grep` روی مخزن قطعی شد: هیچ‌کدام از `APP_VIEWS`، `view:home`،
`cta:app`، `[landing routes]`، `[landing hash]` و `[ev names]` در کد نیستند.
صفحه‌ی معرفی از صفر بازساخته شد، پس این‌ها **الان وجود ندارند** و اگر لازم‌اند
باید دوباره ساخته شوند:

- **هیچ رویدادی نرخ تبدیل صفحه‌ی معرفی را نمی‌سنجد.** `view:home` و `cta:app`
  نیستند، یعنی نمی‌دانیم چند نفر از `/` به `/app` می‌روند — و همان آمار بود که
  اصلاً دلیلِ ساختنِ این صفحه شد (۴۴ از ۵۳ نفر هیچ عددی تایپ نکردند).
- نگهبانی نیست که مسیرهای hash صفحه‌ی معرفی را با نماهای واقعی اپ هم‌گام نگه
  دارد.

### فوترِ یکسان و لوگوی کلیک‌شدنی

دو فوتر drift کرده بودند: اپ «ZAEXA» را متنِ درشت می‌نوشت جایی که صفحه‌ی معرفی
قفلِ لوگو را می‌کشید، خط کپی‌رایت نداشت، و ستون‌های About و Support چیزهای
متفاوتی فهرست می‌کردند. حالا هر دو یک محتوا را رندر می‌کنند — با اندازه‌گیری
تأیید شد، نه چشمی.

- **قفلِ لوگو دوباره استفاده می‌شود، کپی نمی‌شود.** به `<g>`های هدر شناسه داده
  شد و فوتر با `<use>` همان را می‌کشد. یک نسخه‌ی دوم از آن SVG یعنی تکرارِ
  شناسه‌های `zxm-*` (گرادیان و ماسک) در یک سند، و آن‌وقت گرادیان‌ها به اولی وصل
  می‌شوند.
- **نسبت قفل اندازه‌گیری شد:** ارتفاع لوگوتایپ به نشان در هدر اپ، فوتر اپ و
  فوتر صفحه‌ی معرفی هر سه **۰٫۴۷۸** — همان ۰٫۴۸ که در بسته‌ی برند قفل است.
- لوگوی هدر اپ یک `<div>` بود و کلیک روی آن هیچ کاری نمی‌کرد. حالا `<a href="/">`
  است؛ تازه از وقتی صفحه‌ی معرفی روی ریشه نشست، جایی برای رفتن دارد.

### ✅ برچسب عمومی BaseScan تأیید شد (۴ سپتامبر ۲۰۲۶)

آدرس v5 حالا روی BaseScan برچسب **`Zaexa: Swap Executor`** دارد — با `WebFetch`
روی خودِ صفحه‌ی آدرس دیده شد، نه فقط از روی ایمیل. باگ ۱۰ بسته شد.
⚠️ ربی و Pocket Universe پایگاه برچسب خودشان را دارند و از BaseScan نمی‌خوانند،
پس «Unknown Signature Type» ربی (باگ ۱۱) سرِ جایش می‌ماند — و جمله‌ی FAQ که
همین را می‌گوید هنوز **درست** است و نباید عوض شود.

### نگهبان‌های تازه — ۱۹۳ → ۱۹۶ کاوشگر

- `[landing]` — صفحه‌ی معرفی هیچ درخواست بیرونی نداشته باشد (بر اساس **دامنه**،
  نه آدرس کامل)، دو فونت woff2 جاسازی‌شده و بریده‌نشده داشته باشد، و تگ‌های og
  داشته باشد.
- خط Build — هم `_site/app.html` و هم `web/landing.html → _site/index.html` را
  می‌سنجد. اگر کسی این خط را «ساده» کند، یا اپ ناپدید می‌شود یا صفحه‌ی معرفی
  هرگز منتشر نمی‌شود، و هیچ‌کدام خطا نمی‌دهند.
- `check_one_executor_address` حالا `landing.html` را هم می‌سنجد: آن صفحه
  `CHAIN` را اجرا نمی‌کند و آدرس را ثابت در HTML دارد، پس باید مو‌به‌مو با
  `CHAIN.executor` یکی باشد. وگرنه روزی قرارداد عوض می‌شود و **درِ ورودیِ سایت**
  آدرس مرده را تبلیغ می‌کند.
- `[og tags]` حالا روی **هر صفحه‌ای که تگ og دارد** حلقه می‌زند، نه فقط اپ.
  نسخه‌ی `?v=` در دو فایل نوشته می‌شود و `/og.png` هدر immutable دارد — صفحه‌ی
  جامانده تا ابد کارتِ کهنه را نشان می‌داد و هیچ تستی قرمز نمی‌شد.

هر چهار مورد عمداً شکسته و بازگردانده شدند.

### دو درسِ CSS که هر دو یک ریشه دارند

ویرایشِ CSS با regex دو بار بی‌صدا چیزی را کشت:

1. حذف یک قاعده، پیشوندِ جامانده را به انتخابگر بعدی چسباند و
   `.road-item.active .closing` ساخت — قاعده‌ی `.closing` چند دور **هیچ اثری
   نداشت** (نه پدینگ، نه خط بالا، نه وسط‌چینی) و در اسکرین‌شات هم به نظر
   قابل‌قبول می‌آمد.
2. جابه‌جاکردن یک عنصر بین دو بخش، انتخابگرِ محدودشده‌اش (`.closing .fine`) را
   بی‌مصرف کرد — متن یک روز کامل **بی‌استایل** رندر شد.

📌 قاعده: وقتی عنصری جابه‌جا می‌شود، **انتخابگرش را چک کن نه فقط اعلان‌هایش**.
قاعده‌ای که به هیچ کلاسِ موجودی نمی‌خورد باید FAIL بدهد.

---

## ۰الف. کارِ ۲ سپتامبر ۲۰۲۶ — قابلیت مشاهده، سخت‌سازی، و RPC

پنج چیز روی سایت زنده عوض شد. همه دیپلوی شده‌اند.

**۱) گیرنده‌ی خطای سراسری.** تا پیش از این یک استثنای گرفته‌نشده در جاوااسکریپت
**صفر** اثر داشت: صفحه بی‌صدا کار نمی‌کرد و ما هرگز نمی‌فهمیدیم. حالا بالای
`<head>` نصب است (پایین‌تر، خطایی که خودِ آن اسکریپت را می‌کشد گرفته نمی‌شود)
و دو خروجیِ **مستقل** دارد:
- روی صفحه: کد پایدار مثل `E-JS-4C1A` (هش FNV-1a از پیام+فایل+خط، پس یک باگ
  همیشه یک کد) + دکمه‌ی Copy details. هیچ‌جا فرستاده نمی‌شود مگر کاربر کلیک کند.
- در آمار: `err:js` / `err:promise` / `err:res` به‌علاوه‌ی همان چهار رقم hex.
  ⚠️ hex **فقط** برای این سه رویداد مجاز است؛ `EV_DETAIL_OK` برای بقیه بسته ماند.

⚠️ **این گیرنده سه بار مقصر را اشتباه گرفت و هر سه بار اصلاح شد** — اول همه‌ی
خطاها، بعد شکست‌های مدل‌شده‌ی خودمان (`gtBackoff` و برادرانش، که رابط کاربری
خودشان را دارند)، و آخر خطای افزونه‌ی مرورگر (`Cannot redefine property:
isZerion` از یک `chrome-extension://` — دو کیف پول سرِ `window.ethereum` دعوا
داشتند). **قاعده: «یک خطا رخ داد» با «باگِ ما رخ داد» یکی نیست.** فیلترها
همیشه روی پرچمی که کدِ خودمان گذاشته یا روی *منشأ*ند، هرگز روی متن پیام.

**۲) محدودکننده‌ی نرخ.** `/ev`، `/gt` و `/t/<آدرس>`: ۱۲۰ درخواست در ۶۰ ثانیه
برای هر مسیر، کلید = هشِ IP (نه خودِ IP). یک Map درون‌ایزوله، **عمداً نه
بایندینگ KV** — هر بایندینگ تازه یک قدم دستیِ پنل می‌خواهد و نبودنش کل deploy
را می‌شکند. ⚠️ ۱۲۰ را تنگ‌تر نکن: کاربران پشت VPN، IP خروجی مشترک دارند.
⚠️ `/t/` بالای سقف **۴۲۹ نمی‌دهد** — کارت عمومی سرو می‌کند. کاربر یک صفحه‌ی
واقعی باز کرده؛ پیش‌نمایش را خراب کن، نه صفحه را.

**۳) هدرهای امنیتی در `web/_headers`.** CSP با `script-src 'self'`،
`frame-ancestors 'none'`، nosniff، Referrer-Policy، Permissions-Policy.
⚠️ **`base-uri` باید `'self'` باشد، هرگز `'none'`** — `index.html` روی
`/t/<آدرس>` یک `<base href="/">` تزریق می‌کند و `'none'` همه‌ی صفحه‌های توکن
را با «Could not load ethers» می‌کشد. نگهبان صریحاً `'none'` را رد می‌کند.
⚠️ `connect-src` باز می‌ماند روی `https:`/`wss:` وگرنه Custom RPC و
WalletConnect می‌شکنند.
📌 `_headers` فقط روی کلادفلر اعمال می‌شود؛ **هارنس محلی هرگز اجرایش نمی‌کند**،
پس نگهبان فقط *فایل* را می‌سنجد و تأیید واقعی فقط بعد از deploy است.

**۴) اسکن ایمنی توکن — باگ AERO.** AERO ریسک **۷۷** گرفت (بلوچیپ Base با ~۴۶۰
میلیون دلار مارکت‌کپ). علت: `hex.includes(selector)` روی کل بایت‌کد به‌شکل یک
رشته — نه به مرز بایت تراز بود، نه فرق می‌گذاشت بین «تابع وجود دارد» و «این
چهار بایت اتفاقی در داده آمده». حالا فقط `PUSH4 <selector>` واقعی شمرده
می‌شود (بایت `63` قبلش، شروع روی موقعیت زوج). **روی سایت زنده: ۷۷ → ۱۲.**
⚠️ این قاعده عمداً به `verifyDexes` تعمیم داده **نشد** — آنجا یک منفیِ کاذب یک
صرافی سالم را خاموش می‌کند، و این پروژه یک بار سه دکس زنده را «مرده» اعلام کرد.
📌 ماک تست هم غلط بود: سلکتورها را با `dead` می‌چسباند و هیچ `PUSH4`ی نداشت —
شکلی از بایت‌کد که در واقعیت وجود ندارد. ماک اصلاح شد، نه قاعده.

**۵) RPCهای مرده.** سایت کند شده بود چون **اولین** اندپوینت فهرست
(`1rpc.io`) دیگر هدر CORS نمی‌فرستد و در یک بارگذاری هشت بار امتحان می‌شد.
اندازه‌گیری از مرورگر واقعی روی مبدأ zaexa.com (⚠️ CORS به مبدأ بستگی دارد؛
curl هیچ چیز اثبات نمی‌کند):

```
PASS  330ms base.publicnode.com      PASS 453ms base.meowrpc.com
PASS  492ms base.drpc.org            PASS 537ms base.gateway.tenderly.co
PASS  650ms mainnet.base.org  ← زیر بار ۴۲۹ می‌دهد، پس آخر فهرست
CORS  1rpc.io · base.llamarpc.com · blockpi · omniatech   (حذف شدند)
ankr  ۲۰۰ می‌دهد ولی chainId ندارد — حالا کلید می‌خواهد
```

⚠️ **قاعده‌ی مقایسه‌ای.** در مرورگر، شکستِ CORS و قطعیِ اینترنت از هم قابل
تشخیص نیستند — هر دو `Failed to fetch`. پس یک اندپوینت فقط وقتی مرده علامت
می‌خورد که **اندپوینت دیگری در همان تلاش جواب داده باشد**. اگر همه بیفتند،
تقصیر شبکه‌ی کاربر است و هیچ‌کدام علامت نمی‌خورد. بدون این قید، کاربرِ پشت
فیلترینگ — که بخشی از کاربران واقعی ماست — سایت را برای همیشه خراب می‌دید.

**جانبی:** `.gitattributes` با `* -text` (کلون پیش‌فرض ویندوز باندل
walletconnect را خراب می‌کرد و هیچ‌کس روی ویندوز نمی‌توانست تست بگیرد) ·
سقف `gtCache` · `_patch.py` حذف شد · دکمه‌ی اصلی و هدر حالا از اولین رندر
هر دو «Connect wallet» می‌گویند.

📌 **ادعایی که رد شد:** یک بازبین گزارش داد `SwapExecutor.sol:333` روی مسیر
حلقه‌ای با توکن fee-on-transfer و کارمزد غیرصفر underflow می‌کند. با خواندن کد
رد شد: `swapAmount` از اختلاف موجودی واقعی می‌آید و `_safeTransfer` دقیقاً
`fee` را کم می‌کند، پس عبارت به موجودیِ خرده‌ی قبلی تقلیل پیدا می‌کند که همیشه
≥ صفر است. با `MockFeeToken` خودِ مخزن هم تطبیق داده شد.

---

## ۱. وضعیت فعلی ✅

```
قرارداد فعال : 0x15e511Bf2Ea1a0F50F25E973d57Dce0D01946b6d   ← Base، v5، ۱ سپتامبر ۲۰۲۶
               سورس روی BaseScan تأیید شد (Pass - Verified)، و مستقل هم با
               getsourcecode بررسی شد — به پیام خودِ forge بسنده نشد
owner        : 0x8A0Dcb583C8CAdc481E34487c34f1B856fe97e23
feeRecipient : همان owner
کارمزد       : 0 bps  (سقف سخت ۱۰۰ bps = ۱٪، غیرقابل عبور حتی برای owner)
روترها       : هر هفت تا لیست‌سفید — Uniswap V3، Aerodrome، BaseSwap،
               SushiSwap، Alien Base، PancakeSwap V3، Aerodrome Slipstream
آداپتورها    : هیچ‌کدام. `allowedAdapter` خالی است و عمداً خالی مانده.
تست‌ها       : ۸۲ تست واحد سبز + ۱۱ تست fork سبز روی مین‌نت
               (۳۳ واحد + ۱۱ v2 + ۳ v3 + ۷ v4 + ۲۱ v5 + ۷ سلکتور + ۱۱ fork)
               ⚠️ تست fork بدون RPC رد می‌شود و باز هم «PASS» می‌دهد —
               گس حدود ۲۴۰۰ یعنی بدنه اجرا نشده. اجرای واقعی فقط با
               ./script/fork_test.sh (بلاک را پین می‌کند؛ یک `forge test`
               ساده روی اندپوینت عمومی ۴۲۹ می‌گیرد و شبیه خرابی به نظر می‌رسد)
               + سوییت Playwright رابط وب (**۲۷۶ کاوشگر**) + تست Worker با
               node (**۵۱ بخش**)، همه سبز
سایت         : https://zaexa.com — `/` صفحه‌ی معرفی، `/app` اپ، `/t/<آدرس>` اپ
               (هم Base هم سولانا)، `/pairs` جفت‌های تازه
               (Cloudflare Workers، از روی مخزن؛ جابه‌جایی در خطِ Build — بخش ۰ب)
انبار        : KV با نامِ `zaexa-data` — در `wrangler.toml`، نه فقط در پنل
کرون         : دقیقه‌ی ۱۷ هر ساعت، گزارشِ روزانه را می‌نویسد (بخش ۰ج)
رازهای پنل   : CG_KEY · SOL_RPC · JUP_KEY · RUN_KEY · **BASE_RPC** (تازه،
               ۱۳ سپتامبر — آلچمیِ Base؛ کلید در مسیرِ URL است و هرگز لاگ
               نمی‌شود. بخش ۰)
آرپی‌سیِ Worker: فقط دو اندپوینتِ **اندازه‌گیری‌شده** که دسته را قبول می‌کنند
               (`publicnode`، `tenderly`) — سه‌تای دیگر عمداً بیرون‌اند، بخش ۰.
               ⚠️ فهرستِ `web/index.html` جداست و هر پنج‌تا را دارد، چون
               مرورگر تماس‌ها را تکی می‌زند
آرپی‌سیِ لاگ  : فهرستِ **سومِ** جدا برای `eth_getLogs` (`V4_LOG_RPCS`):
               tenderly، publicnode، mainnet.base.org. اندازه‌گیری‌شده از داخلِ
               خودِ Worker — publicnode آن‌جا روی همین یک متد ۴۰۳ می‌دهد. بخش ۰
برچسب BaseScan: `Zaexa: Swap Executor` — تأیید شد ۴ سپتامبر ۲۰۲۶
مخزن         : github.com/0xheiy/ZAEXA (عمومی، بدون LICENSE) · ایکس @zaexadex
```

**تازه‌ترین تغییر:** ایندکسِ کلیدِ واقعیِ استخرِ نسخه ۴ از لاگِ `Initialize`، و
بازشدنِ مرحله‌ی USDC برای اثباتِ یک مثبت — یک توکنِ واقعی روی سایتِ زنده از
«نامعلوم» به «فروش ممکن» رسید. بخش ۰. قرارداد از ۱ سپتامبر دست نخورده است.

**پیش از آن:** گزارشِ روزانه، صفحه‌ی جفت‌های تازه، و بازسازیِ تمِ روشن —
بخش ۰ج.

**پیش از آن:** قرارداد **v5** دیپلوی و تأیید شد (بخش ۱۱د). تنها دلیلِ
اجباری‌اش این بود که یک توکن تازه‌ی Base که کل بازارش یک استخر Aerodrome
Slipstream است، در v4 **اصلاً قابل معامله نبود** — نه گران‌تر، ناممکن. حالا
`KIND_SLIPSTREAM` هست، و کنارش `KIND_ADAPTER` تا دکس بعدی بدون دیپلوی تازه
اضافه شود. رابط وب، هر دو README، `PROJECT_STATUS.md` و نگهبان تست همگی به
همین آدرس اشاره می‌کنند و سوییت اگر جز این باشد می‌افتد.

**قراردادهای رها شده (استفاده نکن):**

| آدرس | چرا رها شد |
|---|---|
| `0x6443C06bb117223DC818df54A09A642696D0489c` | بازنشسته — بدون ETH بومی — دو امضا لازم داشت |
| `0x9fc4608fA104b032B902650A4D12E0CA51a2F684` | بازنشسته — باگ SwapRouter02 (سلکتور غلط) |
| `0xC261E57cF5739A8a538884405600E4e45dF24802` | بازنشسته — کارمزد از توکن خروجی می‌گرفت |
| `0x2fea35aaDae6Cbf9b9481B06164907ccF95DB081` | بازنشسته — v1، جایگزین شد با v2 (بخش ۱۱الف) |
| `0xE980825d4B3911e35Be5804349be26eBBe93BcC6` | بازنشسته — v2، جایگزین شد با v3 |
| `0xb6AE1C7157f877854C498C44ab5ea3d6742416DC` | بازنشسته — v3، باگ ۰۴: سرریز حسابی برای گیرنده‌ای که پرداخت را در قلاب خودش جلو می‌فرستد |
| `0x76082b0fbd0a29C236dD2ae2B2F47BFD96d7F455` | بازنشسته — v4، هیچ شاخه‌ای به استخر Aerodrome Slipstream نمی‌رسید؛ `KIND_MAX` سه بود |

---

## ۲. 🔁 قاعده‌ی اول پروژه

**«نمی‌دانم» هیچ‌وقت نباید مثل «نه» رفتار کند.**

این کلاس باگ تا امروز **هفت بار** در جاهای مختلف تکرار شده:

1. ربات آربیتراژ: «RPC قطع است» می‌گفت وقتی فقط `is_connected` خطا داشت
2. اسکنر: «totalSupply وجود ندارد» می‌گفت وقتی شبکه جواب نداده بود
3. روتر: «هیچ مسیری نیست» می‌گفت وقتی کوت‌ها timeout شده بودند
4. رابط وب: قطعی شبکه را «No route» نشان می‌داد
5. اسکریپت دیپلوی: `cast balance … || echo 0` خطای شبکه را «موجودی صفر» خواند
   (کاربر ۰.۰۰۲۳ ETH داشت و اسکریپت گفت صفر)
6. ETH بومی: «بایت‌کد در دسترس نیست» — در حالی که ETH اصلاً قرارداد ندارد
7. **Exit check (۱۰ آگوست):** هر revert در شبیه‌سازی فروش «نمی‌توانی بفروشی»
   خوانده می‌شد. قطعی RPC وسط `staticCall` → توکن سالم قرمز می‌شد. بدترین جای
   ممکن برای این باگ، چون کل ارزش این پنل اعتماد است.

**محافظ‌های فعلی:** `try_call` در پایتون · تفکیک `success:false` از قطعی شبکه در
Multicall3 · `read_uint` در اسکریپت‌های bash (رشته‌ی خالی برمی‌گرداند، نه صفر) ·
برچسب‌های `unknown` در رابط · `preconditionGap()` در Exit check ·
تست‌های Playwright که مخصوصاً این را می‌گیرند (`[rpc down]` و `[exit:rpc-down]`).

### درس اضافه از باگ هفتم: **با تطبیق رشته‌ی خطا تصمیم نگیر**

نسخه‌ی اول اصلاح، پیام خطا را regex می‌کرد: اگر `transferFrom failed` بود
«تقصیر کیف پول» و وگرنه «تقصیر توکن». دو جور غلط می‌شد — و جهت دومش خطرناک
بود: توکن مخربی که پیام revertش همان رشته را داشت، **سالم** اعلام می‌شد.
حالا به‌جای خواندن پیام، دوباره از زنجیره پرسیده می‌شود: واقعاً موجودی و
allowance هست یا نه. سه‌حالته — «هست» / «نیست» / «نتوانستیم بفهمیم».

هر بار کدی می‌نویسی که از شبکه می‌خواند، اول این را از خودت بپرس.

## ۳. 🧪 قاعده‌ی دوم

**ماک باید شکل قرارداد واقعی را آینه کند، نه انتظار ما را.**

۲۷ تست سبز بودند در حالی که **هیچ سواپ یونی‌سواپی روی مین‌نت کار نمی‌کرد**.
قرارداد ساختار ۸ فیلدی `ExactInputSingleParams` (SwapRouter01، سلکتور
`0x414bf389`) می‌فرستاد، ولی Base از SwapRouter02 استفاده می‌کند (۷ فیلد،
سلکتور `0x04e45aaf`). تابع وجود نداشت → revert خالی → `missing revert data`.
`MockV3Router` همان ساختار غلط را پیاده کرده بود، پس تست‌ها هم‌دست باگ بودند.

حالا دو تست این شکاف را می‌بندند:
`SwapExecutor.selectors.t.sol` (آفلاین، میلی‌ثانیه) و
`SwapExecutor.fork.t.sol` (مقابل Base واقعی).

## ۴. 📦 قاعده‌ی سوم (سبک کار)

هر وقت تغییر **کوچک و مشخص** است، باید **دستور مستقیم** داده شود (`sed`)، نه زیپ.
دلیلش تجربه‌ی واقعی است: مرورگر زیپ دوم را `zaexa (1).zip` ذخیره کرد و کاربر
نسخه‌ی قدیمی را باز کرد؛ یک بار هم فایل‌ها در پوشه‌ی اشتباه باز شدند.
اگر زیپ لازم بود، **اسم یکتا** بگذار.

هرگز دستور با جای‌خالی (`<همان عدد>`) نده — کاربر عیناً paste می‌کند و bash می‌شکند.
دستورها باید خودکفا و قابل کپی‌کردن مستقیم باشند.

---

## ۵. رابط وب — چه چیزهایی ساخته شده

**معماری بدون سرور:** همه‌چیز در مرورگر، امضا با کیف پول خود کاربر،
هاست استاتیک رایگان. یک تجمیع‌کننده‌ی غیرمتمرکز نباید به سرور ما وابسته باشد.

```
web/index.html          ← کل اپلیکیشن در یک فایل (~۲۶۰۰ خط، HTML + CSS + JS)
web/test/run.py         ← تست Playwright، harness را خودش از index.html می‌سازد
web/test/stub-ethers.js ← ethers قلابی + AMM ساختگی (بدون شبکه)
web/README.md           ← اجرا، انتشار، تست
```

امکانات موجود:

- **سواپ** با مسیریابی کامل: مستقیم، دومرحله‌ای از WETH/USDC، و تقسیم سفارش
  بین دو مسیر برتر (۴ نسبت). ترجمه‌ی کامل `router.py` به JS.
- **جدول زنده‌ی صرافی‌ها** — قیمت هر ۶ صرافی کنار هم. برنده مشخص است.
  استخر نبودن `no pool` و قطعی شبکه `unknown` — **دو چیز متفاوت**.
- **Exit check** — رفت‌وبرگشت واقعی در یک `staticCall`. ستون اصلی محصول.
  «شکست پیش‌شرط» (`estimated`) از «فروش واقعاً revert شد» (`blocked`) جداست.
- **اسکنر ایمنی توکن** — ترجمه‌ی `scanner.py`: بایت‌کد، مالکیت، proxy، نقدینگی.
  برای ETH بومی پنل جداگانه‌ی «Native asset» نشان می‌دهد.
- **پورتفولیو** (صفحه‌ی جدا) — توکن‌های کیف پول با درصد سهم هرکدام.
- **Money flow** (صفحه‌ی جدا) — از `eth_getLogs` روی رویداد `Swap` استخرهای V3.
  بدون هیچ سرویس شخص ثالث. پنجره‌ها ۱ ساعته و ۶ ساعته
  (۲۴ ساعت ≈ ۴۳ هزار بلاک است و از سقف RPCهای عمومی رد می‌شود).
- **کوت معکوس** — مقدار دریافتی را وارد کن، ورودی لازم محاسبه می‌شود
  (`solveForOutput`، حل عددی — با Aerodrome هم کار می‌کند و split را نگه می‌دارد).
- دکمه‌های ۵۰٪ و MAX · تم تیره/روشن · ناوبری موبایل پایین صفحه
- **ردیف `vs Uniswap direct`** — چقدر بهتر از رفتن مستقیم به یونی‌سواپ. تفاوت
  ناچیز «same» نوشته می‌شود نه `+0.00%`، و اگر یونی‌سواپ کوت نداده باشد
  «unknown». هشدار گس همیشه در tooltip همراهش می‌رود.
- **لینک اشتراک‌پذیر کوت** — کل وضعیت در hash. توکن ناشناخته از همان دروازه‌ی
  `importToken` رد می‌شود و اگر قابل تأیید نبود، صریح گفته می‌شود.
- **برچسب Exit check در لیست توکن** — فقط نتایج همین جلسه، با سن داده.
  نبودِ برچسب یعنی «بررسی نشده»، نه «سالم».
- بخش بازخورد و لینک شبکه‌های اجتماعی — `LINKS` پر شد (ایکس و گیت‌هاب)

**تصمیم مهم پیاده‌سازی:** همه‌ی کوت‌ها از **Multicall3** عبور می‌کنند
(`0xcA11bde05977b3631167028862bE2a173976CA11`). به‌جای ~۴۵ درخواست، ۴ تا.
علاوه بر سرعت، تفکیک «استخر نیست» از «شبکه جواب نداد» را رایگان می‌دهد.

**درخواست‌های GeckoTerminal** در یک صف سریالی با فاصله‌ی ۲۴۰ms و ۳ بار retry
می‌روند. **شکست هرگز کش نمی‌شود** — فقط نتیجه‌ی موفق. قبلاً کش شدنِ شکست
باعث می‌شد آیکون و قیمت USDC برای همیشه خراب بماند.

**تصمیم طراحی:** جهت «میز معاملاتی» است نه جعبه‌ی سواپ، چون مزیت رقابتی
این محصول قیمت و شفافیت مسیر است و یونی‌سواپ مسیریابی را *پنهان* می‌کند.
حس بصری نرم و گرد (نزدیک به Matcha) ولی کپی نیست. سطوح خاکستری خنثی،
رنگ فقط جایی که **معنا** دارد (برنده، ریسک، هشدار).

### ۵ب. لایه‌ی داده‌ی قیمت — سقف نرخ و کش (۱۶ آگوست ۲۰۲۶)

**نشانه‌ای که حسام پیدا کرد:** اگر سایت را پشت سر هم رفرش کنی، لوگوها و نمودار
می‌افتند. سرویس خراب نبود — سقف عمومی GeckoTerminal ۳۰ درخواست در دقیقه **روی
IP** است و هر رفرش همه‌چیز را از نو می‌پرسید.

سه چیز انجام شد:

1. **پراکسی روی همان Worker.** `worker/index.js` مسیر `/gt/*` را پراکسی می‌کند.
   `wrangler.toml` حالا `main` دارد؛ هر مسیر دیگری دست‌نخورده به بایندینگ
   `ASSETS` می‌رود.
   ⚠️ **بار اول بی‌کلید روشن شد و وضع را بدتر کرد** — IP خروجی Workers بین
   همه‌ی مشتری‌های کلادفلر مشترک و از قبل سوخته است، پس همان اولین درخواست ۴۲۹
   خورد. برگردانده شد، بعد با کلید دوباره روشن شد.
   **قاعده:** API با سقفِ IP را بدون کلید از پشت Workers پراکسی نکن.
2. **کلید رایگان CoinGecko Demo.** با کلید، از `api.coingecko.com/api/v3/onchain`
   خوانده می‌شود و سقف روی *کلید ما*ست نه IP. کلید در پنل کلادفلر به‌شکل
   **Secret** با نام `CG_KEY` است و هرگز به مرورگر نمی‌رسد — دلیل اصلی وجود
   این پراکسی همین است. کلید فقط در هدر می‌رود، نه در URL (وگرنه در کش لبه و
   لاگ‌ها می‌نشیند). هر چهار مسیری که سایت می‌زند زیر `/onchain` آزموده شد.
   بازرسی از بیرون: هدر `x-zaexa-proxy` باید `miss-keyed` بدهد.
3. **کش ماندگار در `localStorage`.** آدرس لوگو (۷ روز) و شناسه‌ی استخر برتر
   (۱ روز). قیمت و مارکت‌کپ و والیوم عمداً ذخیره **نمی‌شوند** — عدد کهنه بدتر
   از نبود عدد است. «این توکن لوگو ندارد» فقط ۱ ساعت نگه داشته می‌شود، تا یک
   پاسخ ناقصِ گذرا یک هفته «ندارد» نشود. و هرچه از انبار مرورگر بیرون می‌آید
   بی‌اعتبار فرض می‌شود: آدرس لوگو مستقیم در `img.src` می‌نشیند، پس فقط
   `https://` پذیرفته است.

دو باگ جانبی هم درست شد: `if(!r.ok)throw` کامنتش «دائمی — بس است» بود ولی همان
`catch` می‌گرفتش و دو بار دیگر تلاش می‌کرد؛ و خطای دائمی شمارنده‌ی قطع‌کننده‌ی
مدار را بالا می‌برد، پس سه توکن ناشناس کل نمودار را یک دقیقه می‌خواباند.

### ۵ج. چیدمان صفحه‌ی سواپ — قواعدی که هرکدام یک بار شکست

هر کدام کاوشگر دارد. قبل از دست‌زدن به CSS این را بخوان.

- `header{width:100%}` **لازم است.** `body` ستون فلکس است و `margin:0 auto` روی
  محور عرضی کشیدگی را خنثی می‌کند؛ بدون آن هدر ۷۸۵ پیکسل می‌شود در برابر ۱۱۴۰
  محتوا. کاوشگر `[header width]`.
- `header>.navPad{flex:0 0 0}` — لوگو و ناوبری یک گروه در چپ، چیپ‌ها لبه‌ی
  راست. با دو فاصله‌گذار کشسان، ۳۵۰ پیکسل اضافه به دو حفره‌ی ~۲۱۰ پیکسلی
  تقسیم می‌شد. کاوشگر `[header]`.
- `.row.hero{align-items:start}` **به‌علاوه‌ی** `@media(min-width:941px){.plot{flex:0 0 363px}}`
  — این دو با هم معنا دارند. فقط اولی → نمودار به کف `min-height` می‌افتد
  (۳۸۵ → ۲۴۰). فقط دومی → کارت نمودار همچنان کشیده می‌شود و هر تغییر ارتفاع در
  کارت کناری تکانش می‌دهد. هیچ‌کدام → کارت سواپ کش می‌آید و زیر دکمه ۸۶ تا ۹۹
  پیکسل فضای مرده می‌ماند. نشانه‌ای که حسام دید: نمودار روی بارگذاری ۵۷۲ بود و
  با اولین تعویض توکن روی ۵۱۰ می‌نشست. کاوشگرهای `[card fit]` و `[chart steady]`.
- **اعلان‌ها بالای دکمه‌اند و دکمه ته کارت** — با
  `@media(min-width:941px){#notices{min-height:64px}}` تا جای اعلان رزرو باشد.
  یک بار برعکس شد چون ارتفاع اعلان به جفت توکن بستگی دارد و دکمه تا ۷۸ پیکسل
  می‌پرید؛ حالا با رزرو، ترتیب برگشت بدون بازگشت باگ. ⚠️ اعلان بلندتر از ۶۴
  پیکسل باز هم دکمه را جابه‌جا می‌کند — کاوشگر `[cta]` همین را می‌سنجد.
- **مبلغ ورودی از رفرش جان سالم به در نمی‌برد.** `savePair` فقط نماد دو توکن را
  ذخیره می‌کند. قبلاً مبلغ هم ذخیره می‌شد و `restorePair` فیلد را پر می‌کرد
  بدون اینکه کوتی راه بیندازد (پر‌کردن برنامه‌ای رویداد `input` تولید نمی‌کند)،
  پس دکمه روی «Finding best route…» قفل می‌ماند تا کاربر عدد را پاک کند و از نو
  بزند. لینک اشتراکی دست‌نخورده است. کاوشگر `[refresh]`.

---

### ۵د. آمار بازدید — اندپوینت `/ev` (۱۷ آگوست ۲۰۲۶)

هدف: بدانیم کاربرها کدام نما را باز می‌کنند و کجا رها می‌کنند. کامیت `2aac370`.
⚠️ **روی سایت زنده نیست** — همان روز گیت‌هاب incident داشت و Webhooks در حالت
Degraded بود، پس رویداد push به کلادفلر نرسید و هیچ deploy تازه‌ای اجرا نشد.
کد سالم روی مخزن است؛ فقط زنگش نخورد.

**چرا سمت سرور نمی‌شود شمرد.** سایت با بایندینگ `[assets]` سرو می‌شود و وقتی
آدرس با فایلی در `_site` جور دربیاید، **Worker اصلاً اجرا نمی‌شود**. پس
بازکردن صفحه از سمت سرور دیده نمی‌شود؛ شمارش باید بیکن از خودِ صفحه باشد، به
مسیری که فایل ثابت نیست — همان الگوی `/gt` در بخش ۵ب. و چون سه نما با hash
عوض می‌شوند، هیچ آمارگیر معمولی‌ای `swap` و `folio` و `flow` را از هم تفکیک
نمی‌کند؛ شمارش داخل `setView` است که تنها نقطه‌ی ورود هر سه است.

**انبار:** Workers Analytics Engine — بایندینگ `ZX_EV`، دیتاست `zaexa_events`،
روی پلن رایگان، نگهداری ۳ ماه (پس انباشت دائمی نداریم). دیتاست را لازم نیست
دستی ساخت؛ اولین نوشتن می‌سازدش.

⚠️ **مرز حریم خصوصی — عمدی است، بدون پرسیدن گسترشش نده.** هر ردیف دقیقاً چهار
رشته است: نام رویداد · جزئیات از فهرست بسته (`""`/`inj`/`wc`) ·
`mobile`/`desktop` · کد کشور از `request.cf.country`.
ثبت **نمی‌شود**: آدرس کیف پول، مبلغ، توکن، IP، User-Agent، Referer، کوکی، و
**هیچ شناسه‌ی نشست یا بازدیدکننده**.
دلیلِ بی‌شناسه‌بودن حدس نیست: با ترافیک کم، «سواپ در ۱۴:۳۲» به‌علاوه‌ی تراکنشی
که همان دقیقه روی زنجیره نشسته، ردیف آمار را به یک کیف پول وصل می‌کند — حتی
بدون آدرس، و کشور که کنارش باشد بدتر. پس **قیف ریزش از نسبت شمارش‌ها درمی‌آید،
نه از دنبال‌کردن یک نفر.** `navigator.globalPrivacyControl` همه‌چیز را خاموش
می‌کند، یعنی هر عدد یک **کف** است نه مقدار دقیق.

سه نگهبان، چون ادعا در کامنت مرز حریم خصوصی را نگه نمی‌دارد:

1. **`EV_NAMES` در `index.html` و `EV_OK` در `worker/index.js` باید یکی باشند.**
   اگر صفحه نامی بفرستد که Worker ندارد، با ۴۰۰ بی‌صدا دور ریخته می‌شود و ما
   فکر می‌کنیم «کسی این کار را نمی‌کند» — بدترین حالت، چون عدد غلط شبیه عدد
   درست است. `check_event_allowlists()` در run.py.
2. **هیچ فراخوانی `ev()` نباید یک ویژگی بخواند.** `ev("swap:done",tokenIn.symbol)`
   یا `ev("x",$("amtIn").value)` در همان نگهبان می‌افتد، نه روی سایت زنده.
3. **آنچه واقعاً روی سیم می‌رود سنجیده می‌شود**، نه آنچه فکر می‌کنیم می‌رود.
   کاوشگر `[events]` روی همان سرور http محلیِ داخل run.py، و بیکن با
   `page.route` گرفته می‌شود. `evUrl` مثل `gtProxy` یک `let` است چون روی
   `file://` بیکن مقصدی ندارد.

هر سه با جهش عمدی آزموده شدند — ۱۴ جهش، همه گرفته شد.
⚠️ **آزموده نشده:** خودِ SQL API با توکن واقعی.

---

### ۵ه. ریویو ۱۸ آگوست ۲۰۲۶ — هشت مورد، همه بسته شد

ریویو با «خواندن سورس، بدون اجرا» انجام شده بود و خودش می‌گوید ممیزی نیست.
هر هشت مورد سنجیده شد، و **دو موردش با اجرای واقعی روی EVM بازتولید شد** — به
حرف ریویو اکتفا نشد.

| # | شدت | چه بود | کجا بسته شد |
|---|---|---|---|
| ۰۱ | HIGH | هر دو README قرارداد **v2 بازنشسته** را «Live contract» می‌نامیدند و `verify_dexes.sh` یک نسل عقب‌تر v1 را هاردکد کرده بود | آدرس زنده فقط `CHAIN.executor` است؛ اسکریپت از همان می‌خواند؛ نگهبان `[one address]` |
| ۰۲ | HIGH | README می‌گفت «no backend، nothing is proxied» در حالی که `/gt` و `/ev` با هر بازکردن صفحه اجرا می‌شوند | بخش Architecture بازنویسی شد با جدول «چه می‌کند / چه هرگز نمی‌کند» |
| ۰۳ | MEDIUM | با برگشت به مسیر جانشین، `Min received` روی صفحه از نقشه‌ی قبلی می‌ماند؛ کاربر کفی را امضا می‌کرد پایین‌تر از عددی که می‌دید | `kMin` از `chosen.minOut` دوباره رندر می‌شود و اعلان کف تازه را می‌گوید؛ کاوشگر `[fallback floor]` |
| ۰۴ | MEDIUM | `delivered = balance - before` برای کیف پول قراردادی که در `receive` جارو می‌کند **panic حسابی (0x11)** می‌داد، نه revert با پیام | کلمپ در هر دو شاخه؛ `SwapExecutor.v4.t.sol` |
| ۰۵ | MEDIUM | در شبیه‌سازی خروج، پیش‌شرط با کل سفارش سنجیده می‌شد نه با پای شبیه‌سازی‌شده؛ یک `blocked` واقعی به `estimated` نرم می‌شد | هر سه سنجش با `simAmount`؛ کاوشگرهای `[exit:split-precondition]`، `[exit:cached-balance]`، `[exit:cached-allowance]` |
| ۰۶ | LOW | `ETHERS_CDNS` یک آرایه با حلقه‌ی fallback بود و sha384 فقط کامنت | `ETHERS_SRC` تک‌رشته + `integrity` واقعی؛ نگهبان هش را از خودِ فایل بازمحاسبه می‌کند |
| ۰۷ | LOW | `okLogo` فقط روی مسیر ذخیره اجرا می‌شد، مسیر رندر آزاد بود | یک قاعده برای هر دو، به‌علاوه‌ی دفاع دوم در `paintAvatar` |
| ۰۸ | INFO | فهرست «Contract design» کامل به نظر می‌رسید ولی سه قدرت owner در آن نبود؛ `rescue`/`rescueETH` هیچ رویدادی نمی‌دادند | دو رویداد تازه + بند «What the owner key can still do» در README و در سرصفحه‌ی قرارداد |

**۰۴ چطور اثبات شد.** foundry در این محیط نصب نمی‌شود، پس با `solc` از npm و یک
EVM محلی (ganache + ethers) همان صحنه اجرا شد، یک بار روی قرارداد **قبل** از
اصلاح و یک بار **بعد**:

```
BEFORE the fix   revert data 0x4e487b71…0011   ← Panic(0x11) = سرریز حسابی
AFTER  the fix   ok, delivered = 0, sweep took payout + prior balance
```

هر سه ادعا جدا سنجیده شد: شاخه‌ی ETH، شاخه‌ی ERC-20 با قلاب گیرنده، و اینکه
کلمپ نگهبان را ضعیف نمی‌کند (کمبود واقعی حالا با پیام `slippage: output below
minimum` رد می‌شود، نه با panic).
⚠️ `forge test` را من اجرا نکردم — آن با توست.

**۱۳ جهش عمدی** روی اصلاح‌های وب زده شد و همه گرفته شدند. دو موردشان اول
**گرفته نشدند** و همان دو، دو نقص در خودِ تست‌ها را لو دادند: نگهبان آدرس با
پنجره‌ی ۴۰۰ نویسه‌ای یک خط «Live contract: <آدرس بازنشسته>» را می‌بخشید (حالا
برچسب باید روی همان خط باشد)، و دفاع دوم در `paintAvatar` چون `tokenLogo`
زودتر فیلتر می‌کرد هرگز اجرا نمی‌شد و برداشتنش هیچ تستی را نمی‌انداخت (حالا
جدا سنجیده می‌شود).

**v4 مستقر شد — ۱۸ آگوست ۲۰۲۶، `0x76082b0fbd0a29C236dD2ae2B2F47BFD96d7F455` (اکنون بازنشسته، جای خود را به v5 داد).**
قرارداد ارتقاناپذیر است، پس ۰۴ و ۰۸ فقط با قرارداد تازه به زنجیره رسیدند.
نسل قبلی بازنشسته شد و باگ ۰۴ روی همان زنده ماند؛ آدرسش فقط در جدول
«قراردادهای رها شده» بالا، در جدول Retired هر دو README و در
`RETIRED_EXECUTORS` می‌آید — جای دیگری به آن ارجاع نمی‌دهیم.

**و پیش از فرستادن، اثبات گرفته شد که آن تست‌ها چیزی می‌سنجند.** فایل v4 روی
قرارداد *قبل* از اصلاح اجرا شد (با `git stash` روی `src`، و یک نسخه‌ی موقت بدون
دو تست رویداد، چون آن دو حتی کامپایل نمی‌شوند — که خودش اثبات ۰۸ است):

```
FAIL test_nativeOut_sweepingWalletDoesNotPanic   panic 0x11
FAIL test_tokenOut_hookWalletDoesNotPanic        panic 0x11
FAIL test_nativeOut_genuineShortfall…            panic 0x11 ≠ "slippage: output below minimum"
PASS test_nativeOut_passiveWalletUnchanged
PASS test_tokenOut_passiveRecipientUnchanged
```

دو تست `passive` عمداً سبز ماندند: مسیر عادی هرگز خراب نبود و کلمپ آن را دست
نزده. نگهبان همان اسکریپت اول اشتباه بود — دنبال `delivered >= minAmountOut`
می‌گشت که در v3 هم بود، و روی فایلِ اصلاح‌نشده هم می‌گفت «اصلاح سرِ جاست».
نشانه باید چیزی باشد که *فقط* نسخه‌ی تازه دارد: خودِ کلمپ، یا رویدادها. همان
درس «با تطبیق رشته تصمیم نگیر»، این بار در ابزار تست.

---

### ۵و. بازبینی اصلاح‌ها — دو مورد تازه (۱۸ آگوست ۲۰۲۶)

بازبینی دوم تأیید کرد هر هشت مورد بسته است، و دو چیز تازه پیدا کرد که **هر دو
را خودِ اصلاح‌های من ساخته یا آشکار کرده بودند**.

**A (HIGH) — اصلاح ۰۶ برنامه را روی `file://` کشت.** وقتی `integrity` را واقعی
کردم، صفحه‌ای که مستقیم از روی دیسک باز شود دیگر بالا نمی‌آمد: مبدأ `null` است،
کروم برای طرح `file` هیچ CORS نمی‌دهد، پس اسکریپت **پیش از** مقایسه‌ی هش بلاک
می‌شد. هش و فایل کاملاً درست بودند؛ مسئله جای دیگری بود. بدتر از همه، پیام روی
صفحه می‌گفت «اتصال یا ad-blocker خودت را چک کن» — یعنی تقصیر را گردن کاربر
می‌انداخت برای کاری که خودِ صفحه با خودش کرده بود. و هر دو README همان مسیر را
به کاربر پیشنهاد می‌دهند.

با کرومیوم headless روی هر دو مبدأ سنجیده شد، قبل و بعد:

```
before   file://  ethers -> undefined   «Could not load ethers…»
         http://  ethers -> object      سالم
after    file://  ethers -> object      سالم
         http://  ethers -> object      سالم، و فایل دستکاری‌شده هنوز بلاک می‌شود
```

اصلاح: `integrity` فقط روی `http/https` اعمال می‌شود. SRI در برابر میزبانِ
دستکاری‌شده دفاع می‌کند و برای فایلی روی دیسک خودت چنین تهدیدی وجود ندارد، پس
چیزی از دست نرفت — و با یک جهش سنجیده شد که روی http هنوز فایل دستکاری‌شده
بلاک می‌شود.

**چرا سوییت تست نگرفته بودش:** هارنس `ETHERS_SRI` را خالی می‌کند، پس مرورگر
هیچ‌وقت با هش فعال اجرا نمی‌شد. حالا دو نگهبان هست: یکی ایستا (شرط پروتکل باید
بماند) و یکی زنده — `[file://]` که **خودِ `index.html` واقعی** را از روی دیسک
باز می‌کند و می‌سنجد `window.ethers` بالا آمده.

**B (LOW) — کیف پول جاروکننده هنوز می‌افتد، با توصیه‌ای که هرگز جواب نمی‌دهد.**
کلمپ پنیک را برداشت، ولی `require(delivered >= minAmountOut)` هنوز می‌افتد، چون
`minOutOf()` هیچ‌وقت صفر نیست (کوت منهای فی و لغزش). پس نتیجه‌ی عملی برای کاربر
عوض نشده بود؛ فقط شکل خطا عوض شده بود. و پیام می‌گفت «تحمل لغزش را بالا ببر» —
توصیه‌ای که **هرگز نمی‌تواند جواب بدهد**، چون علت حرکت قیمت نیست.

اصلاح در صفحه است، نه در قرارداد: وقتی همه‌ی کاندیدها با رشته‌ی `slippage`
افتادند، یک شبیه‌سازی اضافه با **کف صفر** اجرا می‌شود. اگر موفق شد و صفر
برگرداند، علت روشن است و پیام واقعیت را می‌گوید. فقط روی مسیر شکست اجرا می‌شود
و گس ندارد. کاوشگر `[recipient sweep]`.

**⚠️ کامنت قرارداد عمداً دست‌نخورده ماند.** آن کامنت («دیگر یکسره بلاک نمی‌شود»)
فقط وقتی `minAmountOut == 0` باشد درست است، و بازبین به‌درستی گیر داد. ولی
سورس v4 روی BaseScan با `exact_match` تأیید شده و متادیتای solc هش خودِ سورس را
دارد: **عوض کردن یک کاراکتر کامنت هم بایت‌کد را عوض می‌کند** و آن‌وقت مخزن با
قرارداد مستقر نمی‌خواند. تأیید دوباره هم ممکن نیست، چون آن آدرس قبلاً تأیید شده.
پس انتخاب این بود: یک کامنت بیش‌ازحد خوش‌بین در سورسِ مستقر، یا مخزنی که با
زنجیره نمی‌خواند. اولی را نگه داشتیم و اینجا ثبتش کردیم. **در v5 اصلاح شود.**

سه جهش زده شد و هر سه گرفته شدند: برداشتن شرط پروتکل (هم نگهبان ایستا هم
`[file://]` افتادند)، خاموش‌کردن تشخیص کف‌صفر، و نادیده‌گرفتن متن توضیح در
`showError`.

---

### ۵ز. لینک بررسی — حلقه‌ی رشد داخل خودِ محصول

**مسئله‌ی واقعی:** دو هفته، صفر سواپ، صفر فالوور. و آن «۱۰۰ بازدید» آمار خود
کلادفلر بود که خزنده‌ها را هم می‌شمارد — یک دامنه‌ی تازه ده‌ها بازدید از ربات
ایندکس و اسکنر امنیتی می‌گیرد. یعنی مسئله «چرا سواپ نکردند» نیست؛ **هنوز کسی
نیامده.** `/ev` هم تازه راه افتاده و درباره‌ی آن دو هفته چیزی نمی‌داند.

رقابت روی قیمت با 1inch و Odos برنده نیست: کاربر برای چند دهم درصد، ریسک
قرارداد اودیت‌نشده را نمی‌پذیرد. ولی exit check یک چیز دارد که آن‌ها ندارند و
**برای دیدنش لازم نیست کسی کیف پولش را وصل کند** — یعنی گیرنده‌ی لینک هیچ
ریسکی نمی‌کند تا جواب را ببیند. سؤال «این توکن هانی‌پات است؟» هر روز در
گروه‌ها پرسیده می‌شود.

`checkUrl()` یک لینک می‌سازد که مستقیم روی نتیجه‌ی یک توکن باز می‌شود:
`#swap?in=USDC&out=<token>&amt=100&check=1`. دکمه‌اش («Copy this check») داخل
همان پنلی است که توصیفش می‌کند. اگر کاربر مبلغی تایپ کرده باشد همان می‌رود؛
وگرنه اندازه‌ی مرجع ~۱۰۰ دلار، تا نتیجه‌ی دو لینک مقایسه‌شدنی باشد.

`check:open` هم به هر دو فهرست (صفحه و Worker) اضافه شد — بدون آن هیچ راهی
نبود بفهمیم لینک‌های به‌اشتراک‌گذاشته‌شده کسی را می‌آورند یا نه.

**دو باگ که خودِ کاوشگر گرفت، نه بازبینی:**

۱. نسخه‌ی اول همیشه USDC را طرف مقابل می‌گذاشت. برای توکنی که خودش USDC بود،
   لینک `in=USDC&out=USDC` می‌شد — مسیر حلقه‌ای، بدون رفت‌وبرگشت، و گیرنده یک
   **جعبه‌ی خالی** می‌دید. حالا اگر خروجی USDC باشد طرف مقابل ETH می‌شود.
۲. تلاش اول تست، لینک را روی همان سند باز می‌کرد. رفتن به هشِ دیگر فقط
   `hashchange` می‌دهد نه بارگذاری، پس پنل خالی می‌ماند — و این باگ خودِ تست
   بود نه باگ لینک. گیرنده‌ی واقعی صفحه را از نو باز می‌کند، پس کاوشگر هم
   همان کار را می‌کند.

دو جهش زده شد و هر دو گرفته شدند: برداشتن `check:open`، و برگرداندن USDC ثابت.

⚠️ **پیش‌نمایش لینک (OG) هنوز نیست.** لینکی که در تلگرام یا ایکس فرستاده شود
کارت پیش‌نمایش با جوابِ توکن نشان نمی‌دهد، چون این کار رندر سمت سرور در Worker
می‌خواهد. قدم بعدی همین است و اثرش روی اشتراک‌گذاری زیاد است.

---

### ۵ح. صفحه‌ی توکن — `/t/<آدرس>`

لینک بررسی اول به فرم سواپ می‌افتاد، و این غلط بود: کسی که با سؤال «این توکن
سالم است؟» می‌آید، روی صفحه‌ای می‌نشیند که از او می‌خواهد تصمیمی بگیرد که
هنوز جوابش را ندارد. حالا لینک به یک **صفحه‌ی جدا** می‌رود: نماد و لوگو و
نام، قیمت، آدرس با لینک به BaseScan، چهار عدد بازار (Market cap / FDV /
Volume 24h / Liquidity)، نتیجه‌ی exit check، فهرست ریسک‌ها، و یک دکمه‌ی
**Trade now** که با همان توکن به فرم سواپ می‌برد — بدون بارگذاری دوباره.

**هیچ منطقی تکرار نشد.** همان `renderTokenStats`، `renderExit` و `renderSafety`
اجرا می‌شوند؛ فقط *مقصدِ* رندر با یک سوییچ عوض می‌شود:

```js
let tokenPage=false;
const panel=id=>$( (tokenPage?"tk-":"")+id );
```

**چرا مسیر و نه هش.** هش هیچ‌وقت به سرور نمی‌رسد، پس با هش هرگز نمی‌شد کارت
پیش‌نمایش (OG) برای تلگرام و ایکس ساخت. با مسیر، Worker می‌تواند بعداً همان را
سمت سرور بسازد. بایندینگ `[assets]` برای مسیری که فایل ندارد ۴۰۴ می‌دهد، پس
Worker خودش `index.html` را برای `/t/<آدرس>` سرو می‌کند.

**باگی که همین کار ساخت و تست گرفتش:** روی `/t/0x…` هر مسیر نسبی — از جمله
`./ethers.umd.min.fd66c046.js` — نسبت به `/t/` باز می‌شود و ۴۰۴ می‌گیرد، پس صفحه با
«Could not load ethers» می‌ماند. **این روی سایت واقعی هم می‌افتاد، نه فقط در
تست.** درمان: یک `<base href="/">` که فقط روی همین مسیر تزریق می‌شود، تا باز
کردن فایل با `file://` (که README پیشنهاد می‌دهد) دست‌نخورده بماند.

کاوشگر `[token page]` لینک را از خودِ دکمه می‌گیرد، در یک صفحه‌ی تازه بازش
می‌کند، و می‌سنجد: فرم سواپ باز *نشده*، توکن نام‌برده شده، هر سه پنل پر
شده‌اند، و «Trade now» همان توکن را به فرم می‌برد. `view:token` هم به هر دو
فهرست رویداد اضافه شد.

سه جهش، هر سه گرفته شد: برداشتن `<base>`، نادیده‌گرفتن مسیر `/t/` در راه‌اندازی،
و «Trade now» که توکن را عوض کند.

**باگ دوم، که فقط روی سایت زنده دیده شد: `/t/0x…` → ۳۰۷ به `/`.**
Worker از `ASSETS` فایل `/index.html` را می‌خواست، و سرویس فایل‌های ثابت برای
`/index.html` طبق قاعده‌ی خودش ۳۰۷ به `/` می‌دهد. آن ریدایرکت از Worker بیرون
می‌رفت، مرورگر سر از صفحه‌ی اصلی درمی‌آورد، `pathname` دیگر `/t/…` نبود و
صفحه‌ی توکن هرگز باز نمی‌شد. درست: از `ASSETS` **ریشه** را بخواه، نه
`/index.html`. حالا `worker/test.mjs` همین را می‌سنجد (با یک ASSETS جاسوس که
مسیر خواسته‌شده را ثبت می‌کند) و جهشِ برگرداندن باگ گرفته می‌شود.

⚠️ **و درسِ تشخیص، که گران‌تر از خودِ باگ بود.** من دو بار بدون دیدن `location`
حدس زدم: اول گفتم قاعده‌ی trailing-slash است و `run_worker_first` را به
`wrangler.toml` اضافه کردم (بی‌اثر، بعداً برداشته شد)، بعد گفتم یک قانون
Redirect در پنل است و حسام رفت در داشبورد گشت. جواب با یک جدول چهارخطی آمد:

```
/t/0x8335…    307 -> https://zaexa.com/
/tx/0xabc     404
/hello-there  404
/gt/…         400        ⇒ Worker وصل است و اجرا می‌شود
/ev  POST     204
```

مسیرهای ناشناخته سالم بودند و فقط همین یکی ریدایرکت می‌شد — یعنی مقصر کدِ
خودمان بود، نه پنل. **اول رفتار را اندازه بگیر، بعد نظریه بساز.**

⚠️ یک درس درباره‌ی خودِ کاوشگرها: دو جهش اول اول با تایم‌اوت پلی‌رایت روی
`click("#tk-trade")` می‌افتادند و پیامشان «element is not visible» بود — یعنی
تست قرمز می‌شد ولی **علت را پنهان می‌کرد**. کلیک مشروط شد تا همان ادعایی که
علت را می‌داند حرف بزند. تست باید تشخیص بدهد، نه فقط بیفتد.

---

### ۵ط. صفحه‌ی توکن — ظاهر، انتظار، و سرعت (۲۵ آگوست ۲۰۲۶)

صفحه‌ی توکن دو بار بازطراحی شد. نسخه‌ی اول چیدمان صفحه‌ی سواپ را تکرار می‌کرد
و حسام گفت «زشته». درس واقعی این بود که مشکل سلیقه نبود، **تراکم** بود:
صفحه‌های حرفه‌ای داده‌ی مالی روی شبکه‌ی ۸ پیکسلی می‌نشینند و هوای اضافه ندارند.

چیدمان نهایی، از بالا: کارت هویت (لوگو ۵۶، نماد ۲۸، و وسطش با *واقعیت* پر
می‌شود — نام، تراشه‌ی «Base»، آدرس قرارداد؛ نسخه‌ی قبل همه‌چیز را به دو لبه
هل می‌داد و وسطش حفره بود)، نوار فشرده‌ی رفت‌وبرگشت، بعد نمودار و چهار عدد
بازار در ستون چپ و کارت ایمنی در ستون راست.

⚠️ **نسبت اندازه‌ها با اندازه‌گیری تنظیم شد، نه با چشم.** دو بار چشمی قضاوت
کردم و هر دو بار غلط بود. حالا اسکریپتی هست که ارتفاع واقعی هر دو کارت را از
مرورگر می‌گیرد. آخرین مقدار: کارت هویت **۱۱۴px**، نوار رفت‌وبرگشت **۱۰۸px**.
اگر روزی این نسبت عوض شد، همان را دوباره اندازه بگیر.

**انتظار = اسکلت، نه چرخ‌فلک.** نوار رفت‌وبرگشت، چهار عدد بازار و ردیف‌های
ریسک هرکدام شکل خودشان را به‌شکل `.sk` نشان می‌دهند تا کاربر بداند چه چیزی
دارد می‌آید و صفحه موقع رسیدن داده نپرد. زیر `prefers-reduced-motion` درخشش
خاموش می‌شود.

**امتیاز از صفر بالا می‌رود** (`animateScore`، ۹۰۰ms با easing) چون همین الان
سنجیده شده، نه اینکه از قبل آنجا بوده باشد.

**و یک سرعت واقعی، نه فقط ظاهر:** خواندن خودِ توکن پشت `verifyDexes()` منتظر
می‌ماند در حالی که هیچ ربطی به آن ندارد. حالا موازی اجرا می‌شود و فقط کوت و
exit check منتظر دروازه‌ها می‌مانند. نام و لوگو و اعداد بازار چند ثانیه زودتر
می‌نشینند.

سه نگهبان تازه در `run.py`: هیچ اسکلتی نباید بعد از نشستن صفحه باقی بماند
(اسکلتِ جامانده یعنی چیزی هرگز نرسید)، امتیاز نباید روی صفر بماند، و «Trade
now» نباید مبلغ ببرد — فقط جفت ارز. آن یکی از یک باگ واقعی آمد: عدد ۱۰۰ که
اندازه‌ی مرجعِ شبیه‌سازی بود به فرم سواپ می‌رفت، ۲۶ کوت بی‌دلیل روی RPC خرج
می‌کرد و به «شبکه جواب نداد» می‌رسید.

---

### ۵ی. کار موازی با یک ایجنت دیگر — درسی که گران درآمد

حسام هم‌زمان با یک ایجنت دیگر روی `web/index.html` کار می‌کرد. دستور همیشگی
من `cp` بود، و `cp` یعنی جایگزینی کامل: کار او را بی‌صدا پاک می‌کند و گیت هم
اعتراضی نمی‌کند، چون از دیدش فقط یک فایل عوض شده.

**آنچه واقعاً افتاد:** یک `git checkout -b` شکست خورد («branch already
exists»)، ولی زنجیره‌ی دستورها ادامه پیدا کرد و `cp` بعدی روی `main` نشست.
یعنی یک دستور شکست‌خورده وسط زنجیره کافی بود.

**راه درست، که جواب داد:** شاخه‌ای از *پایه‌ی خودِ من* (`64d37da`)، فایل من
آنجا، و بعد `git merge` به main. گیت جد مشترک را دارد و سه‌طرفه ادغام می‌کند:
هرجا او دست نزده مال من می‌آید، هرجا هر دو دست زده‌ایم **تعارض اعلام** می‌شود.
نتیجه: ۱۰۸ خط اضافه، صفر تعارض، کار هیچ‌کس گم نشد.

**قاعده از این به بعد:** یک نفر در هر فایل. پیش از ساختن هر چیزی، نسخه‌ی
فعلی از دیسک گرفته می‌شود (`Downloads/zaexa_current/`) تا کار تازه روی آخرین
نسخه سوار شود، نه روی کپی کهنه.

⚠️ یک ایجنت قدیمی‌تر هم به‌اشتباه پیام گرفت و **بخش ۵ح این فایل را پاک کرد**
(۶۷ خط حذف، صفر اضافه). با `git checkout --` برگشت. کد دست‌نخورده بود —
با md5 هر پنج فایل کلیدی سنجیده شد. اگر باز چنین چیزی شد: **اول اثر انگشت
بگیر، بعد قضاوت کن.**

---

### ۵ک. ✅ کارت پیش‌نمایش (OG) برای لینک توکن — ساخته شد

لینک `/t/0x…` در تلگرام و ایکس فقط یک آدرس خشک است. ربات‌های پیش‌نمایش
**جاوااسکریپت اجرا نمی‌کنند**، پس هرچه صفحه در مرورگر می‌سازد را نمی‌بینند.

برای کارت واقعی، Worker باید سمت سرور تگ‌های `og:title` / `og:description` /
`og:image` را در HTML تزریق کند — و برای اینکه کارت *حرف واقعی* بزند (نام
توکن، امتیاز ریسک، هزینه‌ی رفت‌وبرگشت) باید همان داده را سمت سرور بگیرد. یعنی
فراخوانی API با کش، و یک تصویر برندشده.

از قبل آماده است: مسیر `/t/*` از Worker رد می‌شود (`TOKEN_PAGE` در
`worker/index.js`)، و پراکسی `/gt` با کلید `CG_KEY` وجود دارد. **دلیلِ اینکه
از اول مسیر انتخاب شد نه هش، همین بود** — هش هیچ‌وقت به سرور نمی‌رسد.

نکته‌ی حیاتی: هر عددی که در کارت می‌آید باید همان چیزی باشد که صفحه نشان
می‌دهد. کارتی که «۰٫۱۰٪» بگوید و صفحه چیز دیگری، از نبودنِ کارت بدتر است.
به همین دلیل امتیاز ریسک و هزینه‌ی رفت‌وبرگشت **عمداً** روی کارت نمی‌آیند:
هر دو `eth_call` روی Base لازم دارند و Worker هیچ RPC ندارد.

**وضعیت: زنده، نسخه‌ی ۴** (`OG_IMAGE_V="4"`، اتگ `"b399f75f06ea4b65"`).
تصویر یک PNG ثابتِ ۱۲۰۰×۶۳۰ داخل خودِ Worker است (base64 در
`worker/og-image.js`) — نه در `_site`، چون خط Build command فقط در پنل
کلادفلر است و فایل تازه‌ای آنجا یعنی تغییر دستیِ نسخه‌نخورده.

🔴 **تصویر مولد ندارد.** هیچ HTML/SVG سازنده‌ای در مخزن نیست. یک بار ایجنتی
تلاش کرد کارت را «بهتر» کند و کل آن را از نو رندر کرد و خرابش کرد.
**قاعده: فقط کامپوزیت پیکسلی روی همان PNG** — ناحیه‌ی هدف را با درون‌یابی
خطیِ ستون‌به‌ستون پاک کن (نه رنگ تخت؛ گرید پس‌زمینه عمودی است)، تکه‌ی تازه را
جدا رندر و alpha-composite کن، و **اثبات بگیر**: بازه‌ی
`ImageChops.difference(old,new).getbbox()` باید داخل کادر هدف بماند.

🔴 **تلگرام بر اساس آدرس صفحه کش می‌کند، نه آدرس تصویر.** بالا بردن `?v=`
فقط ربات را وادار می‌کند فایل تصویر تازه را بگیرد؛ اگر تلگرام همان لینک
`/t/<addr>` را قبلاً دیده باشد اصلاً دوباره سراغ صفحه نمی‌رود. برای دیدن
کارت تازه لینک را با یک پارامتر بی‌اثر بفرست: `?r=3`. فرستادن همان لینک در
«چت تازه» کافی **نیست**.

**چهار چیز با هم جلو می‌روند وقتی تصویر عوض می‌شود:** `OG_PNG_ETAG` در
`worker/og-image.js` (۱۶ نویسه‌ی اول sha256 بایت‌های PNG) · `OG_IMAGE_V` در
`worker/og.js` · همان عدد در **دو** تگ ثابت `web/index.html` · و هیچ‌جای
دیگر عدد نسخه دستی نوشته نشود (هر دو تست `OG_IMAGE_V` را import می‌کنند).

⚠️ base64 باید **آرایه + `join("")`** باشد نه زنجیره‌ی `+` — acorn روی هزاران
`+` تودرتو stack overflow می‌دهد. طول هر تکه ۹۶ نویسه است.

---

## ۶. قرارداد — تصمیمات کلیدی

`contracts/src/SwapExecutor.sol`

1. **بدون calldata دلخواه** — قرارداد خودش فراخوانی را می‌سازد.
   جلوی حمله‌ی کلاسیک تجمیع‌کننده‌ها (دزدی از کاربرانی که approve داده‌اند).
   تست: `testCannotStealApprovedFunds`
2. **لیست سفید روتر** — تست: `testRejectsUnknownRouter`
3. **کارمزد از توکن ورودی** — تا اگر کاربر به توکن اسکم سواپ کرد، کارمزد ما
   بی‌ارزش نباشد. تست: `testFeeNotExposedToRiskyOutputToken`
4. **سقف سخت کارمزد ۱٪** — `constant` است، owner هم نمی‌تواند بالاتر ببرد
5. **غیرقابل ارتقا** — proxy نیست. باگ پیدا شد، قرارداد جدید دیپلوی می‌شود.
6. **بین تراکنش‌ها هیچ دارایی نگه نمی‌دارد** — و آنچه اتفاقی بیاید با
   `rescue` / `rescueETH` برمی‌گردد
7. **`receive()` فقط از WETH** — تست: `testOnlyWethCanSendEth`.
   ⚠️ `selfdestruct` از این محافظ رد می‌شود؛ برای همین `rescueETH` اضافه شد.
8. **مالکیت دومرحله‌ای** — `transferOwnership` بعد `acceptOwnership`
9. **هر مرحله سنجیده می‌شود، نه باور** — خروجی روتر نادیده گرفته می‌شود

سازنده: `constructor(address _owner, address _feeRecipient, uint256 _feeBps, address _weth)`

⚠️ اگر متغیر محلی جدید اضافه کردی و `Stack too deep` گرفتی: راه‌حل **استخراج
تابع داخلی** است (مثل `_validatePlan` و `_runParts`)، نه فعال کردن `via_ir`.

---

## ۷. اسکریپت‌ها

```bash
cd ~/web3_tools/contracts
./script/fork_test.sh      # anvil محلی روی بلاک پین‌شده + تست‌های fork (سریع، بدون 429)
./script/verify_dexes.sh   # جست‌وجوی سلکتور در بایت‌کد هر صرافی — PASS/FAIL با دلیل
./script/deploy.sh         # دیپلوی + لیست سفید. دروازه‌ی تست دارد؛ آدرس رابط وب را
                           # خودش به‌روز می‌کند و نتیجه را می‌خواند.
```

هر سه اول سراغ `contracts/.rpc` می‌روند و اگر نبود به RPCهای عمومی برمی‌گردند.

```bash
cd ~/web3_tools
./scripts/build_walletconnect.sh   # بازسازی بسته‌ی WalletConnect + تطبیق sha256
./scripts/ev_report.sh             # گزارش آمار بازدید از Analytics Engine (بخش ۵د)
```

`ev_report.sh` توکن را با `read -rsp` می‌گیرد تا روی صفحه نیفتد و در تاریخچه‌ی
شل نماند، و **قبل از هر کوئری یک هدف شاهد می‌زند** — وگرنه «توکن غلط» از
«شبکه وصل نشد» قابل تفکیک نبود (`curl` با کد ۰۰۰ جواب سرور نیست).
شمارش‌ها `SUM(_sample_interval)` هستند نه `COUNT(*)`، چون نمونه‌برداری وگرنه
عدد را بی‌صدا کم می‌کند. ⚠️ `toDate` در Analytics Engine وجود ندارد؛
`toStartOfDay` هست.

### تست‌ها

```bash
cd ~/web3_tools/contracts && forge test      # ۶۰ تست
cd ~/web3_tools/web/test  && python3 run.py  # سوییت رابط + خودش node worker/test.mjs را می‌زند
cd ~/web3_tools         && node worker/test.mjs  # فقط پراکسی، بدون شبکه
```

---

## ۸. محیط کاربر — نکات مهم

- **WSL Ubuntu روی ویندوز.** پروژه در `~/web3_tools`
- **محدودیت جغرافیایی:** بایننس و `mainnet.base.org` مسدودند
- **پروکسی ویندوز روی `127.0.0.1:10808`** فعال است ولی WSL از آن استفاده نمی‌کند
- فارسی در ترمینال چپ‌به‌راست نمایش داده می‌شود → **خروجی‌های اجرایی باید
  انگلیسی باشند.** کامنت داخل کد فارسی است و اشکالی ندارد.
- کاربر (حسام) صراحتاً گفته توان بررسی کد Solidity را ندارد →
  **تست خودکار جایگزین بررسی دستی است.** قبل از هر ادعای امنیتی، تستش را بنویس و اجرا کن.

### RPCهای Base — وضعیت آزمایش‌شده

| RPC | خواندن | نوشتن | یادداشت |
|---|---|---|---|
| Alchemy (در `.rpc`) | ✅ | ✅ | از WSL جواب می‌دهد |
| `https://1rpc.io/base` | ✅ | ✅ | بهترین عمومی |
| `https://base.drpc.org` | ✅ | گاهی | |
| `https://base-rpc.publicnode.com` | ✅ | ❌ 403 | |
| `https://base.meowrpc.com` | ❌ بدون eth_call | ✅ | |
| `https://mainnet.base.org` | ❌ | ❌ | Cloudflare مسدود کرده |

### 🔐 امنیت کلیدها

- کلید Alchemy فقط در `contracts/.rpc` است (`chmod 600`، در `.gitignore`).
- **در `web/index.html` هاردکد نشود** — سایت عمومی است و کلید داخل جاوااسکریپت
  قابل استخراج است. فقط بعد از فعال کردن domain allow-listing در داشبورد Alchemy.
- کلید خصوصی همیشه با `read -s` گرفته می‌شود و هیچ‌جا ذخیره نمی‌شود.
- `.gitignore` شامل: `.rpc` · `.env` · `*.key` · `**/deployment.txt` · `.fork-block`
- **کلید Alchemy یک بار در چت لو رفت — بهتر است rotate شود.**
- **کلید Etherscan/BaseScan لو‌رفته باطل شد ✅** (۱۱ آگوست). کلید تازه هست و
  فقط به‌شکل متغیر محیطی موقع `forge verify-contract` استفاده می‌شود.
- **کلید CoinGecko** در پنل کلادفلر به‌شکل **Secret** با نام `CG_KEY` است
  (Workers → zaexa → Settings → Variables and Secrets). نه در مخزن، نه در
  `index.html`. Worker آن را فقط در هدر می‌فرستد، نه در URL — وگرنه در کش لبه
  و لاگ‌ها می‌نشیند. تست‌های `worker/test.mjs` صریحاً ادعا می‌کنند کلید در
  URL و در پاسخِ مرورگر پیدا نمی‌شود.

---

## ۹. استراتژی — چرا این محصول

در پوشش (تعداد شبکه و توکن) با Matcha نمی‌شود رقابت کرد و **نباید تلاش کرد**.
تز رقابتی:

> **بهترین قیمت روی توکن‌های تازه‌ی Base، به‌علاوه‌ی تنها ابزاری که قبل از
> خرید نشان می‌دهد آیا می‌توانی بیرون بیایی.**

سه ستون:

1. **Exit check** ✅ ساخته شد — رفت‌وبرگشت واقعی. امتیاز نقدینگی رقبا از ریاضی
   عمق می‌آید و مالیات فروش، لیست سیاه و هانی‌پات را نمی‌بیند.
2. **پوشش کامل استخرهای Base** ✅ ۶ صرافی — یک شبکه، ولی همه‌ی استخرهایش.
3. **کار کردن جایی که رقبا کار نمی‌کنند** — بدون بک‌اند، قابل میزبانی روی IPFS،
   خواندن اول از کیف پول کاربر.

**درباره‌ی «smart money»:** جذاب است ولی جعلش آسان است، بک‌اند می‌خواهد، و باید
با سابقه‌ی اثبات‌شده منتشر شود. فعلاً پنل جریان پول کلاینت‌ساید کافی است.

📌 **تصحیح ۱۶ آگوست ۲۰۲۶ — این بخش یک بار بد خوانده شد.** جمله‌ی «در پوشش با
Matcha نمی‌شود رقابت کرد و نباید تلاش کرد» به‌غلط به‌عنوان «شبکه‌ی دیگر اضافه
نکن» ثبت و بازخوانی شد. حسام تصحیح کرد: «قرار نیست رقابت کنیم ولی دلیل نمی‌شود
شبکه‌های دیگر اضافه نکنیم و ایده‌های جدید نزنیم.»
یعنی تز درباره‌ی **کورکورانه کپی‌نکردن از رقبا** است، نه کوچک‌ماندن پروژه.
افزودن شبکه، روتر و بریج روی میز است (بخش ۱۰). محدودیت **فنی** (باگ سلکتور،
سقف نرخ، ارتقاناپذیری قرارداد) با محدودیت **محصولی** یکی نیست: اولی را محکم
نگه دار، دومی تصمیم حسام است.

---

## ۱۰. کارهای باقی‌مانده

- [x] رابط وب، بازطراحی، پورتفولیو، جریان پول، Exit check، کوت معکوس
- [x] ETH بومی در قرارداد — یک امضا برای USDC → ETH
- [x] دیپلوی `0x2fea35aa…` + لیست سفید ۵ روتر
- [x] آدرس جدید در `web/index.html`
- [x] `./script/verify_dexes.sh` روی قرارداد جدید — شش از شش دیده شد
- [x] باگ Exit check (قطعی شبکه و پیش‌شرط کیف پول از «فروش مسدود» جدا شد)
- [x] PancakeSwap V3: نسل روتر پیدا شد و شاخه‌ی legacy روی fork اثبات شد
- [x] لیست‌سفید کردن روتر پنکیک — هر شش روتر روی v3 در یک تراکنش لیست‌سفید شدند
- [x] تأیید سورس روی BaseScan — v2 هم روی BaseScan و هم روی Sourcify
- [x] بازبینی خط‌به‌خط کل کد + اصلاح ۱۷ مورد (یک XSS، پنج نقض قاعده‌ی اول)
- [x] دیپلوی v2 + لیست‌سفید شش روتر
- [x] تست واقعی سواپ با کیف پول روی v3 — انجام شد و کار می‌کند
- [x] لوگو (نشان گرادیانی در هدر) و حساب ایکس `@zaexadex`
- [ ] `LINKS.telegram` هنوز خالی است
- [x] انگلیسی‌شدن کامنت همه‌ی فایل‌های سالیدیتی (قرارداد + پنج فایل تست)
- [x] بستن ریشه‌ی سقف نرخ GeckoTerminal (بخش ۵ب)
- [x] دیپلوی v3 + تأیید سورس + عوض‌کردن آدرس در `index.html` و `stub-ethers.js`
- [x] کشف خودکار توکن‌های تازه‌ی Base (ستون سوم تز) — **ساخته شد** (بخش ۰ج):
      کرونِ ساعتی، انبارِ KV، سه مسیرِ خواندن، و صفحه‌ی `/pairs`.
      ✅ شکافِ نسخه ۴ بسته شد (۱۴ و ۱۵ سپتامبر، بخش ۰): کلیدِ واقعیِ استخر از
      لاگِ `Initialize` ایندکس می‌شود و یک توکنِ واقعی روی سایتِ زنده از
      «نامعلوم» به «فروش ممکن» رسید.
- [x] انتشار روی zaexa.com + مخزن گیت‌هاب
      Cloudflare Workers با static assets. تنظیمات در `wrangler.toml` داخل مخزن،
      نه فقط در پنل. Build command با `web/*.js` (گلوب، نه نام‌های صریح) و
      `web/_headers` را در `_site` می‌گذارد تا `web/test/` منتشر نشود و باندل‌های
      وندور — که حالا نامشان هش محتوایشان را دارد (`ethers.umd.min.fd66c046.js`،
      `walletconnect.bundle.9d119cbe.js`) — هر بار هشش عوض شد خط پنل را عوض
      نکند. `_headers` باندل‌های js را یک سال immutable کش می‌کند؛ نبودنش یعنی
      این قاعده بی‌صدا اثر نمی‌کند.
      `wrangler.toml` حالا `main = "worker/index.js"` هم دارد (بخش ۵ب).
      ⚠️ موقع اتصال دامنه: رکوردهای A/CNAME که از ثبت‌کننده وارد شده‌اند باید
      اول حذف شوند، وگرنه «externally managed DNS records» می‌دهد. کادر
      Subdomain برای دامنه‌ی اصلی باید **خالی** بماند.
- [ ] فعال کردن کارمزد وقتی آماده بودی:
      `cast send $SWAP_EXECUTOR "setFee(uint256)" 10 --rpc-url $RPC --private-key $PRIVATE_KEY`
- [x] **قرارداد v5 + تأیید سورس + آدرس در شش سند** (بخش ۱۱د)
- [x] **لوگوتایپ v1.3 روی هدر سایت و روی کارت OG** (بخش ۵ک)
      نسبت اندازه‌گیری‌شده ۰٫۴۸ روی هر دو. بریک‌پوینت ۳۸۰px در هشت عرض دوباره
      سنجیده شد و سالم است (لوگوتایپ نو ۳٫۱٪ پهن‌تر است).
      📌 باز: با خنثی‌کردن عمدی media query تا عرض ۳۰۰px هم چیزی نشکست، پس
      شاید پنهان‌کردن لوگوتایپ زیر ۳۸۰px دیگر لازم نباشد. تصمیم ظاهری است.
- [x] ربی «Unknown Signature Type» می‌گوید (باگ ۱۱) — [stated] حسام ۲۲ سپتامبر گفت
      حل شده است.
- [ ] `scripts/build_walletconnect.sh` بازنویسی شد ولی **اجرا نشد** (npm در
      کانتینر شبکه ندارد). دفعه‌ی بعدِ بازسازی باندل، اول همین را بسنج.

### 🎯 صفِ کارِ باز — به‌روز عصرِ ۲۳ سپتامبر ۲۰۲۶، به ترتیب

1. **سنجشِ زنده‌ی جدولِ سولانا:** در گزارشِ ۲۴ سپتامبر `too-big` برای پامپ‌فان باید تقریباً
   صفر شود و ردیف‌هایشان حکمِ واقعی بگیرند؛ یک توکنِ پامپ‌فان را هم با `/vd/<mint>` بسنج.
   اگر `too-big` ماند، ببین کدام آدرسِ رایج در جدول نیست (گیرنده‌های کارمزدِ پامپ‌فان عوض
   می‌شوند و عمداً در جدول نیستند).
2. **پایشِ دو دلیلِ تازه:** `v4:unproven` و `sells:recent` در گزارش‌های بعدی چقدرند؟ اگر
   `v4:unproven` زیاد شد، یعنی کوترِ نسخه ۴ روی استخرهای هوک‌دار باید بهتر شود — این
   ریشه‌ی واقعی است، نه وتو.
3. **کوترِ نسخه ۴ روی استخرهای هوک‌دار** — چرا روی استخرِ ۲۰۶ هزار دلاریِ SPIKE کوت
   نداد؟ با `/vd/<addr>?probe=1` شروع کن.
4. **ماکتِ بازطراحیِ صفحه‌ی سواپ** (پیشنهادِ ۲۳ سپتامبر، منتظرِ ماکت): برچسبِ بررسیِ فروش
   کنارِ «You receive»، نوارِ تقسیمِ مسیر به رنگِ هر صرافی، حرکت‌های کوچک (شمارشِ معکوسِ
   کوت، چرخشِ عدد، درخششِ کوت‌گیری)، کارتِ نتیجه‌ی بعد از سواپ. هاله‌ی رنگِ برند پشتِ
   کارت فقط با تأییدِ مالک (تمِ تیره قفل است). اول ماکت، بعد کد.
5. **`aerodrome-slipstream-3`:** حالا در گزارش دیده می‌شود (۳ بار با حکمِ sell در ۲۳
   سپتامبر). پیش از نگاشت در جدولِ ما، روی زنجیره ثابت کن روتری که صدا می‌زنیم همان استخر
   را کوت می‌دهد.
6. **تصویرِ کارتِ per-token** — تصمیمش با حسام (تلگرام بر اساسِ آدرسِ صفحه کش می‌کند).
7. **فایلِ فارکستر** — امضایش کارِ حسام است.
8. **رفت‌وبرگشتِ واقعیِ Base** با state override.
9. **سولانا برش ۲:** والت و اجرای سواپ از راه ژوپیتر.
10. **شاهدِ «استخر خالی» برای صرافی‌های غیرِ نسخه ۴.**

**کارهای کوچکِ باز:** ردیف‌های غلطِ ۷ سپتامبر در `report:2026-09-07` مانده‌اند ·
ارتفاعِ ردیفِ صفحه‌ی جفت‌ها ۶۱px (هدف ۵۶) · رویدادِ `share:copy` هنوز روی سایت دیده نشده ·
جمله‌ی درصدِ بازگشت روی صفحه‌ی توکن در حالتِ واقعی دیده نشده · ابزارهای پایتون با v5 کار
نمی‌کنند · CSP و محدودکننده فقط زیرِ ترافیکِ واقعی · `scripts/build_walletconnect.sh`
اجرا نشده · آزمونِ `[pairs logos] retry after failure` گاهی لرزان است · `frame-src` وال‌کانکت:
«تأییدشده»‌بودنِ دامنه در کیف‌پولِ موبایل هنوز با اتصالِ واقعی دیده نشده · اتصالِ واقعیِ
کیف‌پول روی `/pairs` (فقط تا کیوآر سنجیده شد) · هدرِ اپ در تمِ تیره ~۳٪ اختلافِ لبه‌ی نرم
دارد (پیامدِ sticky) · حاشیه‌ی بودجه‌ی گذر کم است (بدترین ۴۹).

**عمداً انجام نمی‌شود:** ردیابیِ referrer یا UTM · افزودنِ خودکارِ توکن به فهرستِ
مرجع · بک‌فیلِ لاگ برای کلیدِ نسخه ۴ · متن برای تاریخ‌های پیش از ۸ سپتامبر ·
آستانه‌ی بازگشت برای «فروش ممکن» (تصمیمِ حسام) · منبعِ سومِ لوگو · گرافیفای (سنجیده شد:
کدِ اصلی داخلِ سه فایلِ html است و آن را نمی‌خواند؛ کانتینر هم هر جلسه پاک می‌شود) ·
باز کردنِ فونت‌های `fonts.reown.com` در CSP · «هیچ nosellی بدونِ cause» روی همه (هانی‌پاتِ
کلاسیک را پاک می‌کرد).

**کارِ خودِ حسام:** فعلاً چیزی باز نیست. کلیدِ یک‌بارمصرفِ جدولِ سولانا را نگه دارد.
⚠️ حسام گفت کیف‌پولِ اصلی‌اش یک بار هک شده؛ پیشنهاد شد دارایی‌ها به کیف‌پولِ تازه با عبارتِ
بازیابیِ تازه منتقل شود. هیچ‌جای زَکسا از آن کیف‌پول استفاده نمی‌کند.

**یافته‌ی بازِ ثبت‌شده (کار نیست، تا شواهدِ تازه دست نخورد):** استخرهایی با
نقدینگیِ واقعی که *فقط* با USDC جفت‌اند پرسیده می‌شوند، ولی استخرهایی که توکن را
با یک توکنِ نامرتبط جفت کرده‌اند عمداً پرسیده نمی‌شوند. اگر روزی شواهدی آمد که
چنین مسیری خروجِ واقعی است، `VD_V4_STAGE_COUNTERS` جایی است که باید عوض شود.

---

### پنج کارِ ۱۶ آگوست (بایگانی — برای نگه‌داشتنِ استدلال‌ها، نه صفِ کار)

سه‌تا با «در صورت امکان» گفته شد: اول امکان‌سنجی، بعد ساخت.

1. ~~**گزارش رفتار کاربر.**~~ **ساخته و آزموده شد (۱۷ آگوست) → بخش ۵د.**
   حسام دامنه را تأیید کرد: نما + رویداد بی‌هویت، **بدون** جفت‌توکن؛ کشور و
   نوع دستگاه بله؛ زیرساخت روی خودمان، نه سرویس بیرونی. باقی‌مانده فقط انتشار
   است: یک deploy که به‌خاطر incident گیت‌هاب اجرا نشد، بعد ساختن توکن API
   (فقط Account Analytics: Read) و اجرای `scripts/ev_report.sh`.
   ⚠️ `/ev -> 204` ثابت **نمی‌کند** داده نوشته می‌شود — اگر بایندینگ نباشد
   عمداً بی‌صدا رد می‌کند تا سایت نشکند. اثبات نوشتن از پنل (Bindings) یا از
   ردیف‌های گزارش می‌آید.
2. **سفارش‌های لیمیت — طراحی شد (۱ سپتامبر)، فاز ۱ در نوبت است.**
   تصمیم حسام: **با v5 یکی نمی‌شود** و قرارداد خودش را می‌گیرد. دلیلش:
   قرارداد لیمیت می‌تواند خودش *کاربرِ* v5 باشد (توکن را از کاربر می‌کشد،
   `executeSwap` را صدا می‌زند، خروجی را به کاربر می‌دهد) و v5 هیچ تغییری
   لازم ندارد. هزینه‌اش فقط یک approve دوم است، آن هم فقط برای کسی که لیمیت
   می‌زند.
   ⚠️ **درِ وسوسه‌انگیزی که عمداً بسته شد:** می‌شد در v5 یک `allowedSettler`
   و `executeSwapFor(payer, …)` گذاشت تا قرارداد لیمیت از همان approve فعلی
   کاربر استفاده کند و approve دوم لازم نباشد. **رد شد.** امروز حتی یک روتر
   خرابکارِ لیست‌سفید هم فقط به مبلغِ در جریانِ همان تراکنش می‌رسد؛ آن در،
   هر قراردادی را که با کلید مالک ثبت شود قادر می‌کند کل موجودی approve‌شده‌ی
   همه‌ی کاربران را بکشد. ریسک از «ضرر یک معامله» به «ضرر کل موجودی» می‌پرد،
   برای پروژه‌ای که ممیزی رسمی نشده. اگر دوباره مطرح شد، همین استدلال را بیاور.
   **فاز ۱ = هشدار قیمت:** کاربر قیمت هدف می‌گذارد، ما خبرش می‌کنیم، خودش
   می‌زند. بدون قرارداد، بدون کیف داغ، بدون سرور ۲۴ساعته. **فاز ۲ (قرارداد +
   کیپر) فقط اگر مردم از فاز ۱ استقبال کردند** — گران‌ترین بخشِ لیمیت کد نیست،
   تعهد عملیاتی است: رباتْ بخوابد، سفارش پر نمی‌شود و تقصیر ماست.
3. **صفحه‌ی لندینگ.** ✅ FAQ و فوتر چندستونی ساخته شدند (حسام گفت «اول faq بعد
   لندینگ»). ⚠️ لندینگ **نه** به‌صورت تب چهارم در نوار بالا — آن نوار مالِ
   ابزارهاست، نه منوی سایت (تصمیم مالک).
4. **شبکه و روتر بیشتر + بریج با بهترین قیمت.** `chains.py` از قبل چندشبکه‌ای
   است. قید فنی: هر روتر تازه باید از `verify_dexes.sh` و تست سلکتور رد شود و
   آدرس‌ها با ethers واقعی چک‌سام شوند. (بخش ۹ را هم بخوان.)
   ⚠️ هشدارِ قدیمیِ «۳ از ۶ دکس مرده‌اند» **غلط بود** — از ماک می‌آمد نه از
   مین‌نت. هر هفت روتر زنده و لیست‌سفیدند.
   📌 با `KIND_ADAPTER` در v5، خانواده‌ی دکس تازه دیگر دیپلوی تازه لازم ندارد.
5. **جذب کاربر.** ایکس `@zaexadex`. تصویر پست انتشار باید از پکیج برند
   **v1.3** دوباره ساخته شود.

---

## ۱۱. ابزارهای پایتون (فاز اول پروژه)

| فایل | کار |
|---|---|
| `scanner.py` / `scan.py` | اسکنر ایمنی توکن |
| `router.py` / `route.py` | روتر — مستقیم + چندمرحله‌ای + تقسیم سفارش |
| `executor.py` / `swap.py` | ساخت و ارسال تراکنش |
| `discover_tokens.py` / `suggest_tokens.py` | پیشنهاد توکن مرجع (با فیلتر ضداسکم) |
| `chains.py` · `ratelimit.py` · `multirpc.py` | تنظیمات، محدودکننده‌ی نرخ، جابه‌جایی RPC |

```bash
python3 scan.py 0xToken · python3 route.py 100 USDC WETH · python3 swap.py 1 USDC WETH --execute
```

⚠️ **`swap.py --execute` روی قراردادِ مستقرشده‌ی امروز کار نمی‌کند.**
`executor.py:SWAP_STEP_COMPONENTS` هفت فیلد دارد؛ `SwapStep` در
`SwapExecutor.sol` هشت فیلد است (فیلدِ آخر `tickSpacing`، بخشِ ۱۱الف را
ببین) — یعنی سلکتورِ کدگذاری‌شده‌ی `executeSwap` با سلکتورِ واقعیِ قرارداد
یکی نیست. امن شکست می‌خورد چون `executor.py` پیش از ارسال شبیه‌سازی
می‌کند و شبیه‌سازی با سلکتورِ غلط رد می‌شود، نه این‌که تراکنش با پارامترهای
غلط روی زنجیره برود — ولی همچنان یعنی این دستور، همان‌طور که اینجا نوشته
شده، اجرا نمی‌شود.

⚠️ **`DRY_RUN` در محیط باقی می‌ماند.** بعد از یک بار `DRY_RUN=false`، اجراهای
بعدی هم واقعی خواهند بود. قبل از تست `unset DRY_RUN` بزن.

---

## ۱۱الف. قرارداد v2 — چه چیزی عوض شد و چرا

v1 (`0x2fea35aa…`) هیچ مسیر سرقتی نداشت؛ بازبینی خط‌به‌خط تأییدش کرد. ولی شش
ایراد داشت که چون قرارداد ارتقاپذیر نیست، تنها راهشان دیپلوی دوباره بود — و
چون هنوز هیچ کاربری approve نداده بود، هزینه‌ی این جابه‌جایی **صفر** بود.
این پنجره بعد از انتشار بسته می‌شود.

۱. **مقدار هر مرحله از تفاضل موجودی خوانده می‌شود، نه از عدد برگشتی روتر.**
   تنها موردی که به کاربر واقعی آسیب می‌زد: توکن fee-on-transfer در مسیر
   چندمرحله‌ای. `_swap` حالا قبل و بعد را می‌سنجد و `_callRouter` جداست.
۲. **مالکیت دومرحله‌ای.** `setOwner` حذف شد. حالا `transferOwnership` و بعد
   `acceptOwnership` از سمت مالک جدید. یک آدرس اشتباه دیگر قرارداد را
   فریز نمی‌کند.
۳. **`rescueETH()`** — ETHی که با `selfdestruct` تحمیل شود دیگر برای همیشه
   قفل نمی‌ماند. (اگر مالکیت را به قراردادی مثل multisig دادی، مطمئن شو
   `receive` دارد وگرنه این تابع کار نمی‌کند.)
۴. **`revokeApprovals(tokens, router)`** — بستن allowance روتری که از لیست
   سفید حذف شده. خودکار نیست، چون قرارداد تاریخچه‌ی approve را ذخیره نمی‌کند.
۵. **بخشی که سهمش به صفر گرد شود revert می‌کند** به‌جای `continue` بی‌صدا.
۶. **`_ensureApproval` با «چقدر لازم داریم» مقایسه می‌کند**، نه با
   `type(uint128).max`. توکن‌های با allowance در uint96 هرگز به آن آستانه
   نمی‌رسیدند و هر مرحله دو SSTORE اضافه می‌خوردند.

`Swapped` هم مقدار *واقعاً دریافتی* را لاگ می‌کند، نه عدد اعلامی.

---

## ۱۱ب. PancakeSwap V3 — دو نسل SwapRouter

روتر `0x1b81D678…` روی Base **نسل اول** است. بایت‌کدش `0x414bf389`
(ساختار هشت‌فیلدی، با `deadline`) دارد و `0x04e45aaf` را **ندارد**.
کوترش اما QuoterV2 است (`0xc6a5026a`)، پس کوت‌گیری با بقیه‌ی V3 یکی است.

در `index.html` با `kind:KIND.V3` ثبت شده بود — یعنی تابعی صدا زده می‌شد که
وجود ندارد. حالا `KIND.V3_LEGACY` است.

⚠️ **`kind` را تنها عوض نکن.** پنج جای دیگر `d.kind===KIND.V3` را چک می‌کردند
(دروازه، ساخت venue، برچسب، `encodeQuote`، اسکن نقدینگی). همه به کمکی
`isV3(d)` تغییر کردند. تنها جایی که دو نسل باید از هم جدا بمانند `swapSigOf`
است. اگر یکی جا بیفتد، آن صرافی **بی‌صدا** از مسیریابی حذف می‌شود.

اثبات روی زنجیره: `testRealPancakeLegacySwap` (گس ۴۸۵۰۴۸ — عدد واقعی، نه skip)،
`testModernKindFailsAgainstLegacyRouter`، و `testRealMixedGenerationRoundTrip`.
عمیق‌ترین استخر USDC/WETH پنکیک در tier ۰.۰۱٪ است — ولی تست آن را از factory
می‌پرسد و هاردکد نمی‌کند، تا خشک شدن استخر به‌صورت «قرارداد خراب است» ظاهر نشود.

---

## ۱۱ج. قرارداد v3 — چه چیزی عوض شد و چرا

> ⚠️ **v3 بازنشسته است** (۱۸ آگوست ۲۰۲۶، باگ ۰۴). قرارداد فعال در بخش ۱ است.
> این بخش تاریخ است، نه راهنما — آدرس v3 عمداً اینجا تکرار نشده؛ فقط در جدول
> «قراردادهای رها شده» بالا می‌آید.

۱۶ آگوست ۲۰۲۶ — سورس روی BaseScan تأییدشده، هر شش روتر در یک تراکنش
لیست‌سفید، fee = 0 bps. چک‌سام EIP-55 آدرس قبل از گذاشتن در کد با ethers
**واقعی** سنجیده شد.

دو ایرادی که کدکس در بازبینی v2 پیدا کرده بود و اینجا بسته شد:

**۱) باقیمانده‌ی گردکردن در سفارش تقسیم‌شده.** تقسیم صحیح چند wei از توکن ورودی
را در قرارداد جا می‌گذاشت — ناچیز، ولی مال کاربر بود و انباشته می‌شد. حالا
پایه‌ی آخر `swapAmount - spent` را برمی‌دارد؛ چون مجموع `p.amountIn` دقیقاً
برابر `totalAmountIn` است (تضمین‌شده در `_validatePlan`)، این همیشه مثبت است.
تست `test_splitOrderLeavesNoDust` — کارمزد عمداً روی ۳۰ bps است، چون با کارمزد
صفر تقسیم دقیق می‌شود و تست روی قرارداد معیوب هم سبز می‌شد.

**۲) `minOut` روی کیف کاربر، نه قرارداد.** قبلاً `minAmountOut` مقداری را
می‌سنجید که به **قرارداد** رسیده بود. برای توکن‌های fee-on-transfer این دو یکی
نیستند، پس حفاظت لغزش می‌توانست سبز باشد در حالی که کاربر کمتر گرفته بود. حالا
`userBefore = balanceOf(msg.sender)` و `delivered = balanceOf(msg.sender) - userBefore`.

**روند دیپلوی که جواب داد:** `./script/fork_test.sh` (هشت تست fork روی وضعیت
واقعی Base، سبز) → `./script/deploy.sh` → تأیید سورس با `ETHERSCAN_API_KEY` →
عوض‌کردن آدرس در **دو** جا → `verify_dexes.sh`.
⚠️ آدرس باید در `web/index.html` **و** `web/test/stub-ethers.js` عوض شود. اگر
دومی جا بماند، دروازه‌ی سوم در سوییت وب بی‌صدا از کار می‌افتد.

---

## ۱۱د. قرارداد v5 — Slipstream و قلاب آداپتور (۱ سپتامبر ۲۰۲۶)

`0x15e511Bf2Ea1a0F50F25E973d57Dce0D01946b6d`

### چرا اجباری شد — اثبات‌شده، نه حدس

یک توکن تازه‌ی Base که کل بازارش یک استخر Aerodrome Slipstream (نقدینگی
متمرکز) است، در v4 **اصلاً قابل معامله نبود**. نه گران‌تر — ناممکن.
شاهد روی زنجیره (۳۰ آگوست): ALIGN، ۶۳۸۲ توکن، تنها بازارِ موجود
`Slipstream tick=100` با ۶۸٫۵۳ USDC. یونی‌سواپ ۱۴۰٫۲۴ داد (از همین استخر،
رابطشان «Aerodrome V3» صدایش می‌زند)؛ ما ۳۰٫۱۷ از یک مسیر چندپله‌ی بد.
**و تز محصول ما دقیقاً توکن‌های تازه‌ی Base است.**

⚠️ برای توکن‌های جاافتاده Slipstream سودی ندارد (±۰٫۱۱٪ روی ۱۲ توکن). تفاوت
فقط در توکن‌های تازه است — ولی آنجا تفاوتِ «کار می‌کند» و «کار نمی‌کند» است.

**چرا v4 نمی‌توانست:** `KIND_MAX = 3`، و `_callRouter` چهار شاخه‌ی هاردکد
دارد و **هیچ مسیر passthrough ندارد**. `kind=4` روی `bad kind` می‌افتاد و
هیچ شاخه‌ای سلکتور `0xa026383e` نمی‌ساخت. قرارداد ارتقاناپذیر است ⇒ دیپلوی تازه.

### 🔴 دامِ مرکزی: سه شکل ساختار که نباید قاطی شوند

```
Slipstream  exactInputSingle((address,address,int24, address,uint256,uint256,uint256,uint160))  0xa026383e
V3_LEGACY   exactInputSingle((address,address,uint24,address,uint256,uint256,uint256,uint160))  0x414bf389
V3 (02)     exactInputSingle((address,address,uint24,address,uint256,uint256,uint160))          0x04e45aaf
کوتر CL     quoteExactInputSingle((address,address,uint256,int24, uint160))                     0x9e7defe6
کوتر V3     quoteExactInputSingle((address,address,uint256,uint24,uint160))                     0xc6a5026a
getPool CL  getPool(address,address,int24)                                                      0x28af8d0b
getPool V3  getPool(address,address,uint24)                                                     0x1698ee82
```

ساختار Slipstream **هم‌شکلِ** نسل اول است — هشت فیلد با `deadline` — و تنها
تفاوتش `int24 tickSpacing` به‌جای `uint24 fee` است. برای هر مقدار مثبت،
ABI-encode **بایت‌به‌بایت یکسان** است؛ فقط سلکتور فرق دارد، چون سلکتور از
نام نوع ساخته می‌شود نه از عرضش. هر کس از روی شکل قضاوت کند وسوسه می‌شود
زیر `KIND_V3_LEGACY` بگذاردش → **revert بی‌پیام**.
همه‌ی اعداد بالا با `cast sig` و از سورس رسمی
`github.com/velodrome-finance/slipstream` تأیید شده‌اند، نه از حافظه.

**سه تست این دام را قفل کرده‌اند:** یکی واحد
(`testSlipstreamKindAgainstLegacyOnlyRouterReverts`) و دو تای دیگر روی fork
با بایت‌کد واقعی — `kind=4` روی روتر نسل‌اول، و `kind=3` روی روتر Slipstream،
هر دو باید revert شوند.

### چه چیزی عوض شد

- `KIND_SLIPSTREAM = 4` با اینترفیس `ISlipstreamRouter` خودش،
  `KIND_ADAPTER = 5`، و `KIND_MAX = 5`.
- `SwapStep` یک فیلد **در انتها** گرفت: `int24 tickSpacing`.
  تاپل ABI حالا `(uint8,address,address,address,uint24,bool,address,int24)`.
- `require(s.tickSpacing > 0)` فقط برای `kind == 4`.
- `allowedAdapter` — مپینگ **جدا** از `allowedRouter`، با
  `setAdapterAllowed` / `setAdaptersAllowed` و رویداد `AdapterAllowed`.
  آدرسِ یک لیست از راه لیست دیگر **قابل استفاده نیست** (دو تست).
- کامنت خوش‌بین کنار `require(delivered >= minAmountOut)` اصلاح شد؛ ادعایش
  فقط وقتی `minAmountOut == 0` بود درست بود (بدهیِ v4 که عمداً آنجا ماند،
  چون سورس با `exact_match` تأیید شده بود و تغییر یک کاراکتر کامنت هم
  بایت‌کد را عوض می‌کرد).

### چرا قلاب آداپتور، و چرا امن‌تر از مسیر روتر است

هدف: خانواده‌ی دکس بعدی بدون دیپلوی تازه اضافه شود، چون هر دیپلوی یعنی هر
کاربری که approve داده باید دوباره بدهد.

**مسیر آداپتور از مسیر روتر تنگ‌تر است، نه فقط متفاوت:**
`_ensureApproval` به هر روتر `type(uint256).max` می‌دهد و آن اجازه **می‌ماند**.
آداپتور **هیچ allowance نمی‌گیرد** — دقیقاً همان مبلغِ در جریان با یک
`transfer` مستقیم به دستش می‌رسد و بعد از پایان تماس هیچ اجازه‌ای روی
دارایی‌های قرارداد ندارد. کالدیتای دلخواه از سمت کاربر همچنان قبول نمی‌شود؛
قرارداد همچنان خودش هر تماس را می‌سازد.

قرارداد آداپتور باید سه چیز را رعایت کند: با موجودیِ **واقعی** خودش معامله
کند (نه `amountIn` اسمی، به‌خاطر توکن‌های fee-on-transfer)، خروجی را به
`recipient` بفرستد، و بداند که هرچه پس نفرستد از دست می‌رود — محافظِ کاربر
همان `minAmountOut` سطحِ سفارش است، نه این اینترفیس.

### تست‌ها

**۸۲ تست واحد** (۵۹ قبلی + ۲۱ v5 + ۲ سلکتور) و **۱۱ تست fork** روی مین‌نت،
همه سبز. `testRealSlipstreamSwap` با گس **۵۴۶٬۱۶۴** و
`tickSpacing = 50` — یعنی سواپ واقعی از استخر واقعی. **ماک نمی‌توانست این
را ثابت کند**، چون ماک را هم خودمان با همان تصور می‌نویسیم که ممکن است غلط
باشد؛ فقط بایت‌کد واقعی زنجیره می‌توانست ردش کند.

**تست‌ها هم آزموده شدند.** چهار بار عمداً کد شکسته شد و هر بار همان تستی که
باید افتاد، تمیز و بدون crash: شاخه‌ی Slipstream به نسل قدیم برگردانده شد ·
شرط `tickSpacing > 0` حذف شد · دو لیست‌سفید قاطی شدند · به آداپتور approve
داده شد.

⚠️ `forge` **در کانتینر ایجنت نصب‌شدنی است** (از ریلیز گیت‌هاب؛
`foundryup` و `binaries.soliditylang.org` بسته‌اند، پس `solc-static-linux`
را جدا بگیر و با `--use` بده، و `lib/forge-std` را جدا clone کن). یعنی
تست‌های واحد را ایجنت خودش اجرا می‌کند؛ فقط تست fork کار کاربر است.

### روند دیپلوی و شش سندی که آدرس دارند

`./script/fork_test.sh` → `./script/deploy.sh` → تأیید سورس → عوض‌کردن آدرس.
⚠️ **آدرس در شش جا:** `web/index.html` (`CHAIN.executor` — منبع حقیقت) ·
`web/test/stub-ethers.js` (`const EXEC`) · `README.md` (خط Live + جدول
Retired) · `contracts/README.md` · `PROJECT_STATUS.md` · و
`RETIRED_EXECUTORS` در `web/test/run.py`. نگهبان `[one address]` می‌سنجد.

⚠️ **تأیید سورس را با چشمِ خودت ببین، نه با پیام forge:**
`getsourcecode` از API باید `SourceCode` غیرخالی بدهد.
⚠️ **تست fork را فقط با `./script/fork_test.sh` بزن.** یک `forge test` ساده
روی اندپوینت عمومی بلاک را پین نمی‌کند و با `429 rate limit` قرمز می‌شود —
که جواب سرور است، نه جواب قرارداد.

### دو نگهبان تازه که دو شکافِ خاموش را بستند

**`[dex parity]`** — بعد از دیپلوی، `verify_dexes.sh` شش صرافی چاپ کرد و همه
`PASS`، در حالی که اپ هفت تا داشت: **Slipstream اصلاً سنجیده نشد** و خروجی
کاملاً سبز بود. اسکریپت فهرست هاردکد خودش را داشت. نگهبان تازه می‌سنجد که
`verify_dexes.sh` دقیقاً همان روترها و کوترهای `DEXES` در `index.html` را
داشته باشد، و **بایت‌به‌بایت** — یعنی حتی حروف بزرگ و کوچک. همین بند یک باگ
دوم را هم لو داد: کوتر پنکیک در آن اسکریپت با `cEF` نوشته شده بود به‌جای
`CeF`، یعنی چک‌سام EIP-55 غلط — همان باگی که قبلاً در `index.html` رفع شده
بود ولی در فایل خواهرش نه.
**قاعده: «همه PASS» را با «همه سنجیده شد» یکی نگیر. اول بشمار.**

**`[og etag]`** — هیچ تستی نمی‌سنجید که `OG_PNG_ETAG` هشِ همان بایت‌هایی
باشد که سرو می‌شوند. تست ۳۰۴ فقط هر اتگی را که سرور می‌داد به خودش پس
می‌داد، پس یک اتگِ کهنه از همه‌ی تست‌ها سبز رد می‌شد — و چون هدر
`cache-control: immutable` است، کلادفلر **تا ابد** تصویر قدیمی را می‌داد.
حالا `worker/test.mjs` بایت‌های سروشده را هش می‌کند و می‌سنجد.

هر دو نگهبان با شکستنِ عمدی آزموده شدند.

---

## ۱۲. تله‌های فنی که قبلاً خوردیم

**`forge verify-contract` بدون کلید بی‌صدا مسیرش را عوض می‌کند.** ۱۸ آگوست
۲۰۲۶: قدم ۴ دیپلوی اجرا شد، فورج گفت `Contract successfully verified` و
`exact_match` — ولی روی **Sourcify**، نه BaseScan. README به
`basescan.org/…#code` لینک می‌داد و می‌گفت «source verified on BaseScan»، و آن
لینک هیچ سورسی نشان نمی‌داد. یعنی یک پیام سبز، و یک سند نادرست. با
`--etherscan-api-key` دوباره اجرا شد (`Pass - Verified`) و با
`getsourcecode` جدا سنجیده شد. `deploy.sh` حالا اگر کلید نباشد هشدار می‌دهد.
درس: «ابزار گفت موفق» ادعا نیست؛ همان چیزی را بسنج که کاربر می‌بیند.

- **`forge create`** گاهی «contract was not deployed» می‌گوید در حالی که قرارداد
  واقعاً دیپلوی شده. با `cast compute-address` + `cast code` چک کن.
- **`--constructor-args` باید آخرین آرگومان `forge create` باشد**، وگرنه بقیه را می‌بلعد.
- **تست fork که «سبز» است ولی گس ۲۴۱۶ دارد** یعنی skip شده، نه پاس.
  اگر `BASE_RPC_URL` نباشد اسکریپت باید بلند اعلام کند.
- **429 پشت‌سرهم در forge:** بلاک fork را ثابت نگه دار (`.fork-block`)، وگرنه
  کش forge هر بار باطل می‌شود. `fork_test.sh` این کار را می‌کند.
- **`tx.wait()` که برنمی‌گردد:** RPC رسید را سرو نمی‌کند. به‌جایش منبع حقیقت را
  poll کن (مثلاً `allowance` هر ۳ ثانیه تا ۹۰ ثانیه) و در آخر بگو «ارسال شد
  ولی تأیید نشد» — نه اینکه بی‌نهایت بچرخد.
- **`approve` که گیر می‌کند** همین بود.
- متن خطای برگشتی از توکن با `escapeHtml` فرار داده می‌شود تا توکن مخرب
  نتواند در رابط markup تزریق کند.
- **`2>/dev/null` در bash خطای شبکه را به «جواب منفی» تبدیل می‌کند.**
  `verify_dexes.sh` با RPC قطع، هر شش صرافی را `FAIL: no contract` گزارش
  می‌کرد و با exit code صفر تمام می‌شد. حالا `try_cast` کد خروجی را از
  خروجی جدا نگه می‌دارد و run ناقص با exit 3 تمام می‌شود.
- **دروازه‌ی کوتر هم باید سلکتوری باشد، نه فقط «قرارداد وجود دارد؟».**
  همان شکاف باگ SwapRouter02، یک قدم آن‌طرف‌تر. حالا در هر دو جا
  (`verifyDexes` و `verify_dexes.sh`) دنبال `0xc6a5026a` می‌گردیم.
- **استاب تست از واقعیت عقب می‌ماند و کسی نمی‌فهمد.** آدرس `EXEC` در
  `stub-ethers.js` هنوز قرارداد رهاشده بود، پس `allowedRouter` هرگز
  match نمی‌شد و دروازه‌ی سوم اصلاً در تست اجرا نمی‌شد. هر وقت آدرسی در
  `index.html` عوض شد، استاب را هم چک کن.
- **`curl` که `000` می‌دهد جواب سرور نیست** — یعنی اصلاً وصل نشد. یک بار همین
  `000` به‌عنوان «سرویس خراب است» خوانده شد، در حالی که فقط VPN قطع بود و حتی
  خودِ `zaexa.com` هم جواب نمی‌داد. **همیشه یک هدف شاهد در دستور بگذار** تا
  «لینک من قطع است» از «سرویس خراب است» جدا شود.
- **کلید خالی به ۴۰۱ می‌انجامد و ۴۰۱ به نتیجه‌گیری غلط.** `read -rsp` چیزی روی
  صفحه نشان نمی‌دهد، پس راحت می‌شود بی‌پیست‌کردن Enter زد. یک بار همین باعث شد
  دو خط `401` به «پلن رایگان اندپوینت onchain را نمی‌دهد» تفسیر شود که غلط بود.
  هر دستوری که راز می‌گیرد باید یک خط شاهد داشته باشد (`len=${#VAR}` یا یک
  فراخوانی بی‌کلید).
- **ماک گاهی شبیه رگرسیون به نظر می‌رسد.** در هارنس `image_url` یک `data:` URL
  است و `price_usd` بی‌معنا (USDC = ۱۹۰۸ دلار). یک اعتبارسنجی `https`-only
  باعث شد `[icons]` از ۲ به ۰ برود و لحظه‌ای شبیه باگ واقعی به نظر برسد.
  همان قاعده‌ی دوم، از جهت مخالف.
- **اثبات «فقط کامنت عوض شده».** ادعا کافی نیست. کامنت‌ها را از هر دو نسخه حذف
  کن و باقیمانده را بایت‌به‌بایت مقایسه کن (برش‌دهنده باید رشته‌های متنی را
  بشناسد تا `//` داخل یک رشته را کامنت نخواند). خودِ ابزار هم باید آزموده شود:
  تغییر کامنت → یکسان، جابه‌جایی یک فاصله در کد → متفاوت.
