# ⚠️ بدون این فایل، یک کلونِ ویندوزیِ پیش‌فرض (core.autocrlf=true) سطرهای
# پایانِ خط را روی هر فایلِ متنی به CRLF تبدیل می‌کند. باندل‌های وندور در
# web/ (ethers.umd.min.fd66c046.js، walletconnect.bundle.9d119cbe.js) نامشان
# را از هشِ محتوایشان می‌گیرند و index.html روی ethers یک SRI هم دارد —
# یعنی بایت‌ها اینجا معنا دارند، نه فقط متن. تبدیلِ خط، هش را عوض می‌کند،
# نام دیگر با محتوا نمی‌خواند و check_asset_cache_headers در web/test/run.py
# همان‌جا با یک خطای واضح می‌ترکد، قبل از این‌که اولین تستِ مرورگر اصلاً
# اجرا شود — یعنی کسی روی ویندوز نمی‌توانست این سوییت را اجرا کند.
# پس: هیچ فایلی، هیچ‌جا، تبدیل نمی‌شود. بایت‌ها همان بایت‌هایی می‌مانند که
# کامیت شده‌اند.
* -text
